# Finish-Game Marathon `/goal` Prompt

Use the compact prompt below in a fresh Codex chat window. It is intended to stay under the 4,000-character `/goal` limit and starts the next implementation marathon from the current shipped Game Day player-arc/player-memory/draft-trust/tooling/receipt state.

## Compact Paste Prompt

Character count: 3586.

```text
/goal Continue Mr. Football Dynasty in `/Users/tkevinbigham/MFD/MFD-main`.

Mission: implementation marathon, not planning. Read live docs/source, score candidates, ship the highest-leverage safe vertical slice with tests/docs/status, then continue into the next independent slice only while clean.

First reads: `AGENTS.md`, `STATUS.md`, `CODEX_GAME_GUIDE.md`, `CODEX_IMPROVEMENT_PLAN.md`, `CODEX_GOAT_MARATHON_PROMPT.md`, `CODEX_FINISH_GAME_MARATHON_PROMPT.md`, `DESIGN.md`, `README.md`.

Kickoff checks: confirm `pwd`, `.git` presence, `SAVE_VERSION` in `packages/engine/src/config/difficulty.ts` (last known 36), root `packageManager` (last known `pnpm@9.15.9`), build artifact, and whether `pnpm` is on PATH. Treat source/tests as truth and docs as maps.

Latest shipped slice: Game Day Player Arc Follow-Up. `/game-day` reads saved `GameDayPackage.topPerformers`, `recordsMoments`, and `milestoneMoments`, de-dupes players, links them to profiles, preserves saved narratives/stat lines, and routes only to Player Development or Record Book. It adds no package writer, player-history/timeline writer, record/milestone writer, save/schema change, week advance, sim/RNG, route registration, autosave, or sidecar work.

Do not redo shipped slices: Game Day Player Arc Follow-Up, Draft Recap Class Follow-Through, Week Advance Rookie Class Follow-Up, Draft Recap Player Profile Links, Player Timeline/Profile Draft Recap Memory, Draft Post-Draft Recap CTA, Draft War-Room Durable Receipt/Browser Proof/Real-Backed Generation/Live Order Ownership, GOAT Release Sentinel, one-click combined backup, CPU Intent Ledger/Strategy History, Position Coach Lifecycle/Tenure/Source Clarity, Postgame Source/Decision/Named-Game/Record-Memory CTAs, Offseason Command Snapshot, opt-in `goat-25y`/`goat-50y`, scrapbook authoring, market/action receipts on trade/FA/draft/deadline routes, G6/G5/G4/G3 gate proofs, cartridge/import/save-slot round trips, Week-Advance Browser Smoke Action, stadium content, Chip onboarding guards, source/receipt/app-shell/setup guardrails, cap rule-awareness, waiver/practice clarity, achievements, endorsements, rivalry sidecar, depth starters, relocation guard.

Rules: preserve deterministic sim/RNG, save compatibility, route integrity, browser sidecar boundaries, and package ownership. No broad rewrites, invented requirements, destructive commands, or save/schema/sim/math changes without the full safety path. If touching save schema, sim formulas, week advance, import/export, route shell, setup persistence, or browser sidecars, prove migration/default/test/rollback first.

Before editing, make a 4-7 item scoreboard covering impact, correctness value, save/sim risk, size, test path, docs, and rollback. Pick exactly one slice. State the contract: owner, read paths, commit path, state touched, tests, docs, rollback, risk class.

Preferred next slice: use existing saved data for Franchise Book/scrapbook/chronicle/legend/playoff-swing follow-through outside already covered Game Day/draft/profile/timeline/week surfaces; add position-coach poaching/history only with lifecycle tests; run full 36-step release gate only when release-level refresh is warranted; reduce roster-minimum windows only with balance proof.

After each slice: run narrow verification, update `CODEX_GAME_GUIDE.md`, `CODEX_IMPROVEMENT_PLAN.md`, `STATUS.md`, and prompts if stale. STATUS must list files, shipped change, checks run/not run, residual risk, rollback, and best next slice.

Final response sections exactly: Understanding, Plan, Patch, Verification, Risks / Rollback.
```

## Expanded Reference Prompt

The expanded prompt below is intentionally over the `/goal` field limit. Use it as reference material, not as the paste block.

```text
/goal You are continuing work on Mr. Football Dynasty in `/Users/tkevinbigham/MFD/MFD-main`.

Mission: run a serious implementation marathon that pushes this football dynasty sim toward a truly finished, GOAT-level playable game. This is not a planning pass. Your job is to read the current docs/source, choose the highest-leverage safe slices, ship verified improvements, update the living docs, record exact handoff notes, and keep going into the next independent slice while the run remains clean.

Tone and operating mode:
- Be a pragmatic senior engineer.
- Ship concrete code, tests, and docs; do not stop at analysis when a safe path exists.
- Keep changes small enough to verify and roll back.
- Favor playable improvements the user can feel: clearer decisions, fewer misleading states, better receipts, stronger long-save trust, richer player/team identity, and safer route/store behavior.
- Treat source and tests as truth. Treat docs as maps that must be updated when source says otherwise.
- Preserve deterministic simulation, save compatibility, route integrity, browser sidecar boundaries, and existing package ownership.
- Do not invent product requirements. When behavior is ambiguous, make the smallest safe assumption and state it.
- Do not use destructive git/filesystem commands. Confirm whether this extracted working copy even has `.git` before relying on git commands.
- If a slice touches save schema, week advance, sim formulas, RNG, imports/exports, or route shell, slow down and prove the full safety path before editing.

Current facts from the last handoff:
- The working copy is `/Users/tkevinbigham/MFD/MFD-main`.
- `SAVE_VERSION` was confirmed as `36` in `packages/engine/src/config/difficulty.ts`.
- Root `package.json` pins `pnpm@9.15.9`.
- The latest shipped vertical slice is Game Day Player Arc Follow-Up. `/game-day` reads saved `GameDayPackage.topPerformers`, `recordsMoments`, and `milestoneMoments`, de-duplicates player ids, links saved player rows to profiles, preserves record/milestone narratives and top-performer stat lines, and routes only to Player Development or Record Book. The prior Draft Recap Class Follow-Through, Week Advance Rookie Class Follow-Up, Draft Recap Player Profile Links, Player Timeline Draft Recap Memory, Player Profile Draft Recap Signature Memory, Draft Post-Draft Recap CTA, Draft War-Room Durable Accepted-Trade Receipt, Browser Accepted Offer Proof, Real-Backed Offer Generation, and Live Order Ownership slices remain shipped. The prior GOAT Release Sentinel Benchmark remains shipped: `node scripts/release-gate.mjs --only goat-release-sentinel` passed the new 36th release-gate step in 11.5s, completing 4/4 seasons, 113 weeks, 56 medium roster-minimum anomalies under budget, 0 high anomalies, and every named budget PASS. The latest full all-step local proof remains the prior 35/35 run in 2550.5s; full 36/36 proof has not been rerun.
- The prior Standalone CIN Stadium Tradition slice remains shipped. `packages/content/stadiums/cin-sty.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when CIN is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, audio behavior, or package ownership changes.
- The prior Standalone ATL Stadium Tradition slice remains shipped. `packages/content/stadiums/atl-orchard.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when ATL is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, audio behavior, or package ownership changes.
- The prior Post-Setup Route Smoke Matrix slice remains shipped. `scripts/smoke-test-post-setup-route.mjs` keeps the legacy single-route env override, adds `SMOKE_POST_SETUP_ROUTES_JSON` for a validated initialized route/text matrix after one convention-demo launch, and has parser coverage plus a live three-route smoke result. This is tooling/tests/docs and adds no app runtime behavior, `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, or package ownership changes.
- The prior Standalone CLE Stadium Tradition slice remains shipped. `packages/content/stadiums/cle-power-chord.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when CLE is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, audio behavior, or package ownership changes.
- The prior Standalone BOS Stadium Tradition slice remains shipped. `packages/content/stadiums/bos-kettle.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when BOS is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, audio behavior, or package ownership changes.
- The prior Standalone NYC Stadium Tradition slice remains shipped. `packages/content/stadiums/nyc-meter.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when NYC is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, audio behavior, or package ownership changes.
- The prior Standalone PIT Stadium Tradition slice remains shipped. `packages/content/stadiums/pit-furnace.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when PIT is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, audio behavior, or package ownership changes.
- The prior Standalone CHI Stadium Tradition slice remains shipped. `packages/content/stadiums/chi-deep-freeze.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when CHI is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, audio behavior, or package ownership changes.
- The prior Standalone DET Stadium Tradition slice remains shipped. `packages/content/stadiums/det-bass-drop.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when DET is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, audio behavior, or package ownership changes.
- The prior Standalone SEA Stadium Tradition slice remains shipped. `packages/content/stadiums/sea-feedback.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when SEA is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, or package ownership changes.
- The prior Standalone SF Stadium Tradition slice remains shipped. `packages/content/stadiums/sf-mother-dough.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when SF is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, or package ownership changes.
- The prior Standalone BAL Stadium Tradition slice remains shipped. `packages/content/stadiums/bal-crab-pot.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when BAL is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, or package ownership changes.
- The prior Standalone PHI Stadium Tradition slice remains shipped. `packages/content/stadiums/phi-liberty-bell.json` is schema-validated, statically imported by `content-loader.ts`, exposed through the existing stadium accessors, and consumed by broadcast pregame commentary when PHI is home. This is read-only content/tests/docs and adds no `GameState`, save schema, migration, route registration, sim, RNG, movement, sidecar, or package ownership changes.
- The prior Chip Onboarding Spotlight Anchor Guard slice remains shipped. `spotlightAnchors.test.ts` resolves first-ten onboarding routes through `resolveRouteKey` and requires each non-null `FIRST_TEN_MINUTE_ONBOARDING_BEATS[*].spotlightTarget` to appear exactly once on the owning route screen. This is test/docs only and adds no runtime behavior, route registration, save shape, GameState, sim, RNG, movement, sidecar, or package ownership changes.
- The prior Chip Onboarding Route Target Guard slice remains shipped. `App.test.tsx` source-guards `FIRST_TEN_MINUTE_ONBOARDING_BEATS[*].route` from `apps/web/src/features/companion/onboardingMachine.ts` against the registered `App.tsx` route tree. This adds no runtime behavior, route registration, save shape, GameState, sim, RNG, movement, sidecar, or package ownership changes.
- The prior Call Your Shot Result Source Boundary slice remains shipped. Game Day's result panel labels `resolveCallYourShot`, saved `GameResult.callYourShotResult`, recap `GameDayPackage.callYourShotResult`, and the read-only display boundary. This adds no call-your-shot math, week advance, saved shape, GameState, route registration, sim, RNG, movement, sidecar, audio, or package ownership changes.
- The prior Inbox Shell Badge Boundary Clarity slice remains shipped. `/inbox` labels that `computeNavBadges` does not badge `/inbox`; urgent and decision counts are route-local header/filter counts, not top-nav or mobile-tab badges. This adds no shell badge derivation, inbox generation, save schema, GameState, route registration, sim math, RNG state, movement, sidecar, or package ownership changes.
- The prior Trade Center Near-Miss Boundary Clarity slice remains shipped. `/trades` labels generated offer rejections as offer-status-only and direct proposal/counter rejections as possible `nearMissTracker.declinedTrades` inputs only when the user requested partner players. This adds no tracker logic, save schema, GameState, route registration, sim math, RNG state, movement, sidecar, or package ownership changes.
- The prior Trick-Play Planning Boundary Clarity slice remains shipped. `/game-plan` labels selected trick-play ids as planned weekly-prep data in `Weekly Prep Sources` and trick-play tab copy while `trick-plays.test.ts` keeps `executeTrickPlay`, `shouldCallTrickPlay`, and related helpers out of the current simulation spine. This adds no save schema, GameState, route registration, sim math, RNG state, movement, sidecar, or package ownership changes.
- The prior Revenge Newsline Recap Consumption slice remains shipped. `buildBroadcastCommentary` consumes loader-owned `postgame.newsline` as a recap `Newswire headline:` only when the revenge subject's current team beats the former team. This adds no saved news rows, broadcast persistence, save schema, GameState, route registration, sim math, RNG state, movement, sidecar, or package ownership changes.
- The prior Revenge Lines Loader Wiring slice remains shipped. `packages/content/narrative/revenge-lines.json` is schema-validated by `RevengeLinesContentSchema`, loaded through `content-loader.ts`, exported by `getRevengeLineTemplates`, and consumed by `buildBroadcastCommentary` for revenge pregame/in-game/recap flavor through deterministic selection and interpolation. This adds no save schema, GameState, route registration, sim math, RNG state, movement, sidecar, or package ownership changes.
- The prior League Weather Source Labels slice remains shipped. `/league/weather` card rows distinguish saved `schedule.weather`, stadium-dome fallback, and UI-only forecast-profile sources, with focused `WeatherForecast.test.tsx` coverage for the exported schedule-to-forecast helper. This adds no weather generation, saved weather key, forecast formula, save schema, route registration, sim math, RNG, movement, sidecar, or package ownership changes.
- The prior Roster Selected-Player Action Receipts slice remains shipped. `/roster` renders route-local `Roster Action Receipt` rows after existing selected-player cut, trade-block toggle, restructure, place-on-IR, or activate-from-IR commits resolve, without changing store actions, movement rules, cap/contract formulas, saved receipt history, save schema, sim math, RNG, route registration, sidecar, or package ownership.
- The prior Rivalry Sidecar Lifecycle And Archive slice remains shipped. Browser-local `mfd.rivalries.v1` syncs from pure `deriveRivalries(game)` on new game, loaded game, latest autosave, and guarded year rollover; `/dynasty` complete sidecar archives include it with validation and old-archive fallback to an empty derived payload. This stays outside `GameState` and `.mfd` cartridges.
- The prior Progressive Nav Unlock Metadata Reconciliation slice remains shipped. `NAV_UNLOCK_RULES` covers every current primary `NAV_ITEMS` path/label, exports through `@mfd/engine/config`, and `nav-items.test.ts` guards both App-table parity and current non-consumption in `App.tsx`. Existing Week 4, midseason, and phase gates are preserved; `/briefing` plus direct-only routes remain outside the table.
- The prior Depth Chart Starter Target Semantics slice remains shipped. `apps/web/src/lib/depth-chart-starters.ts` owns the shared 22-flag urgency target used by shell nav badges and `/depth-chart`, and the route labels that target as a shared threshold rather than exact formation validation. This adds no starter mutation, lineup enforcement, role update, save schema, route registration, sim math, RNG, movement, or sidecar behavior changes.
- The prior Relocation Store Eligibility Guard remains shipped. `actions.relocateTeam(destinationId)` re-checks `canRelocate` and selected-destination affordability before invoking the engine relocation helper, and `/relocate` disables final confirmation with selected-cost copy when the chosen package is unaffordable. `game-store.franchise.test.ts`, `RelocationScreen.test.tsx`, web typecheck, production build, and post-setup `/relocate` smoke lock the boundary. This adds no relocation formula, destination table, save schema, route registration, sim math, RNG, or movement behavior changes.
- The prior Rivalry Sidecar Package Boundary Cleanup remains shipped. `mfd.rivalries.v1` storage moved from engine-local `packages/engine/src/rivalries/storage.ts` to `apps/web/src/lib/rivalry-storage.ts`; pure `deriveRivalries`/types now export through `@mfd/engine`; and `architecture-boundaries.test.ts` keeps production engine modules browser-API-free while allowlisting only the deliberate web storage, rollover, store sync, and complete-sidecar archive callers.
- The prior Audio Settings Source Boundary slice remains shipped. `AudioSettings` renders `Audio Sources`, labeling `mfd-ui-preferences` browser-local preference ownership, `AudioController` playback gating, route-local preview buttons, and the `RootLayout`/`postGameUi.audioCueQueue` saved game-cue handoff. `AudioSettings.test.tsx` locks the source copy. This adds no audio handler behavior, preference shape, audio queue mutation, sidecar schema, `GameState`, save schema, route behavior, sim math, RNG, movement, asset formula, or package ownership changes.
- The prior Apology-Tour Modal Source Boundary slice remains shipped. `ApologyTourModal` now renders `Tour Sources`, labeling the saved `apologyTourThreads` row, authored `getApologyTourBeat` content key, modal-local `beatIndex` replay state, and delivered-beat ledger. `ApologyTourModal.test.tsx` locks the source copy. This adds no apology-tour engine behavior, delivered-beat write, read receipt, sidecar, `GameState`, save schema, route behavior, sim math, RNG, movement, generated-message formula, or package ownership changes.
- The prior Dynasty Era Prompt Source Boundary slice remains shipped. `DynastyEraPrompt` renders `Era Naming Sources`, labeling the app prompt gate, seeded `generateEraSuggestions(game, mulberry32(game.seed ^ game.year ^ 0xEEEE))` suggestions, `actions.nameDynastyEra(finalName)` as the explicit saved `userDynastyEras`/`team.era` writer, and `Skip` as close-only. `DynastyEraPrompt.test.tsx` locks the source copy. This adds no era detection, era start/end write, franchise-history update, title award, save schema, route registration, `SAVE_VERSION`, sim math, unseeded RNG, movement, sidecar, or package ownership changes.
- The prior Player Profile Farewell Tour Receipt slice remains shipped. Player Profile labels `selectFarewellCandidates`/`selectFarewellTours` in `Profile Sources`, disables `Start Farewell Tour` while the existing store action resolves, and renders a route-local `Farewell Tour Receipt` after `actions.startFarewellTour -> startFarewellTourEngine -> commitGame` completes. `PlayerProfile.test.tsx` locks the source/receipt copy. This adds no store action, engine formula, saved receipt history, save schema, schedule generation, route registration, `SAVE_VERSION`, sim math, RNG, movement, sidecar, or package ownership changes.
- The prior Analytics Win Rate Label slice remains shipped. `/analytics` now labels the Team Overview `successRate` row as `Win Rate` because `calculateAdvancedStats` divides team wins by games played. `AnalyticsDashboard.test.tsx` locks the rendered copy. This is render-only analytics UI clarity: no analytics formula, selector, stat leader, trend helper, save schema, route registration, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Analytics Turnovers Per Game Label slice remains shipped. `/analytics` labels the Team Overview `turnoverRate` row as `Turnovers / Game` and formats it as a per-game value because `calculateAdvancedStats` divides turnovers lost by games played. `AnalyticsDashboard.test.tsx` locks the rendered copy. This is render-only analytics UI clarity: no analytics formula, selector, stat leader, trend helper, save schema, route registration, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Franchise Book Win Percentage Label slice remains shipped. `/franchise/book` labels the chapter-margin `WIN %` value on an explicit percentage scale, so saved `chapter.record.winPct = 0.682` renders as `68.2%` instead of a bare number. `FranchiseBook.test.tsx` locks the rendered copy. This is render-only Franchise Book UI clarity: no builder logic, chapter derivation, save schema, route registration, `SAVE_VERSION`, sim math, RNG, movement, sidecar, or package ownership changed.
- The prior Trade Finder Acceptance Label slice remains shipped. `/trades` Trade Finder labels advisory `acceptanceLikelihood` on the same percent scale as its progress bar, so a `0.9` engine/read-model value renders as `90%`. `TradeCenter.test.tsx` locks the rendered copy. This is render-only trade UI clarity: no trade finder formula, valuation, proposal loading, movement, save schema, route registration, `SAVE_VERSION`, sim math, RNG, or package ownership changed.
- The prior Trade Finder Generated-Offer Receipt slice remains shipped. `/trades` renders per-suggestion `Generated Offer Receipt` copy from saved `TradeSuggestion` partner/package/need/acceptance/value-gap fields and labels the source-panel boundary before the user loads a package. This is render-only Trade Center clarity: no `findTradeTargets`, `selectTradeSuggestions`, proposal creation, offer acceptance, trade valuation, movement, save schema, route registration, `SAVE_VERSION`, sim math, RNG, or package ownership changed.
- The prior FA Target Board Market Receipt slice remains shipped. `/fa-targets` renders per-target `FA Market Receipt` copy from saved `FATarget` fit/sign-probability/projected-salary/market-demand/competing-team fields and labels the source-panel boundary before the user watches, compares, bids, or signs. This is render-only market-planning clarity: no `buildFATargetBoard`, selector refresh, watchlist persistence shape, bid creation, signing, cap math, roster movement, save schema, route registration, `SAVE_VERSION`, sim math, RNG, or package ownership changed.
- The prior Trade Deadline Deal Receipt slice remains shipped. `/trade-deadline` renders per-completed-deal `Deadline Deal Receipt` copy from saved `DeadlineDeal` team/player/pick/grade/timestamp/splash fields and labels the Finalize boundary before any completed-deal movement occurs. This is render-only deadline clarity: no `initializeDeadline`, `advanceDeadlineClock`, `finalizeDeadline`, pending-offer action, offer generation, trade valuation, cap math, roster/pick movement, save schema, route registration, `SAVE_VERSION`, sim math, RNG, or package ownership changed.
- The prior Repeat-Player Fast Lane Launch Gate slice remains shipped. `NewGameScreen` Launch Sources labels the setup-run mode writer and engine fast-lane setup factory path; Full Setup remains the default New Dynasty path; Fast Lane unlocks only from browser-local `mfd:first-ten-completed`; selected starts persist `mfd:setup-run-mode`; and Fast Lane replaces only the initial setup state through `createFastLaneSetupState`. Scenario Challenge, Convention Demo, Continue, and Import remain unchanged. This changes no save schema, route registration, `SAVE_VERSION`, sim math, RNG, movement, or package ownership.
- The prior In-Save Scenario Setup Bypass slice remains shipped. `actions.startScenarioChallenge` deletes `setupState` after `startScenarioEngine`, matching NewGame scenario starts so `/scenarios` does not route the fresh scenario through first-run setup. This changes no scenario definition, constraint, grading, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership.
- The prior Mentors Spotlight Anchors slice remains shipped. `/mentors` Chip beats target the active/budget/effects summary and `Network Source`; copy frames alumni mentor state, compatible effects, Team Ops receipt, and Hire/Release ownership over existing handlers only. `AlumniMentorsScreen.test.tsx` and route-coaching spotlight tests lock the anchors. This is render-only route-guidance work: no mentor hire/release behavior, budget change, progression, CPU-team mentor behavior, normal mentoring-pair behavior, training-camp receipt, save schema, route registration, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Settings Spotlight Anchors slice remains shipped. `/settings` Chip beats target `Simulation Preferences` and `Operations Source`; copy frames comfort/gameplay controls and facility/medical source boundaries as route guidance over existing handlers only. `Settings.test.tsx` and route-coaching spotlight tests lock the anchors. This is render-only route-guidance work: no settings toggle behavior, facility upgrade, medical hire, candidate refresh, injury recovery, training-camp resolution, save schema, route registration, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Dynasty Save/Load Spotlight Anchors slice remains shipped. `/dynasty` Chip beats target `Portable Backup` and the Local Save Slots plus Import Cartridge restore grid; copy frames backup/restore as route guidance over existing handlers only. `DynastyCartridge.test.tsx` and route-coaching spotlight tests lock the anchors. This is render-only route-guidance work: no cartridge build/export, local slot save/load/delete, import validation, sidecar import, `GameState`, save schema, sidecar shape, route registration, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Training Camp Route Coaching Alignment slice remains shipped. `/training-camp` Chip beats target `Camp Readiness` and `Ops Carryover`; copy frames the route as a read-only camp report owned by `advanceFranchiseWeek` plus `buildTeamOpsImpactReceipt` instead of route-local drills. `TrainingCamp.test.tsx` and route-coaching spotlight tests lock the anchors. This is render-only route-guidance work: no camp resolution, progression math, injury generation, route registration, save schema, sidecar, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Awards Hub Spotlight Anchors slice remains shipped. `/awards` gives both `awards-hub` Chip beats unique `data-spotlight-target` anchors: the shared Awards Hub header/current-class context and the season scrubber in archived or empty states. `AwardsHub.test.tsx` locks rendered anchors, and route-coaching spotlight tests lock one source anchor per beat. This is render-only route-guidance work: no route registration, award generation, ceremony write, season-history patch, player mutation, record/HOF/archive update, media publication, achievement unlock, save schema, sidecar, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Launch Action Path Guard slice remains shipped. `NewGameScreen` now routes New Dynasty, Scenario Challenge, and Convention Demo through exported, focused-tested launch-state builders. Tests lock standard dynasty use of `createSeedGameState`, scenario use of `startScenario` plus setup bypass, and convention-demo use of `generateConventionSave('afce1', mulberry32(seed))`. This is behavior-preserving launch coverage: no launch behavior, save schema, setup persistence, sidecar clearing, `GameState`, autosave, save schema, `SAVE_VERSION`, sim math, RNG, route registration, movement, or package ownership changed.
- The prior Launch Command Source Boundary slice remains shipped. `NewGameScreen` renders `Launch Sources`, labeling `createSeedGameState`, `actions.newGame`, validated `loadGame`, scenario/convention/demo ownership, and the no-render-write boundary. This is read-only launch UI source work: no launch action, dynasty creation, setup start, import validation, sidecar clearing, `GameState`, autosave, save schema, `SAVE_VERSION`, sim math, RNG, route registration, movement, or package ownership changed.
- The prior Attract Mode Source Boundary slice remains shipped. `AttractModeFrame` labels `NewGameScreen` inputs, `mulberry32` seeded demo-frame selection, render-only reel ownership, and the timer/input no-write boundary. This is read-only UI source work: no launch flow, dynasty creation, setup start, `GameState`, autosave, save schema, `SAVE_VERSION`, sim math, RNG, route registration, sidecar, movement, or package ownership changed.
- The prior Ghost Broadcast Panel Source Boundary slice remains shipped. `GhostBroadcastsPanel` labels `broadcast.ghostLines`, `mfd.broadcast.ghost.v1` browser prefs, HOF/callout plus legacy unsourced-line classification, and the no-generate/no-broadcast-write/no-GameState/no-sim/no-RNG boundary. This is read-only UI source work: no ghost-line generation, pref shape, broadcast payload, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Breaking News Ticker Source Boundary slice remains shipped. App-shell `BreakingNewsTicker` labels `leagueNews via selectTickerItems`, read-only ticker behavior, no queue-dismiss/story-generation/save/RNG writes, and `breakingNewsQueue` separation in title text. This is read-only UI source work: no ticker gating, queue dismissal, story generation, route registration, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Power Rankings Ticker Source Boundary slice remains shipped. Embedded `PowerRankingsTicker` labels `selectPowerRankings`, `selectUserTeamId`, clickable `/power-rankings` navigation versus static embeds, and the no-recalculate/no-media-write/no-save/no-RNG boundary. This is read-only UI source work: no ranking formula, media cycle, route registration, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Game Day Weather Impact Receipt Source slice remains shipped. `/game-day` `Weather Impact` labels saved `GameDayPackage.weather`, shared `getWeatherImpactCopy`, and the no-generation/no-rerun/no-RNG boundary for saved rain/snow/wind packages. This is read-only UI receipt work: no weather formula, weather generation, schedule weather, saved result, route registration, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Production Hash-Helper Navigate Target Guard remains shipped. `App.test.tsx` source-scans production hash-helper `navigateTo('/...')` / `goTo('/...')` call sites and fails if a local hash-navigation wrapper target is missing from the registered route tree. The store `navigateTo`, production `navigateTo`, and TanStack router `navigate({ to })` route-tree guards remain. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Production Router Navigate Target Guard remains shipped. `App.test.tsx` source-scans production `apps/web/src` `.ts`/`.tsx` files for literal TanStack router `navigate({ to: '/...' })` and `router.navigate({ to: '/...' })` calls and fails if any component/app-shell target is missing from the registered route tree. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Production Component Navigate Target Guard remains shipped. `App.test.tsx` source-scans production `apps/web/src` `.ts`/`.tsx` files for literal `navigateTo('/...')` calls and fails if any component/helper target is missing from the registered route tree. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Newsroom Headline Story Receipt slice remains shipped. `/newsroom` `HeadlineModal` renders read-only `Story Receipt` rows for the selected saved `game.leagueNews` item, `NewsItemSchema` id, story year/week, linked team/player ids, engine/news writer ownership, and the no-write modal boundary without generating stories, appending `leagueNews`, dismissing breaking-news interrupts, advancing the week, autosaving, mutating roster/player state, changing save shape, changing `SAVE_VERSION`, touching route registration, sidecars, sim math, RNG, report shape, or package ownership.
- The prior Free Agency Action Receipts slice remains shipped. `/free-agency` renders route-local receipts after existing re-sign offer, open-market bid, or street-sign actions resolve through their current store/engine commit paths. Receipts label player/offer/round or phase context, forecast status, touched re-sign/bid/roster/cap/audio/autosave state, commit path, and no-extra-write boundaries without adding durable receipt history, save shape, `SAVE_VERSION`, bid or negotiation formula changes, cap formula changes, phase advancement, route registration, sidecars, sim math, RNG, report shape, or package ownership changed.
- The prior Free Agency Bid Resolution Summary slice remains shipped. `/free-agency` renders a read-only `Bid Resolution Summary` from saved `offseasonState.freeAgencyBids` rows after `resolveFreeAgencyRound` marks bids `won` or `lost`. It labels user wins, user losses, CPU-only outcomes, saved winner scores, bid counts, and any user lost-bid score without re-scoring bids, resolving the round, submitting offers, moving players, changing cap totals, autosaving, rerolling outcomes, adding durable receipt history, changing save shape, or touching `SAVE_VERSION`.
- The prior CPU Strategy History Display slice remains shipped. `/team-needs` renders read-only `Strategy History` rows from saved `game.eventLog` `gm_strategy_shift` rows plus saved `game.leagueNews` team-philosophy signal rows for the selected CPU comparison team, without reevaluating strategy, applying philosophy, writing news, refreshing `teamNeedsCache`, generating offers, moving assets, autosaving, rerolling outcomes, changing save shape, or touching `SAVE_VERSION`.
- The prior Postgame Source/Decision Receipt slices remain shipped. `/game-day` renders read-only source rows from the saved `GameDayPackage` returned by `selectLatestGameDayPackage`, and decision rows from saved `prepGrade`, `coachingNotes`, `carryForwardRecommendations`, `injuryNotes`, `activeEffectSummaries`, and `autopsy.nextFocus`. Rendering does not replay the game, build packages, recalculate Film Room, apply training, adjust fatigue, change injuries/morale, autosave, answer press, or touch RNG.
- The Week Advance Rookie Class Follow-Up slice remains shipped. `/week-advance` renders saved draft-recap rookies in the opening window with profile/development/training/draft-recap CTAs and no training/depth/week mutation. The prior Offseason Command Snapshot slice remains shipped: `/week-advance` renders read-only command rows from `selectOffseasonCalendar`, saved `offseasonState`, user-team roster contracts, and team cap fields without advancing offseason, submitting bids, resolving rounds, re-signing players, moving assets, mutating cap totals, autosaving, or touching `SAVE_VERSION`.
- The prior Trade Deadline Pending Action Receipts slice remains shipped. `/trade-deadline` renders route-local receipts after existing Accept, Reject, or Advance Clock actions resolve through their current store/engine commit paths. Receipts label active offer/clock context, touched deadline/trade/autosave state, commit path, and no-extra-write boundaries without adding durable receipt history, save shape, `SAVE_VERSION`, offer generation changes, trade valuation changes, cap formula changes, completed-deal finalization changes, route registration, sidecars, sim math, RNG, report shape, or package ownership changed.
- The prior Settings Operations Action Receipts slice remains shipped. `/settings` renders route-local receipts after enabled facility Upgrade or Hire Staff buttons resolve through existing `actions.upgradeFacility -> upgradeFacilityEngine -> commitGame` and `actions.hireMedicalStaff -> hireMedicalStaffEngine -> commitGame` paths. Receipts label active forecast context, touched facility/medical/autosave state, commit path, and no-extra-write boundaries without adding durable receipt history, save shape, `SAVE_VERSION`, facility formula changes, medical pool refresh, injury recovery, progression, training-camp resolution, route registration, sidecars, sim math, RNG, report shape, or package ownership changed.
- The prior Practice Squad Action Receipts slice remains shipped. `/practice-squad` renders route-local receipts after Add, Elevate, or Release resolves through existing practice-squad store/engine actions. Receipts label slot/elevation context, touched practice-squad/free-agent/roster/player/transaction/autosave state, commit paths, and no-extra-write boundaries without adding durable receipt history, save shape, `SAVE_VERSION`, practice-squad limit changes, movement behavior changes, contracts, route registration, sidecars, sim math, RNG, report shape, or package ownership changed.
- The prior Draft Pick Submission Receipts slice remains shipped. `/draft` renders a route-local `Draft Pick Receipt` after the Draft Player button resolves through existing `actions.makeDraftPick -> makeDraftPickEngine -> applyDraftSelection -> commitGame`. The receipt labels selected prospect/pick context, pre-pick forecast, touched draft/roster/player/news/archive/press-conference/audio/autosave state, commit path, and no-extra-write boundary without adding durable receipt history, save shape, `SAVE_VERSION`, draft order changes, prospect generation, rookie-creation changes, movement formulas, route registration, sidecars, sim math, RNG, report shape, or package ownership changed.
- The prior Waiver Claim Submission Receipts slice remains shipped. `/waivers` renders a route-local `Waiver Claim Receipt` after the Submit Claim button resolves through existing `actions.submitWaiverClaim -> submitWaiverClaimEngine -> commitGame`. The receipt labels claim intent, priority/board contract context, delayed waiver-run resolution, state touched, commit path, and no-extra-write boundary without adding durable receipt history, save shape, `SAVE_VERSION`, waiver processing, priority changes, roster movement, contract/cap formulas, route registration, sidecars, sim math, RNG, report shape, or package ownership changed.
- The prior Cap Lab Execution Receipts slice remains shipped. `/cap-lab` renders a route-local `Cap Lab Execution Receipt` after the Confirm button resolves through existing `actions.executeCapMoves(sandbox)`. The receipt labels queued moves, preview cap-space result, warnings, commit path, state touched, and no-extra-write boundary without adding durable receipt history, a second cap writer, save shape, `SAVE_VERSION`, cap/contract formulas, route registration, sidecars, sim math, RNG, movement, report shape, or package ownership changed.
- The prior Front Office Contract Action Receipts slice remains shipped. `/front-office` renders a route-local `Contract Action Receipt` after Restructure, Backload, Standard Cut, or Post-June-1 Cut resolves through the existing contract store actions. The receipt labels the player, preview cap/dead-money context, commit path, state touched, and no-extra-write boundary without adding durable receipt history, a second contract writer, save shape, `SAVE_VERSION`, cap/contract formulas, route registration, sidecars, sim math, RNG, movement, report shape, or package ownership changed.
- The prior Endorsement Action Receipts slice remains shipped. `/endorsements` renders a route-local `Endorsement Receipt` after `Accept` or `Decline` resolves through the existing endorsement action path. The receipt labels the offer, player, requirement snapshot, accept/decline commit path, saved pending-offer boundary, and no-extra-write copy without adding durable receipt history, a second endorsement writer, save shape, `SAVE_VERSION`, endorsement formulas, revenue/morale math, route registration, sidecars, sim math, RNG, movement, report shape, or package ownership changed.
- The prior Handshake Promise Receipt remains shipped. `/handshakes` renders a route-local `Promise Receipt` after `Promise Starter`, `Promise No Trade`, or `Promise Restructure` resolves through the existing `actions.makePromise -> makePlayerPromise -> commitGame` path, labeling saved `game.handshakes`, evaluator ownership, and no-extra-evaluation boundaries without changing promise effects or saves.
- The prior Store Navigation Target Route Guard remains shipped. `App.test.tsx` scans `game-store.ts` hard-coded `navigateTo('...')` targets and fails if any are not in the registered `rootRoute.addChildren([...])` tree. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim math, RNG, movement, or package ownership changed.
- The prior Direct Saved-Game Mutation Source Guard remains shipped. `game-store.source.test.ts` fails if a `set((s) => ...)` block touches `s.game` outside `commitGame`, new-game/load/autosave hydration, or undo restore. No runtime behavior, save schema, `SAVE_VERSION`, sim math, RNG, movement, route registration, or package ownership changed.
- The prior Contract/Trade-Block Commit Path slice remains shipped. Direct `restructure`, `backload`, and `toggleTradeBlock` actions clone current state and commit through `commitGame(nextGame)`. Contract actions keep existing helper formulas, sync player-map contracts, refresh cap totals/league cap space, and autosave; trade-block toggles mirror into `GameState.players` when present. No save schema, `SAVE_VERSION`, contract formula, sim math, RNG, movement, route registration, or package ownership changed.
- The prior Setup Progress Commit Path slice remains shipped. `advanceSetup`, `goBackSetup`, `applySetupChoice`, and `toggleSetupDrilldown` now clone the current game and commit through `commitGame(nextGame)` so setup wizard progress autosaves through the standard durable store path. No save schema, `SAVE_VERSION`, setup helper formula, sim math, RNG, movement, route registration, or package ownership changed.
- The prior Set Phase Commit Path slice remains shipped. `setPhase` now clones the current game, writes only `game.phase`, and commits through `commitGame(nextGame)` so saved phase changes autosave through the standard durable store path. No save schema, `SAVE_VERSION`, phase-transition formula, sim math, RNG, movement, route registration, or package ownership changed.
- The prior Owner Refresh Commit Path slice remains shipped. `refreshOwner` now clones the current game, delegates approval plus the single owner-history row to `updateOwnerApproval`, and commits through `commitGame(nextGame)` so owner approval refreshes autosave through the standard durable store path. No save schema, `SAVE_VERSION`, owner formula, sim math, RNG, movement, route registration, or package ownership changed.
- The prior Playtest Phase-Boundary Schedule Awareness slice remains shipped. `detectPhaseBoundaries` now reads `getRegularSeasonWeekCount(context.state)` so generated/configured 17/18/19-week playtest frames are judged against their own regular-season boundary. No harness fixture, report shape, save schema, `SAVE_VERSION`, sim math, RNG, movement, route registration, or package ownership changed.
- The prior Autosave Toast Week-Baseline Guard slice remains shipped. `RootLayout` now updates the remembered week through `resolveAutosaveToastStep` before showing the cosmetic autosave toast, so a rerender on the same week cannot retrigger the save cue. No save schema, `SAVE_VERSION`, route registration, nav metadata, sidecar, sim math, RNG, movement, persistence writer, or package ownership changed.
- The prior Current-Save Rivalry Latest-Meeting Clarity slice remains shipped. `/franchise` `RivalryHeatMap` now shows total games and latest saved meeting result/score/year/week from the same completed schedule/playoff results that drive W-L-T. No durable all-time field, persistence, sidecar, save schema, `SAVE_VERSION`, sim math, RNG, movement, route registration, or package ownership changed.
- The prior Farewell-Tour Store Schedule Guard slice remains shipped. `game-store.test.ts` verifies that `actions.startFarewellTour` saves a tour with three or more scheduled future moments and a `final_game` moment from a live seed schedule, not just an empty top-level tour row. No store action, engine formula, schedule generation, save schema, `SAVE_VERSION`, sim math, RNG, movement, sidecar, or package ownership changed.
- The prior Franchise Legends Farewell-Tour Visibility slice remains shipped. `/legends` renders saved `game.farewellTours` for the current user team in an `Active Farewell Tours` panel, adds that saved source to `Franchise Legend Sources`, and labels Player Profile's `startFarewellTour` action as the tour-start owner. No farewell-tour writer, retired-jersey writer, Hall of Fame logic, route registration, save schema, `SAVE_VERSION`, sim math, RNG, movement, sidecar, or package ownership changed.
- The prior Relocation Route Coaching slice remains shipped. `/relocate` resolves to an unanchored `relocation` Chip route key that points to eligibility, destination cost, market, fanbase, prestige, impact preview reads, and the confirmed `Move Franchise` commit path through `relocateTeam`. No route registration, relocation eligibility, relocation cost, engine helper behavior, roster/player-map sync, save schema, `SAVE_VERSION`, sim formula, RNG, sidecar, route ownership, or package ownership changed.
- The Draft Recap Class Follow-Through slice remains shipped. `/draft-recap` renders current-player follow-through for selected saved recap picks from `game.players`/`selectTeams`, with profile links and no recap/history/roster mutation. The prior Draft Recap Player Profile Links slice remains shipped: `/draft-recap` renders saved recap picks through `PixelPlayerLink` callbacks in best value, biggest reach, steals, league highlights, and Pick By Pick rows. The prior Draft Recap Route Coaching slice remains shipped: `/draft-recap` resolves to an unanchored `draft-recap` Chip route key that points to saved user-team recap rows, grade/value/reach/steal/highlight reads, route-local year selection, `finalizePostDraft` writer ownership, and the no render-time generation/repair boundary.
- The prior Player Development Route Coaching slice remains shipped. `/player-development` resolves to an unanchored `player-development` Chip route key that points to selected roster-player reports, projection curves, breakout candidates, coach-context narrative, and the no progression/training-camp/archetype/coach-effect/player-history write boundary.
- The prior League Weather Route Coaching slice remains shipped. `/league/weather` resolves to an unanchored `league-weather` Chip route key that points to saved schedule weather, route-local forecast profiles, and Week Advance/Game Day ownership of generated weather, saved results, and receipts.
- The prior Player Timeline Route Coaching slice remains shipped. Dynamic `/player/$playerId/timeline` routes resolve to an unanchored `player-timeline` Chip route key that points to archived seasons, current-season rows, transaction memory, awards/highlights, Back To Profile and Stat Central navigation, and the display-only no season/record/transaction/archive write boundary.
- The prior Player Profile Route Coaching slice remains shipped. Dynamic `/player/$playerId` profile routes resolve to an unanchored `player-profile` Chip route key that points to Profile Sources, Career Memory, Signature Moments, Transaction Memory, owner-workflow action buttons, and the display-only no history/archive/award/record/contract/rivalry write boundary.
- The prior Player Rivalries Route Coaching slice remains shipped. `/rivalries` resolves to an unanchored `player-rivalries` Chip route key that points to saved feud intensity/tier/history/team context, saved social-post trash-talk projection, and Week Advance ownership of feud creation, decay, and sim-bonus context.
- The prior Player Comparison Route Coaching slice remains shipped. `/compare` resolves to an unanchored `player-comparison` Chip route key that points to current contracted player reads, Player A's stat lens, active-cap contract grade rows, cap-hit rows, and the no-profile/no-record write boundary.
- The prior Franchise Legends Route Coaching slice remains shipped. `/legends` resolves to an unanchored `franchise-legends` Chip route key that points to saved franchise history, rings, rafters, Hall rows, coaches, eras, and the local Hall detail modal boundary.
- The prior Awards Route Coaching slice remains shipped. `/awards` resolves to `awards-hub` Chip route beats that point to saved award classes, winners/runners-up comparison, and the route-local season scrubber; the latest slice now anchors both beats to unique Awards Hub DOM targets.
- The prior Commissioner Route Coaching slice remains shipped. `/commissioner` resolves to an unanchored `commissioner-governance` Chip route key that points to active proposals, CBA status, rule context, and the Petition/Vote commit boundary.
- The prior Endorsements Route Coaching slice remains shipped. `/endorsements` resolves to an unanchored `endorsements` Chip route key that points to active deals, requirements, pending offseason offers, and the Accept/Decline commit boundary.
- The prior Mentors Route Coaching slice remains shipped and is now anchored by Mentors Spotlight Anchors. `/mentors` resolves to `mentors` Chip beats that point to active slots, budget, compatible effects, saved mentor state, and the Hire/Release commit boundary.
- The prior Front Office Route Coaching slice remains shipped. `/front-office` resolves to an unanchored `front-office` Chip route key that points to contract selection, pure previews/projections, forecast warnings, and the Apply/Cut commit boundary.
- The prior Locker Room Route Coaching slice remains shipped. `/locker-room` resolves to an unanchored `locker-room` Chip route key that points to culture, tension, captain, and cooldown context before spending meeting or rally actions.
- The prior Scenario Constraint Route Coaching slice remains shipped. `/scenarios` resolves to an unanchored `scenario-constraints` Chip route key that points to active locks, enforcement badges, safe planning lanes, and the challenge-start confirmation boundary.
- The prior Analytics/Stat Central Route Coaching slice remains shipped. `/analytics` and `/stat-central` resolve to an unanchored `analytics-evidence` Chip route key that frames both routes as evidence boards over live stats, histories, and derived helpers.
- The prior Super Bowl Route Coaching slice remains shipped. `/super-bowl` resolves to an unanchored `super-bowl` Chip route key that frames the route as presentation over saved playoff results after the bracket resolves.
- The prior Owner/Handshakes Route Coaching slice remains shipped. `/owner` and `/handshakes` resolve to an unanchored `owner-promises` Chip route key that frames owner pressure and promises as shared trust-consequence surfaces.
- The prior Film Room Route Coaching slice remains shipped. `/film-room` resolves to an unanchored `film-room` Chip route key that frames Film Room as saved prep-report review after a result.
- The prior Broadcast Suite Route Coaching slice remains shipped. `/broadcast`, `/presentation`, `/play-by-play`, and `/game-flow` resolve to an unanchored `broadcast-suite` Chip route key that frames those routes as replay/read-model views over saved or deterministically rebuilt game output.
- The prior Schedule Route Coaching slice remains shipped. `/schedule` resolves to an unanchored `schedule` Chip route key that reinforces display-only saved week, bye, broadcast, flex, and result reads.
- The prior Game Day Route Coaching slice remains shipped. `/game-day` resolves to an unanchored `game-day-recap` Chip route key that frames the screen as receipt review over saved result, injury, weather, podium, and reaction outputs before next-week fixes.
- The prior Watch List Route Coaching slice remains shipped. `/watch-list` resolves to an unanchored `watch-list` Chip route key that labels global pins as browser-local and points real player actions back to owner routes.
- The prior Trade Block Route Coaching slice remains shipped. `/trade-block` resolves to an unanchored `trade-market-radar` Chip route key that frames the ticker as advisory radar and points commits back to `/trades`.
- The prior Roster Churn Route Coaching slice remains shipped. `/waivers` and `/practice-squad` resolve to an unanchored `roster-churn` Chip route key, and Waiver Wire / Practice Squad feature visibility is `route_beat`.
- The prior Market Planning Route Coaching slice remains shipped. `/team-needs`, `/free-agency`, and `/fa-targets` resolve to an unanchored `market-planning` Chip route key, and Team Needs / Free Agency feature visibility is `route_beat`.
- The prior Player Development Player Picker slice remains shipped. `/player-development` lets the user choose any current roster player through route-local state and feeds that selected player into the existing generated report, three-year `projectDevelopmentCurve`, breakout candidates, and report-owned coach-impact narrative.
- The prior Comeback Kings Metric Unlockability slice remains shipped. `hidden:comeback_kings` counts existing saved comeback named-game dynasty events won by the user team after the schedule-aware playoff boundary, bucketed by event year for one postseason run.
- The prior Settings/Dynasty Route-Coaching Split slice remains shipped. `/dynasty` resolves to `dynasty-save-load` with deliberate Dynasty Save/Load spotlight anchors.
  `/settings` resolves to `settings` with deliberate Simulation Preferences and Operations Source spotlight anchors.
- The prior Waiver Claim Results Scope Clarity slice remains shipped. `selectClaimResults` still scopes claim wins/losses to the user team, but cleared waiver rows now carry a `League-wide clearance` qualifier and `/waivers` renders it in Claim Results.
- The prior Elevated Practice-Squad Return Refresh slice remains shipped. `returnElevatedPlayers` refreshes roster/locker-room state after returning at least one elevated practice-squad player, clearing stale captain/tension rows tied to that player.
- The prior Waiver Awarded-Contract Repair slice remains shipped. `awardClaim` gives contractless claimed players a one-year minimum deal from `getMinSalary(player.yearsExp)`, updates existing claimed contracts to the winning team id, removes any free-agent entry, and syncs the claimant cap totals after roster movement. Waiver board copy explains no-contract rows become minimum deals if awarded.
- The prior Deadline Completed-Deal Archive Sync slice remains shipped. Finalized completed deadline player moves in `trade-deadline.ts` call `syncPlayerArchiveEntry(game, player, game.year)`, matching generated-offer and direct-proposal trade movement so `playerArchive.teamHistory` records the buyer stint.
- The prior Trade Movement Cap Synchronization Guard slice remains shipped. Accepted generated offers in `trade-market.ts`, accepted direct proposals/counters in `trade-negotiation.ts`, and finalized completed deadline deals in `trade-deadline.ts` call shared `syncTeamCapTotals(game, team)` for both involved teams after movement, recomputing `capUsed` from post-trade roster contract hits plus `deadCap`, and `capSpace` from rule-aware `getSalaryCap(game.year, game)`.
- The prior Direct Conditional-Pick Proposal Support slice remains shipped. Direct `/trades` proposals now expose unresolved owned `conditional_pick` assets through `getTradeableAssets` / `getTradeTargets`, value them through expected value, and transfer them through `trade-negotiation.ts` while updating `game.conditionalPicks` ownership plus the matching base draft pick holder when present. Trade Center keys them as `conditional:<id>`, labels them as `CONDITIONAL PICK`, and source/context copy now says generated offers and direct proposals both support conditional-pick assets.
- The prior Governance Route Coaching slice remains shipped. `/cba` now resolves to a `cba` route key and `/league-rules` resolves to `league-rules`, each with two unanchored Chip beats explaining vote/commit ownership, non-CBA rule scope, effective-rule reads, and the no-render-mutation boundary. No route component, route registration, tutorial state, save schema, `SAVE_VERSION`, sim, RNG, movement, sidecar, or package ownership changed.
- The prior League Governance Nav Discoverability slice remains shipped. `/cba` and `/league-rules` are `NAV_ITEMS` in the `LEAGUE` group, so desktop nav, mobile More, and command-palette screen search expose CBA bargaining and rule-registry surfaces directly. `nav-items.test.ts` removes both from the direct-only allowlist, and `App.test.tsx` locks grouped nav membership plus route mapping.
- The prior CBA Rule-Scope Clarity slice remains shipped. `/cba` source context labels `schedule_weeks`, `trade_deadline_week`, `overtime_format`, and `comp_pick_limit` as outside CBA term projection / commissioner-lane rules, pointing to `/league-rules` for active values and `/commissioner` for petitionable changes. No CBA term projection, votes, schema, rule math, save shape, `SAVE_VERSION`, sim, RNG, movement, route, sidecar, or package ownership changed.
- The prior Coaching Carousel HC Mirror Repair slice remains shipped. `runCoachingCarousel` CPU head-coach hires now write both `team.staff.hc` and the legacy `team.coachingStaff.hc` mirror, matching normal staff-market/open-slot hire behavior. No carousel scoring, fired/hired event/news shape, RNG, save schema, `SAVE_VERSION`, route, sim formula, player movement, sidecar, or package ownership changed.
- The prior Season-End Coaching Lifecycle slice remains shipped. `franchise-week.ts` calls existing coach-development and OC/DC poaching helpers during playoff champion rollover after season archive/story-arc work and before `nextState.year` increments. Staffed teams progress one active coach from saved record, clinic, and skill-tree inputs; deterministic `RNG.ai` coordinator poaching can remove OC/DC staff, clear matching `team.coachingStaff` mirrors, and write existing coach-departure event/news rows. No save schema, `SAVE_VERSION`, route, sim formula, player movement, sidecar, or package ownership changed.
- The prior Franchise Book Chapter Alerts slice remains shipped. `franchise-week.ts` calls `fireFranchiseBookChapterAlerts(nextState)` immediately after `archiveSeasonHistory(nextState)` in playoff champion rollover and before `nextState.year` increments, so live saves can receive deduped `leagueNews` milestone rows when a new non-Chapter-1 Franchise Book chapter opens. No chapter derivation, franchise-history archive shape, save schema, sim math, RNG, movement, sidecar, route, or package ownership changed.
- The prior Era Transition Reveal Integration slice remains shipped. `EraTransitionEmitter` renders the staged `EraTransitionReveal` for detected `selectFranchiseEras` / `detectFranchiseEras` changes, maps detected era names to reveal variants, keeps the year range in narrative copy, preserves reduced-motion and session-only fired-id semantics, and writes no save, sidecar, sim, RNG, movement, route, or package state.
- The prior Ceremony Sources slice remains shipped. `CeremonyViewer` renders a modal source panel for saved `game.ceremonies` rows selected by `selectCeremonies`, engine ceremony generators plus `recordCeremony` as the writer path, saved headline/description/highlights/MVP payload fields as the display source, and route-local open/close state. No save schema, `GameState`, ceremony generation, awards, HOF rows, records, sim, RNG, movement, route, sidecar, or package ownership changed.
- The prior Season Report Sources slice remains shipped. `SeasonReportViewer` renders a modal source panel for saved `game.seasonReports` rows selected by `selectSeasonReports`, `advanceOffseason` / `generateSeasonReport(game, team.id)` as the writer path, saved `report.sections` as the grade/highlight/stat source, and route-local open/expand state. No save schema, `GameState`, report generation, awards, records, sim, RNG, movement, route, sidecar, or package ownership changed.
- The prior Chip Event Dialogue Guidance slice remains shipped. `eventBridge` passes `weekRollover` / `gameComplete` / `seasonEnd` into `buildWeeklyGuidance`, so postgame and season-end Chip dialogue use event-specific top action/context while stable weekly dialogue ids and transient UI-only state stay intact. No save schema, `GameState`, sim, RNG, movement, route, sidecar, or package ownership changed.
- The prior Training Assignment Receipt slice remains shipped. `/roster` now renders a route-local receipt after the existing `actions.assignTraining -> assignTraining` commit resolves, reporting selected player, focus, previous focus, `calculateTrainingXP` preview, mentor-pair context, and saved `team.trainingAssignments` source. No save schema, `GameState`, progression formula, weekly training processing, sim, RNG, movement, route, sidecar, or package ownership changed.
- The prior Breaking News Overlay Source Receipt slice remains shipped. `App.tsx` renders the full-screen `BreakingNews` overlay from saved `game.breakingNewsQueue[0]`, passes queued event `source` through, and labels the saved breaking-news queue plus dismiss-only boundary. `dismissBreakingNews` remains the only queue-advance commit path. No save schema, `GameState`, generation formula, sim, RNG, movement, route, sidecar, or package ownership changed.
- The prior Coaching Skill Activation Receipt slice remains shipped. `/coaching` renders route-local development receipts after existing durable commit paths: `Clinic Run Receipt` for `actions.addClinicXP` and `Skill Activation Receipt` for `actions.setHeadCoachSkillSelection`. No save schema, `GameState`, formula, sim, RNG, movement, route, sidecar, or package ownership changed.
- The prior Expansion Draft Source Clarity slice remains shipped. `/expansion-draft` renders a read-only `Expansion Sources` panel in active and idle states, labeling saved `game.expansionDraftState`, route-local protection checkbox state, `protectExpansionPlayers`, the non-mutating preview feed boundary, and `finalizeExpansionDraft` as the league mutation that creates the new team, moves selected players/contracts, rebuilds the schedule, clears expansion state, commits, and routes back to `/franchise`. No route registration, nav metadata, save schema, sim, RNG, sidecar, or package ownership changed.
- The prior Trade Deadline Source Clarity, Completed-Deal Receipt, and Pending Action Receipt slices remain shipped. `/trade-deadline` renders a read-only `Deadline Sources` panel in active and idle states, labeling saved `game.tradeDeadlineState`, no render-time offer generation, store-owned Accept/Reject/Advance/Finalize actions, and the Finalize boundary where completed-deal rows actually move players or picks. It renders read-only `Deadline Deal Receipt` copy from saved completed-deal fields and route-local receipts after existing Accept, Reject, or Advance Clock actions resolve; Finalize intentionally keeps no route-local receipt because it resumes week advance and navigates to `/game-day`. No route registration, nav metadata, save, sim, RNG, sidecar, movement-rule, offer-generation, valuation, cap-formula, or package ownership changed.
- The prior Remaining Direct Route Identity Guard remains shipped. `App.test.tsx` now references every registered route variable and locks `/inbox`, `/social`, `/franchise/book`, `/film-room`, `/trade-deadline`, and `/franchise/scrapbook` to their intended inbox, social, book, film-room, deadline, and scrapbook surfaces. No runtime route registration, nav metadata, save, sim, RNG, sidecar, movement, or package ownership changed.
- The prior Special Direct Route Identity Guard remains shipped. `App.test.tsx` locks `/franchise/achievements` and `/league/weather` to their standalone achievement gallery and league forecast surfaces. No runtime route registration, nav metadata, save, sim, RNG, sidecar, movement, or package ownership changed.
- The prior Franchise Business/Scenario Route Identity Guard remains shipped. `App.test.tsx` locks `/relocate`, `/expansion-draft`, and `/scenarios` to their relocation, expansion draft, and scenario setup surfaces. No runtime route registration, nav metadata, save, sim, RNG, sidecar, movement, or package ownership changed.
- The prior Core/Team/Gameplay Route Identity Guard remains shipped. `App.test.tsx` locks roster/team/gameplay command routes such as `/roster`, `/watch-list`, `/locker-room`, `/depth-chart`, `/training-camp`, `/mentors`, `/endorsements`, `/week-advance`, `/handshakes`, `/game-day`, `/game-plan`, `/schedule`, and `/super-bowl` to their intended surfaces. No runtime route registration, nav metadata, save, sim, RNG, sidecar, movement, or package ownership changed.
- The prior Governance/League Route Identity Guard remains shipped. `App.test.tsx` locks `/owner`, `/commissioner`, `/cba`, `/league-rules`, `/standings`, `/analytics`, and `/power-rankings` to their intended owner, governance, rules, standings, analytics, and ranking surfaces. No runtime route registration, nav metadata, save, sim, RNG, sidecar, movement, or package ownership changed.
- The prior Meta/System Route Identity Guard remains shipped. `App.test.tsx` locks `/about`, `/credits`, `/faq`, `/dynasty`, and `/settings` to their launch, backup, and settings surfaces, while `nav-items.test.ts` keeps the meta nav group complete. No runtime route registration, nav metadata, save, sim, RNG, sidecar, movement, or package ownership changed.
- The prior Standings Dynamic Playoff Seed Display slice remains shipped. `/standings` derives its seed-count badge from `selectPlayoffPicture` and marks every displayed playoff seed after the locked top group as bubble instead of assuming seven seeds. No standings formulas, playoff helpers, save, sim, RNG, sidecar, route registration, movement, or package ownership changed.
- The prior Game Day Podium Receipt slice remains shipped. `/game-day` renders a read-only `Podium Receipt` from saved `GameDayPackage.pressConference` and labels its `buildPostWeekMoment` `Podium Follow-Up` hook. No package writer, podium queue answer, timed effect, event/news/social feed, save schema, sim, RNG, route-registration, sidecar, or movement rule changed.
- The prior Locker Room Action Receipts slice remains shipped. `/locker-room` shows route-local Team Meeting and Captain Rally receipts returned after the existing `callTeamMeeting` / `triggerCaptainRally` store actions commit saved `team.lockerRoom`. No save schema, event/news/social/inbox write, locker-room formula, sim, RNG, route-registration, sidecar, or movement rule changed.
- The prior Coaching Tree Notable Proteges slice remains shipped. `/coaching/tree` renders `buildCoachingLegacy(...).notableProteges` with branch rank, role, active team, and title count. No save schema, coach development, poaching, staff movement, relationship edge, coaching-history write, sim, RNG, route-registration, sidecar, or movement rule changed.
- The prior Monday Action Center Post-Week Receipt slice remains shipped. `/` reuses engine `buildPostWeekMoment` in `ActionCenter` to show a compact saved last-week receipt from `game.weekSummaries` and `game.gameDayState.recentPackages`, with two `whatNow` prompts and a `/week-advance` full-deck CTA. No save schema, package writer, weekly-summary writer, week-advance mutation, media generation, sim, RNG, route-registration, sidecar, or movement rule changed.
- The prior Missed-FA Near-Miss Receipt Wiring slice remains shipped. Free-agency round resolution records an existing near-miss tracker row when the user bid on a player and a CPU team actually signs him, and `/season/recap` labels that saved receipt source. No save schema, `SAVE_VERSION`, offer formula, signing winner, bid scoring, season-rollover receipt generation, sim, RNG, route-registration, sidecar, or movement rule changed.
- The prior Free Agency Forecast Readout remains shipped. `/free-agency` renders `buildFreeAgencyDecisionForecast` season-impact and future-risk fields beside existing immediate impact, resolution, warnings, source, status, and confidence for re-sign, open-market, and street-sign actions. No forecast helper, offer formula, bids, signings, roster movement, save schema, `SAVE_VERSION`, sim, RNG, sidecar, route-registration, or mutation behavior changed.
- The prior Companion Route Context Guardrail slice remains shipped. `useChipEvents`, `ChipDock` fallback route handling, and `ErrorBoundary` fallback context reuse `resolveCurrentAppRoute`, so hash routes and `/MFD/` deploy-base browser-history paths normalize consistently. No route registration, nav metadata, route-coaching catalog, save schema, `SAVE_VERSION`, sim, RNG, sidecar, movement, or mutation behavior changed.
- The prior Player Profile Signature Moments plus `PixelPlayerLink` hash-route guard slice remains shipped. `/player/$playerId` renders `Signature Moments` from existing awards, records, career/profile rows, saved player rivalries, user-team transaction rows, and lineage/archive context. Reusable player-card links hash-route to `/MFD/#/player/:id` under `createHashHistory()`. No save schema, history writer, award/record/rivalry/transaction writer, `SAVE_VERSION`, sim, RNG, sidecar, movement, route-registration, or mutation behavior changed.
- The prior Scenario Planning-Lanes Clarity slice remains shipped. `getScenarioConstraintCoverage` separates enforced blockers from `allowedPlanningPaths`, and `/scenarios` active state renders `Still open:` read-only planning lanes for active locks. No scenario definitions, enforcement paths, save schema, `SAVE_VERSION`, sim, RNG, movement, route-registration, or mutation behavior changed.
- The prior HOF Ballot Pressure Summary slice remains shipped. `/legacy` `Ballot Watch` renders saved `game.ballotWaitlist` candidates plus average saved vote, 70%+ support count, year-5 count, top-N shown, and top-watch copy. No save, formula, threshold, offseason timing, sim, RNG, movement, sidecar, or route-registration behavior changed.
- The prior Rivalry Heat-Map Saved Results slice remains shipped. `/franchise` `RivalryHeatMap` derives current-save W-L-T from saved completed regular-season schedule and playoff bracket results without persistence or sidecar writes.
- The prior HOF Ballot Persistence slice remains shipped. `HallOfFameBallotEntrySchema`, v35 migration normalization, current schema defaults, seed/demo/test/playtest defaults, web cartridge import normalization, and `/legacy` ballot-count copy make the existing deterministic ballot branch live for production-shaped saves. No `SAVE_VERSION` bump, HOF score/vote formula, offseason timing, sim math, RNG formula, movement, sidecar behavior, or route registration changed.
- The prior Complete-Dynasty Sidecar Archive slice remains shipped. `/dynasty` exports/imports `mfd.dynastySidecars.archive.v1`, bundling `mfd.hallOfFame.v1`, `mfd.scrapbook.v1` including pending playoff lore, `mfd.rookieOfYear.v1`, `mfd.rosterContinuity.v1`, and `mfd.careerMeta.v1`. Import validates all payloads before replacement and stays separate from `.mfd` cartridges, Dexie slots, `GameState`, autosave, save schema, `SAVE_VERSION`, sim math, RNG, movement, and routes.
- The prior Player Transaction Memory slice remains shipped, and Player Profile Signature Moments now builds on it. `/player/$playerId` renders display-only Transaction Memory from saved user-team `txLog` rows, plus `Signature Moments` from existing awards, records, career/profile rows, rivalries, transaction rows, and lineage/archive context. No save schema, transaction write, history writer, award/record/rivalry mutation, sim math, RNG, route registration, sidecar, or player movement changed.
- The prior Franchise Tag Web Wiring slice remains shipped. `/contracts` renders active `franchise_tag_limit` / `tag_types_allowed` context and a selected-player tag action; `actions.applyFranchiseTag` clones the current save, rejects non-expiring or duplicate tags, calls the engine helper with `GameState`, mirrors roster/player-map references, refreshes cap totals, snapshots undo, commits/autosaves, and keeps existing tag save fields. `labor-relations.ts` counts every `franchiseTags` entry with legacy `franchiseTag973` fallback. No save schema, `SAVE_VERSION`, cap formula, sim math, RNG, route registration, or player movement changed.
- The prior Monday Briefing Team Ops Carryover slice remains shipped. `/` renders a read-only `Team Ops Carryover` panel from `buildTeamOpsImpactReceipt(game, team.id)` over saved facility state, medical staff, active alumni mentors, mentor budget, and training-camp results. Settings, Training Camp, and Alumni Mentors remain the commit owners; no save schema, store action, sim math, progression math, RNG, movement, or sidecar path changed.
- The prior depth-chart starter commit persistence slice remains shipped. `setStarter` clones the current save, commits `Team.roster[].isStarter` through `commitGame`/autosave, mirrors `GameState.players[playerId].isStarter` when present, and completes depth-chart tutorial progress on promotes. It still does not enforce legal lineup counts, update roles, snapshot undo, auto-repair starters, alter sim math, move players, change save schema, bump `SAVE_VERSION`, or touch RNG/sidecars. The prior coaching clinic commit persistence, CBA user-abstain persistence, achievement condition schema tightening, durable HOF epilogue persistence, commissioner-ruling schema compatibility, endorsement deal schema tightening, `/inbox` comp-pick active limit receipt, `/trades` active deadline rule display, `/draft` comp-pick active rule display, `/front-office` contract rule context, `/newsroom` storyline receipts, `/team-needs` CPU Intent ledger/filters, and `/trade-block` CPU Intent filters remain shipped.
- The prior direct-only route-coaching guard remains shipped. `useActiveRouteBeats.test.ts` derives direct-only registered routes from `App.tsx` and requires every one to be explicitly route-coached or explicitly uncoached. No production runtime, route registration, save schema, sim math, RNG, movement, sidecar, or mutation path changed.
- The prior Season Recap route-coaching slice remains shipped. `/season/recap` resolves to the `season-recap` Chip route key with two unanchored read-only beats for recap receipt review and export/copy share-control boundaries. No route registration, recap generation, save schema, sim math, RNG, movement, sidecar, or mutation path changed.
- The prior Season Recap source clarity remains shipped. `/season/recap` renders `Season Recap Sources` for `buildSeasonRecap(game, team.id)`, `SeasonRecapCard`, saved `game.seasonNearMissReceipts`, explicit `Export PNG` / `Copy Text` helper imports, browser download/clipboard behavior, and the no-render-write boundary. No season archive, near-miss receipt generation, league-year advancement, player mutation, media write, save schema, sim math, RNG, movement, sidecar, or route registration changed.
- The prior Franchise Book source clarity remains shipped. `/franchise/book` renders `Franchise Book Sources` for `buildFranchiseBook(game, userTeam.id)`, saved `game.franchiseHistory`, saved `game.userDynastyEras`, saved `game.dynastyTimeline`, signature-player reads from `game.playerArchive` and `game.playerSeasonHistory`, authored `era-templates.json` / `book-commentary.json`, route-local TOC state, browser-only print controls, and the no-render-write boundary. No dynasty-event write, era start/name write, franchise-history update, player mutation, news write, save schema, sim math, RNG, movement, sidecar, or route registration changed.
- The prior Franchise Hub source clarity and Franchise Action Receipts remain shipped. `/franchise` renders `Franchise Sources` for `selectFranchiseDashboard`, `selectUserTeam`, `selectFranchiseEras`, `selectStadiumDealOffers`, `selectCanRelocate`, read-only child modules, `buildCoachingLegacy`, explicit `upgradeStadium` / `acceptNamingRights` / `/relocate` write paths, and the no-render-write boundary. Stadium and naming-rights buttons show route-local receipts after the existing store actions resolve. No durable receipt history, stadium/naming formula, relocation behavior, doctrine award, era detection, franchise-history update, save schema, sim math, RNG, movement, sidecar, or route registration changed.
- The prior Era Hall source clarity remains shipped. `/franchise/eras` renders `Era Sources` for detected dashboard eras, saved `game.franchiseHistory`, `buildEraHallEntries`, current-era roster snapshots, user-named era write ownership, and the no-render-write boundary. No era naming, era start/end write, `team.era` write, franchise-history update, championship award, save schema, sim math, RNG, movement, sidecar, or route registration changed.
- The prior GM Career source clarity remains shipped. `/franchise/career` renders `Career Sources` for browser-local `mfd.careerMeta.v1`, `readCareerMeta`, validated/recomputed career totals, `deriveDynastyId(game)`, the Rookie of the Year dynasty-id handoff, `appendDynastySummary`, `finalizeDynasty`, `actions.newGame`, and `actions.loadGame`; `App.test.tsx` also locks `/franchise/career` to `GmCareer`. Its Rookie of the Year panel now labels the browser-local award sidecar and rollover source boundary. No career-meta write, dynasty finalization, sidecar clearing, autosave, save schema, sim math, RNG, movement, or route registration changed.
- The prior Dynasty Chronicle source clarity remains shipped. `/franchise/chronicle` renders `Chronicle Sources` for `deriveDynastyId(game)`, `computeDynastyChronicle`, saved `franchiseHistory` / `hallOfFame` / `coachingHistory`, scrapbook and playoff-lore sidecar reads, filter/presenter helpers, route-local filter chips/detail modal state, browser-only PNG export, and the no-render-write boundary. No franchise-history write, Hall of Fame write, coaching-history write, scrapbook write, playoff-lore write, ceremony/record/media/achievement write, save schema, sim math, RNG, movement, or route ownership changed.
- The prior Dynasty Scrapbook source clarity and named-game auto-authoring remain shipped. `/franchise/scrapbook` renders `Scrapbook Sources` for `deriveDynastyId(game)`, browser-local `mfd.scrapbook.v1`, `readScrapbookForDynasty`, `summarizeScrapbook`, app-shell rollover write ownership through `syncScrapbookAtYearRollover` / `buildSeasonRecap` / `buildScrapbookEntry` / `appendScrapbookEntry`, pending playoff-lore merge semantics, route-local export status, browser-only PNG export, and the no-render-write boundary. `buildScrapbookEntry` also folds saved `dynastyTimeline` `named_game` events for the recap team/year into season notable moments. No scrapbook sidecar schema, sidecar write trigger, pending-lore clear rule, season-recap rebuild trigger, `GameState` update, media generation, era detection, achievement award, save schema, sim math, RNG, movement, or route ownership changed.
- The prior MVP Plaque Wall source clarity remains shipped. `/franchise/mvps` renders `Plaque Sources` for saved `game.awardsHistory` through `selectAwardsHistory`, current `selectUserTeam`, `selectFranchiseEras` labels from franchise history, `buildMvpPlaqueAwards` user-team filtering of MVP/OPOY/DPOY/Coach of the Year rows, saved `winnerStats`, rounded award `score`, route-local filter/sort controls, and the no-render-write boundary. No award generation, career-stat mutation, ceremony write, season-history patch, era write, player/record/HOF/archive/media/achievement write, save schema, sim math, RNG, movement, or route ownership changed.
- The prior Super Bowl presentation source clarity and latest route-coaching slices remain shipped. `/super-bowl` renders `Super Bowl Sources` for saved playoff bracket/result/champion/user-team/year inputs, deterministic presentation helpers, authored `getAwardSpeech` podium copy, browser-local `mfd-celebration-dismissed:<teamId>:<year>` dismissal state, and the no-render-write boundary; it also resolves to unanchored `super-bowl` Chip beats. No playoff bracket write, game-result write, ceremony write, awards/franchise-history/archive/media/achievement write, save schema, sim math, RNG, movement, or route ownership changed.
- The prior Awards Hub source clarity remains shipped. `/awards` renders `Award Sources` for saved `game.awardsHistory` through `selectAwardsHistory`, saved `AwardsHistoryEntry.ceremony` headline/intro fields, saved winner/runner-up/score/narrative rows, route-local season scrubber state, and the no-render-write boundary. No award generation, ceremony writes, season-history patching, player mutation, records/HOF/archive changes, media publication, achievement unlocks, save schema, sim math, RNG, movement, or route ownership changed.
- The prior player-rivalries source clarity remains shipped. `/rivalries` renders `Rivalry Sources` for saved `game.playerRivalries`, `selectAllPlayerRivalries`, saved `socialFeed` rivalry posts, franchise-week/player-matchup write ownership, live sim-bonus source semantics, and the no-render-write/no-derived-sidecar boundary. No rivalry detection, decay, social generation, sim bonus, save schema, RNG, sidecar wiring, or movement changed.
- The prior player-development projection/source clarity remains shipped and is extended by Player Development Player Picker. `/player-development` now supplies `generateDevelopmentReport`, a three-year `projectDevelopmentCurve`, `identifyBreakoutCandidates`, and report-owned `coachImpact` copy for the route-local selected roster player; the view renders `Development Sources` copy and remains no-write. No progression math, archetype mutation, training-camp spend, coach-effect mutation, player history, save schema, sim math, or RNG changed.
- The prior Team-needs scenario planning notice remains shipped. `/team-needs` renders planning-only Scenario Lock copy from saved `scenarioState.activeScenario.constraints.blockDraft` / `blockFreeAgency` / `blockTrades` while keeping the report and comparison rooms available because the route does not refresh the needs cache, submit bids, sign players, draft players, create trade proposals, or move roster entries. No scenario definitions, enforcement, store commits, cache refresh, bids, signings, draft picks, trade proposals, roster movement, save schema, sim math, or RNG changed.
- The prior FA target-board acquisition-lock planning notice remains shipped. `/fa-targets` renders planning-only Scenario Lock copy from saved `scenarioState.activeScenario.constraints.blockFreeAgency` while keeping Refresh Board plus Watch/Unwatch available because those only update the target snapshot/watchlist and do not sign players. No scenario definitions, enforcement, store commits, bids, signings, roster movement, save schema, sim math, or RNG changed.
- The prior Trade-block scenario planning notice remains shipped. `/trade-block` renders planning-only Scenario Lock copy from saved `scenarioState.activeScenario.constraints.blockTrades` while keeping read-only scanning, filters, sorting, and the Trade Center link available. It still does not create proposals, accept offers, move assets, write trade suggestions, or mutate player trade-block flags. No scenario definitions, enforcement, store commits, offer generation, trade valuation, movement, save schema, sim math, or RNG changed.
- The prior Trade-center scenario-lock notice and store guard remain shipped. `/trades` renders Scenario Lock copy from saved `scenarioState.activeScenario.constraints.blockTrades`; generated-offer Accept, direct proposal Submit, and Accept Counter buttons are disabled, and direct store calls to generated-offer accept, proposal creation/submission, or counter accept now return before undo/audio/autosave/commit. Reject, Reject Counter, package building, and trade-block scanning remain available no-movement paths. No scenario definitions, offer generation, trade valuation, movement, save schema, sim math, or RNG changed.
- The prior Trade-deadline scenario-lock notice remains shipped. `/trade-deadline` renders Scenario Lock copy from saved `scenarioState.activeScenario.constraints.blockTrades`; deadline Accept buttons are disabled to match the existing store no-op guard, while Reject, clock advance, and finalization remain available. No scenario definitions, enforcement, store commits, offer generation, trade valuation, movement, save schema, sim math, or RNG changed.
- The prior Free-agency scenario-lock notice remains shipped. `/free-agency` renders Scenario Lock copy from saved `scenarioState.activeScenario.constraints.blockFreeAgency`; open-market bid and street-signing buttons are disabled to match existing store/engine no-op guards, while in-house re-sign negotiation and market advancement remain available. No scenario definitions, enforcement, store commits, bids, signings, save schema, movement, sim math, or RNG changed.
- The prior Waiver/practice-squad scenario-lock notice remains shipped. `/waivers` and `/practice-squad` render Scenario Lock copy from saved `scenarioState.activeScenario.constraints.blockFreeAgency`; Submit Claim and Add buttons are disabled to match existing store no-op guards, while practice-squad release/elevation remains available. No scenario definitions, enforcement, store commits, save schema, movement, sim math, or RNG changed.
- The prior Draft scenario-lock notice remains shipped. `/draft` renders Scenario Lock copy from saved `scenarioState.activeScenario.constraints`, disables Draft Player buttons when `blockDraft` is active, disables draft-night Accept buttons when `blockTrades` is active, and leaves Reject available for stale offers. No scenario definitions, scenario enforcement, store commits, save schema, movement, sim math, or RNG changed.
- The prior Draft war-room offer confidence and market receipt slices remain shipped. `/draft` incoming offer cards render read-only Offer Confidence from `selectWarRoomState.incomingOffers`, current pick context, critical needs, top visible prospect, and `calcPickValue` chart reads before the existing Accept/Reject buttons. They also render read-only `Draft Market Receipt` copy from saved incoming-offer team/target/urgency/reasoning/assets plus that confidence read. No offer generation, trade valuation, draft order, pick movement, save schema, sim math, RNG, or commit path changed.
- The Draft War-Room Live Order Ownership slice remains shipped. `acceptDraftTradeOffer -> applyDraftTradeOffer` validates live current-year pick assets against source `draftPicks`, transfers real picks, updates matching `offseasonState.draftOrder` owner/id entries, and returns the original state for stale/no-op accepts so the store skips commit/audio/autosave. Do not redo this current-year live-order path.
- The Draft War-Room Real-Backed Offer Generation slice remains shipped. `generateDraftTradeOffers` now emits trade-down offers only from real live draft-order rows backed by source team `draftPicks`, retires synthesized fallback candidates, and includes future round sweeteners only when the offering team owns a transferable future pick with a year-qualified pick id. Do not redo generated-offer backing.
- The Draft War-Room Browser Accepted Offer Proof slice remains shipped. `SMOKE_DRAFT_WAR_ROOM_TRADE=1` opens `/draft`, accepts a route-generated source-backed offer with a future sweetener, verifies autosave/hard-reload persistence of pick ledgers and live draft order, and reports zero browser errors. Do not redo this smoke unless `/draft` accept behavior regresses.
- The Draft War-Room Durable Accepted-Trade Receipt slice remains shipped. Accepted draft-war-room trades now append a deterministic `leagueNews` trade receipt after the engine validates and applies the transfer; the focused smoke verifies that receipt after autosave hard reload. Future draft-trust work should target prospect/player memory, recap callbacks, or broader release proof rather than accepted-offer receipt persistence.
- The Draft Post-Draft Recap CTA slice remains shipped. `/draft` now renders `Draft Closure` in `post_draft`, routes to a saved current-year recap when available, or uses existing `advanceWeek -> finalizePostDraft` before routing. Future draft work should target player arcs outside already covered draft/Game Day surfaces or broader gate refresh when draft behavior changes, not this CTA or recap profile links.
- The prior Legacy/archive route identity guard remains shipped. `App.test.tsx` locks `/legacy`, `/legacy/named-games`, `/legacy/bloodlines`, `/awards`, `/records`, `/franchise`, `/legends`, `/franchise/hall`, `/franchise/trophy-room`, `/franchise/eras`, `/franchise/mvps`, `/franchise/playoff-lore`, and `/franchise/chronicle` to their distinct saved-memory, sidecar-archive, trophy, era, playoff-lore, and chronicle surfaces. No production runtime, save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, selector formula, week advance, schedule behavior, or result behavior changed.
- The prior Coaching decision receipt slice remains shipped. `/coaching` renders a read-only `Coaching Decision Receipt` from staff vacancies, best current market fit, highest retention/poach signal from `buildCoachRetentionDecision`, scheme continuity from `projectSchemeTransition`, and clinic leverage from saved XP/perks. Existing Hire, Fire, Promote, Refresh Market, Apply Identity, Run Clinic, and Activate tier buttons remain the only commit paths. No save schema, route registration, route component ownership, staff mutation, market scoring, retention lifecycle, GameState, sidecar, sim math, RNG, movement, week advance, schedule behavior, or result behavior changed.
- The prior Player route identity guard remains shipped. `App.test.tsx` now locks `/player/$playerId`, `/player/$playerId/timeline`, `/compare`, `/rivalries`, and `/player-development` to their profile, timeline, comparison, rivalry, and development surfaces. No production runtime, save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, selector formula, week advance, schedule behavior, or result behavior changed.
- The prior Acquisition route identity guard remains shipped. `App.test.tsx` locks `/trades`, `/trade-block`, `/free-agency`, `/fa-targets`, `/team-needs`, `/waivers`, `/practice-squad`, `/scouting`, and `/draft` to their distinct market, planning, waiver, squad, scouting, and draft surfaces. No production runtime, save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, selector formula, week advance, schedule behavior, or result behavior changed.
- The prior Meta/System route identity guard remains shipped. `App.test.tsx` locks `/about`, `/credits`, `/faq`, `/dynasty`, and `/settings` to their launch, backup, and settings surfaces, while `nav-items.test.ts` keeps the meta nav group complete. No production runtime, save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, selector formula, week advance, schedule behavior, or result behavior changed.
- The prior Governance/League route identity guard remains shipped. `App.test.tsx` locks `/owner`, `/commissioner`, `/cba`, `/league-rules`, `/standings`, `/analytics`, and `/power-rankings` to their distinct owner, governance, rules, standings, analytics, and rankings surfaces. No production runtime, save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, selector formula, week advance, schedule behavior, or result behavior changed.
- The prior Core/Team/Gameplay route identity guard remains shipped. `App.test.tsx` locks roster/team/gameplay routes including roster, watch list, locker room, depth chart, training camp, mentors, endorsements, week advance, handshakes, game day, game plan, schedule, and Super Bowl to their command surfaces. No production runtime, save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, selector formula, week advance, schedule behavior, or result behavior changed.
- The prior Presentation source context slice remains shipped. `/presentation` renders `Presentation Sources` for `selectLatestBroadcast`, `buildBroadcastPresentation`, matching saved packages through `selectGameDayPackageByBroadcastGameId(null)`, `buildHalftimeDecisionReceipt`, route-local beat controls, and the no-render mutation boundary. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, presentation generation, broadcast generation, selector rebuild formulas, week advance, schedule behavior, or result behavior changed.
- The prior Game Flow source context slice remains shipped. `/game-flow` renders `Game Flow Sources` for `selectLatestBroadcast`, pure `analyzeGameFlow`, `PixelEkg` win-probability inputs, derived quarter momentum / drive efficiency / scoring run / turning-point reads, and the no-render mutation boundary. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, game-flow formulas, broadcast generation, selector rebuild formulas, week advance, schedule behavior, or result behavior changed.
- The prior Play-by-Play source context slice remains shipped. `/play-by-play` renders `Play-by-Play Sources` for `selectLatestBroadcast`, saved or rebuilt `BroadcastOutput.quarters`, route-local drive expansion, copied/sorted/capped highlights, ghost lines, `buildHalftimeDecisionReceipt`/`buildBroadcastCommentary` texture rows, and the no-render mutation boundary. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, broadcast generation, selector rebuild formulas, game-day package writes, halftime receipt parsing, ghost-line generation, week advance, schedule behavior, or result behavior changed.
- The prior Broadcast source context slice remains shipped. `/broadcast` renders `Broadcast Sources` for transient `useUiStore.broadcastGameId`, `selectBroadcastByGameId`, saved result broadcast read models, `selectGameDayPackageByBroadcastGameId`, `buildHalftimeDecisionReceipt`, deterministic `buildBroadcastCommentary`, route-local tabs/ghost lines/MVP radar, and the no-render mutation boundary. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, broadcast generation, selector rebuild formulas, game-day package writes, halftime receipt parsing, ghost-line generation, week advance, schedule behavior, or result behavior changed.
- The prior Film Room source context and route-coaching slices remain shipped. `/film-room` renders `Film Room Sources` for `selectLatestFilmRoomReport`, `selectFilmRoomHistory`, `buildFilmRoomReport`, `franchise-week.ts` write ownership, the 24-report cap, and the no-render report-generation boundary; it also resolves to unanchored `film-room` Chip beats for saved prep-report review after a result. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, film-room grading, weekly-prep evaluation, opponent-intel requirements, week advance, `weeklyPrepHistory`, `filmRoomHistory`, game-plan reset behavior, schedule behavior, or game-result behavior changed.
- The prior game-plan weekly-prep source context slice remains shipped. `/game-plan` renders `Weekly Prep Sources` for opponent report/intel selectors, route-local prep draft state, `buildPrepDecisionForecast`, Save Weekly Prep & Sim, Skip With Auto Prep, and the no-render simulation boundary. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, opponent-report generation, weekly-prep formulas, Call Your Shot persistence, week advance, snap resolution, contingency execution, film-room receipt generation, schedule behavior, or game-result behavior changed.
- The prior league-rules source context slice remains shipped. `/league-rules` renders `Rule Registry Sources` for saved `game.leagueRules`, selector diffs/history, `/commissioner` and `/cba` commit owners, and the `getActiveRule` / `getRuleValueForYear` effective-year boundary. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, rule application, schedule/playoff/cap/roster/deadline recalculation, or league-rule history behavior changed.
- The prior CBA source context slice remains shipped. `/cba` renders `CBA Sources` for saved `game.cbaState`, displayed `negotiationState` proposals, approve/reject/abstain `voteOnCBA`, `advanceCBANegotiation`, `/league-rules` as the effective-rule history view, and no-render-mutation boundaries. User CBA abstain persistence is shipped, and the later CPU CBA Neutral Abstain slice records exactly neutral CPU owner scores as `abstain`. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, rule application, labor update, or CBA schema behavior changed in the source-context slice.
- The prior governance source context slice remains shipped. `/commissioner` renders `Governance Sources` for saved commissioner-state selectors, petition/rule-vote commit actions, `selectCBAState`, `/cba` as the bargaining commit route, `LEAGUE_RULE_DEFINITIONS` / `/league-rules`, and the no-render-ruling-write boundary. No route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, CBA vote behavior, rule application, or issue-ruling path changed.
- The prior Commissioner Action Receipts slice remains shipped. `/commissioner` shows route-local Rule Petition/Vote receipts after existing `petitionRuleChange` or `voteOnProposal` actions resolve, labeling source, commit path, state touched, and pre-action rule context without adding durable vote history, a second governance ledger, save schema, CBA vote behavior, rule-application behavior, sim math, RNG, movement, or route registration.
- The prior CBA Action Receipts slice remains shipped. `/cba` shows route-local Vote/Advance/Lockout receipts after existing `voteOnCBA` or `advanceCBANegotiation` actions resolve, labeling source, commit path, state touched, and pre-action bargaining context without adding durable vote history, a second bargaining ledger, save schema, vote threshold changes, CBA term formulas, rule projection changes, labor math, sim math, RNG, movement, or route registration.
- The prior trade-block source clarity and market-receipt slices remain shipped. `/trade-block` renders `Trade Block Source` for `selectLeagueTradeBlock`, CPU-team `findTradeTargets` output, trade-block flags, needs, cap compatibility, valuation, route-local filters/sort state, `/trades` as the commit path, and no-proposal/no-movement/no-route-write boundaries. It also renders per-target `Market Receipt` copy from saved CPU intent plus seeker/valuation fields, and planning-only Scenario Lock copy when saved `blockTrades` is active. `/trades` now also renders saved Trade Finder generated-offer receipts. No save schema, route registration, route component ownership, GameState, sidecar, sim math, RNG, movement, generated-offer, or trade commit behavior changed.
- The prior coaching staff source context slice remains shipped. `/coaching` labels `selectCoachingStaff`, `selectCoachingMarket`, route-local scheme preview state, saved clinic/skill selections, existing commit buttons, and no-render-write boundaries. The duplicate market-refresh affordance is guarded.
- The prior Position Coach Lifecycle Actions slice remains shipped. `/coaching` can initialize missing saved `team.positionCoaches` rooms, upgrade one existing role, and show route-local `Position Coach Receipt` panels after explicit store actions commit. The actions use existing engine helpers and deterministic store seeding; no position-coach save shape, migration, progression formula, HC/OC/DC staff path, clinic XP, skill-selection, coaching market, route registration, sim math, or package dependency changed.
- The prior Position-Coach Season Tenure Rollover slice remains shipped. Championship rollover calls existing `advancePositionCoachSeason` from `advanceSeasonEndCoaching` for teams that already have saved `team.positionCoaches`, incrementing `yearsWithTeam` before the next offseason year. No default seeding, save shape, migration, `SAVE_VERSION`, progression formula, HC/OC/DC development rule, poaching rule, coach-history row, route registration, sim math, RNG, or package dependency changed.
- The prior Position-Coach Tenure Source Clarity slice remains shipped. `/coaching` labels championship rollover as the saved-tenure source in Coaching Source Context and the Position Coach Report footer, while preserving that opening the route does not seed coaches, advance tenure, upgrade roles, alter progression, add clinic XP, write save data, or run offseason coach development.
- The prior social route-coaching split remains shipped. `/social` resolves to `social-feed`; `/news` and `/newsroom` remain split as `league-news` and `newsroom`; all three media keys are intentionally unanchored and mapped to route files in `spotlightAnchors.test.ts`.
- The prior relationship-graph source clarity slice remains shipped. `/coaching/relationships` renders `Relationship Sources` from saved `game.relationships`, active staff/roster label resolution, route-local filters/layout, and the no-write boundary; it also uses stable empty selector fallbacks so old/demo saves without `relationships` do not trigger React store-snapshot loops. No save schema, relationship writer, staff lineage field, route registration, sidecar, sim math, RNG, movement, or route behavior changed.
- The prior command-palette honesty slice remains shipped and is extended by Command Palette Roster Player Search plus Keyboard Ownership Guard. `RootLayout` now passes `Search screens, actions, roster players...`, builds commands from `NAV_ITEMS`, capped `selectRoster` player commands, and `action-advance-week`, routes player commands to `/player/$playerId`, owns `Cmd/Ctrl+K` toggling, and disables the reusable palette shortcut listener for the app instance; reusable team category support remains design-system capacity. No save schema, route registration, team command source, UI store persistence, GameState, sidecar, sim math, RNG, movement, or route behavior changed.
- The prior Monday Briefing source clarity slice remains shipped, and the Team Ops Carryover plus Action Center Post-Week Receipt follow-ups are now shipped. `/` renders display-only `Briefing Sources` copy from `MondayBriefing` selectors, saved `game.dashboardState`, widget read models, `ActionCenter` guidance inputs, local customize-modal drafts, and a no-render-write boundary. It also renders read-only team-ops carryover from `buildTeamOpsImpactReceipt(game, team.id)` and a compact saved last-week receipt from `buildPostWeekMoment(game.weekSummaries.at(-1), latest game-day package)` with a `/week-advance` full-deck CTA. No save schema, dashboard save shape, widget formulas, layout actions, route shell, week advance, package writer, weekly-summary writer, media generation, facility/medical/camp/mentor commit path, sim math, RNG, movement, or route registration changed.
- The prior Inbox source clarity slice remains shipped. `/inbox` renders display-only `Inbox Sources` copy from `buildInboxMessages`, selector-gathered route inputs, local `selectedMsg`/filter/apology-modal state, CTA navigation boundaries, the owner-personality mailbox split, regenerated read flags, and a no-render-write boundary. No save schema, inbox sidecar, read receipt, saved owner inbox state, generated-message formula, route CTA behavior, sim math, RNG, movement, or route registration changed.
- The prior Locker Room source clarity and action receipt slices remain shipped. `/locker-room` renders display-only `Locker Room Sources` copy from `selectLockerRoom`, saved `team.lockerRoom`, existing meeting/rally/captain action owners, route-local `eligibleBench`, `CAPTAIN_PERK_EFFECTS`, and a no-render-write boundary. Meeting/rally actions now return route-local receipts after existing saved `team.lockerRoom` commits. No save schema, locker-room payload shape, culture math, captain effects, weekly update, sim bonus, RNG, movement, route registration, or durable event/news/social/inbox write changed.
- The prior Watch List source clarity slice remains shipped. `/watch-list` renders display-only `Watch List Sources` copy from browser-local `mfd.watchlist.v1`, `buildWatchListRows`, separate saved scouting/FA watchlists, board-level `updatedAt`, and a no-render-write boundary. No save schema, watch-list payload shape, player movement, draft/free-agent board, archive, sim math, RNG, movement, or route registration changed.
- The prior Depth Chart source clarity slice remains shipped, and starter commit persistence is now shipped. `/depth-chart` renders display-only `Depth Chart Sources` copy from `selectRoster`, saved `Team.roster[].isStarter` flags, durable `setStarter`, `detectPositionBattles(roster)`, `selectSpecialTeams`, returner assignment actions, and a no-render-write boundary. Starter buttons now commit roster flags, mirror the global player map, and complete tutorial progress on promotes, but still do not enforce legal lineup counts, update roles, auto-repair starters, or change save schema, special-teams helpers, snap counts, sim math, RNG, movement, or route registration.
- The prior Analytics source clarity slice remains shipped. `/analytics` renders display-only `Analytics Sources` copy from `selectAdvancedStats`, `selectAnalyticsLeaders`, `selectRoster` plus `calculatePlayerEfficiency`, `selectWeeklyTrend`, and `selectPlayerComparison`, with a no-render-write boundary. No save schema, advanced-stat formulas, player-efficiency formulas, stat-leader formulas, trend selectors, sim math, RNG, movement, or route registration changed.
- The prior Standings source clarity and dynamic seed display slice remains shipped. `/standings` renders display-only `Standings Sources` copy from `selectStandings`, `selectPlayoffPicture`, `selectStatLeaders`, and local division/seed/streak signals; it derives seed count from playoff-picture output instead of assuming seven seeds. No save schema, standings formulas, playoff helpers, stat-leader formulas, sim math, RNG, movement, or route registration changed.
- The prior Schedule source clarity slice remains shipped. `/schedule` renders display-only `Schedule Sources` copy from `selectTeamSchedule`, `selectWeekSchedule(selectedWeek)`, saved broadcast/flex/result fields, and byes, with a no-render-write boundary. No save schema, schedule generation, flex/broadcast mutation, weather generation, sim math, RNG, movement, or route registration changed.
- The prior Player Profile source clarity and selector stability slice remains shipped, and the profile route now includes Signature Moments. `/player/$playerId` renders display-only `Profile Sources` copy from `selectPlayerProfileBundle`, season/profile rows, `playerArchive`, saved `playerRivalries`, saved user-team `txLog`, and a no-render-write boundary; `selectPlayerRivalries(playerId)` is memoized by player id and `state.game`; `PixelPlayerLink` hash-routes player-card navigation to `/player/:id`. No save schema, durable history, award/record/transaction/rivalry mutation, sim math, RNG, sidecar, or movement changed.
- The prior training-camp readiness/carryover clarity slice remains shipped. `/training-camp` renders read-only `Camp Readiness` copy from saved `game.phase`, saved `trainingCampResults`, the shared Team Ops receipt, and `advanceFranchiseWeek` as the only camp write path. No training-camp resolution, progression math, injury generation, save schema, sim math, RNG, movement, or store action changed.
- The prior Settings Operations Action Receipts slice remains shipped. `/settings` renders route-local `Operations Action Receipt` panels after enabled facility Upgrade or Hire Staff buttons resolve through existing facility/medical store and engine commit paths. No durable receipt history, facility formula, medical pool refresh, injury recovery, progression, training-camp resolution, save schema, sim math, RNG, movement, or route registration changed.
- The prior medical-staff hiring forecast slice remains shipped. `/settings` staff candidates render read-only `Hiring Forecast` copy from saved `team.medicalStaff`, game-level `availableMedicalStaff`, phase gate, salary, and recovery/prevention deltas; `hireMedicalStaff` remains the only commit path. No medical pool refresh, injury recovery, facility formula, progression, training-camp resolution, save schema, sim math, RNG, or movement changed.
- The prior facility upgrade forecast slice remains shipped. `/settings` facility rows render read-only `Upgrade Forecast` copy from saved `team.facilityState`, saved `facility.effect`, current budget/costs, and `getFacilityLevelEffect`; the existing `upgradeFacility` button remains the only commit path. No facility formulas, progression, training-camp resolution, injury recovery, save schema, sim math, RNG, or movement changed.
- The prior draft-pick confidence forecast slice remains shipped. `/draft` Big Board rows render a read-only `Pick Forecast` from current pick round, prospect projected round, critical-need fit, saved `offseasonState.scoutingState` confidence/risk/ceiling/character, and source copy before `makeDraftPick`; no prospect generation, pick mutation, reveal persistence, war-room formulas, save schema, sim math, RNG, or commit paths changed.
- The prior roster-cut decision clarity slice remains shipped. `/roster` renders a selected-player `Cut Forecast` from standard-cut `calcDeadMoney`, cap hit, room delta, active roster relief, waiver path, and `buildCutAdvisor` context before the existing cut confirmation; the confirmation copy labels dead money and waiver movement without changing `cutPlayer`, movement, cap formulas, save schema, sim math, RNG, or commit paths.
- The prior free-agency decision forecast slice remains shipped. `buildFreeAgencyDecisionForecast` is an engine-owned read model for re-sign offers, open-market bids, and street signings; `/free-agency` renders forecast rows with immediate impact, season impact, future risk, resolution, source, warnings, status, and confidence beside the existing action buttons without changing save schema, bids, signings, roster movement, sim math, RNG, or commit paths.
- The prior free-agency scenario-lock notice remains shipped. `/free-agency` renders Scenario Lock copy from saved `scenarioState.activeScenario.constraints.blockFreeAgency`, disables open-market bid and street-signing buttons, and leaves in-house re-sign negotiation plus market advancement available. No scenario definitions, enforcement, bids, signings, save schema, movement, sim math, RNG, or commit paths changed.
- The prior Offseason Calendar Readout remains shipped. `/free-agency` mirrors the existing `selectOffseasonCalendar` read model beside market actions, labeling saved phase/offseason/CBA/expansion calendar status, selector ownership, and the no-render-write boundary. Opening the route does not advance weeks, resolve bids, generate draft state, move players, mutate saves, touch RNG, simulate games, or create a second calendar model.
- The prior Free Agency Bid Resolution Summary slice remains shipped. `/free-agency` reads saved `offseasonState.freeAgencyBids` won/lost rows after round resolution and renders the result summary without resolving, scoring, submitting, moving, autosaving, or rerolling anything.
- The prior CPU Strategy History Display slice remains shipped. `/team-needs` reads saved `eventLog` and `leagueNews` strategy/philosophy history rows for the selected CPU club and renders them without reevaluating or writing AI state.
- The prior Postgame Source/Decision Receipt slices remain shipped. `/game-day` reads the saved latest `GameDayPackage` and renders package-source plus decision-context receipt rows without replaying the game, rebuilding a package, recalculating Film Room, applying training, mutating scores/injuries/players/morale, answering press, autosaving, or touching RNG.
- The prior Offseason Command Snapshot slice remains shipped. `/week-advance` reads the shared offseason calendar, `offseasonState`, roster contract, and cap fields to summarize owner route, expiring core, cap room, and market load without committing any offseason action.
- The prior private-workout/pro-day explanation slice remains shipped. `/scouting` prospect cards explain pro-day `proDayRating`, confidence/visible-grade tightening, no workout-slot spend, private-workout slot consumption, saved `privateWorkoutRatings`, top-rating verification, and risk/ceiling-band unlock behavior from existing saved intel. No scouting action, prospect generation, workout spending, save schema, movement, sim math, RNG, or route registration changed.
- The prior Scouting Action Receipts slice remains shipped. `/scouting` shows route-local receipts after existing hire/fire/watchlist/film/combine/interview/pro-day/private-workout actions resolve, labeling saved or route-local state touched, existing commit paths, and pre-action context without adding durable receipt history, save schema, scouting formulas, scout pool refresh, prospect generation, workout-spend changes, movement, sim math, RNG, or route registration.
- The prior custom trade-deadline recognition slice remains shipped. `game-store.ts` reads the active `trade_deadline_week` for post-engine interruption routing after both normal `advanceWeek` and `resolveHalftimeDecision`, matching halftime suppression and engine deadline initialization. `/trade-deadline` now also renders Scenario Lock copy when saved `blockTrades` is active. No trade generation, deadline clock, package writer, weekly-summary writer, save schema, sim math, RNG, route registration, or movement path changed.
- The prior structured halftime/broadcast receipt hook remains shipped. `buildHalftimeDecisionReceipt` parses saved `Halftime hell:` summaries in `GameDayPackage.activeEffectSummaries`; `selectGameDayPackageByBroadcastGameId` matches the saved package to the selected/latest broadcast result; and `GameDayRecap`, `GameBroadcast`, `PlayByPlay`, and `BroadcastPresentation` consume the same read model. No package writer, weekly-summary writer, week-advance mutation, broadcast generation, save schema, sim math, RNG, route registration, or movement path changed.
- The prior cap rule-awareness regression guard remains shipped. `apps/web/src/app/architecture-boundaries.test.ts` blocks production app/runtime-system code from calling `getSalaryCap(...)` or `getCapFloor(...)` with only a year, while allowlisting `apps/web/src/app/store/seed.ts` because seed-time salary scaling runs before a save `GameState` exists. No cap formulas, save schema, sim math, movement, or mutation path changed.
- The prior halftime receipt source note remains shipped. `GameDayRecap.tsx` detects saved `Halftime hell:` summaries in `GameDayPackage.activeEffectSummaries` and renders a `HALFTIME DECISION RECEIPT` source note. No package writer, weekly-summary writer, week-advance mutation, broadcast generation, save schema, sim math, or RNG changed.
- The prior scouting-board source clarity slice remains shipped. `/scouting` renders `Scouting Sources`, labeling saved `game.draftClass`, saved `offseasonState.scoutingState` intel, store/engine scouting action ownership, saved draft scouting watchlist versus browser-local `mfd.watchlist.v1` pins, authored scouting-report templates, and the no-render-mutation boundary. No prospect generation, scouting formulas, scout pool refresh, workout spending, watchlist persistence shape, save schema, movement, sim math, or RNG changed.
- The prior draft-board source clarity slice remains shipped. `/draft` renders `Draft Board Sources`, labeling saved `game.draftClass`, saved `offseasonState.scoutingState` intel/fallbacks, `selectWarRoomState` saved-versus-derived behavior, existing store action owners, and the route-local non-durable reveal card boundary. No prospect generation, pick mutation, war-room formulas, save schema, movement, sim math, or RNG changed.
- The prior halftime modal copy alignment slice remains shipped. `HalftimeDecision.tsx` describes the engine-backed `switch` `-1/+1` and `gamble` `+3/-2` team OVR deltas instead of stale percentage-efficiency copy. No sim formulas, save schema, RNG, movement, or week-advance commit path changed.
- The prior Super Bowl storyline champion-casing slice remains shipped. `generateSuperBowlContext` counts prior championships through a case-insensitive champion-finish helper, so canonical lowercase `playoffFinish: 'champion'` franchise-history rows drive dynasty and first-title Super Bowl storylines. No playoff simulation, bracket, save schema, sim math, or RNG changed.
- The prior near-miss recap receipt slice remains shipped. `/season/recap` renders saved `game.seasonNearMissReceipts` as `What-If Receipts` when present, labels season-rollover generation, declined direct-trade inputs, passed-pick draft-flow inputs, user free-agent bids that lose to signed CPU offers, and no render-time receipt generation or tracker mutation.
- The prior coaching tree source clarity slice remains shipped, and the notable-proteges readout is now shipped. `/coaching/tree` labels active league `team.staff` HC/OC/DC records, saved `game.coachingHistory`, `coach_retirement` event labels, `buildCoachingLegacy` metrics, mentor/disciple staff fields, and no route writes for staff moves, relationship edges, coaching history, or coach development. It also renders `buildCoachingLegacy(...).notableProteges` as active branch rank, role, team, and title-count rows.
- The prior bloodlines source clarity slice remains shipped. `/legacy/bloodlines` labels `selectBloodlineFamilies`, active `game.players`, draft prospects with saved `bloodline` fields, active roster OVR/age/team data, scout-grade rookie OVR signal, and no route writes for bloodline assignment or family relationship edges.
- The prior franchise memory source-label slice remains shipped. `/franchise` labels Homegrown as current roster plus `playerArchive` provenance without archive writes, Continuity as browser-local `mfd.rosterContinuity.v1` starter snapshots rather than cartridge data, and RivalryHeatMap as authored team rivalry content plus runtime lookup. The heat map now derives W-L-T, total games, and latest saved meeting result/score/year/week from saved completed schedule/playoff results without persistence or sidecar writes.
- The prior app lifecycle guardrails slice remains shipped. `App.test.tsx` source-guards `RootLayout` global keyboard setup, route shortcut registration/cleanup, hotkey-help metadata, shell overlays, audio queue playback/clearing, save reminders, year-rollover sidecar sync calls, and the autosave toast week-baseline cue; `appShellLifecycle.test.ts` locks the pure toast-baseline decision and `useKeyboard.test.ts` locks `useGlobalKeyboard` document listener cleanup.
- The prior broader post-week receipt expansion remains shipped. `buildPostWeekMoment` carries saved `GameDayPackage` stakes, rivalry heat, special-teams swings, podium follow-up, and coaching notes into Week Advance's existing `Post-Week Command Deck` without changing package writers, weekly summaries, week-advance mutation, media generation, save schema, sim math, or RNG.
- The prior action-point scenario notices slice remains shipped. Monday Briefing `Command Queue` renders `Scenario locks active` from active `game.scenarioState` through `getScenarioConstraintCoverage`, labels enforced trade/free-agency/draft locks, says locked actions do not commit, and links to `/scenarios`.
- The prior Owner-pressure clarity slice remains shipped. `/owner` renders `Owner Pressure Sources`, labeling saved `ownerMandates`, mandate handshake mirrors, no-double-application behavior, saved week summaries, and Inbox/season-report receipt consumers. `/handshakes` renders `Handshake Sources`, separating setup owner-mandate mirrors, player promises committed by `Make Promise`, and ordinary owner demands, and now shows a route-local `Promise Receipt` after the existing promise action resolves. `selectOwnerMandates` is memoized by game identity for stable missing-mandate fallbacks.
- The prior Conditional-pick explanation slice remains shipped and is now superseded by direct proposal support. `/trades` renders `Conditional Pick Context`, labeling saved `game.conditionalPicks`, user-team incoming/outgoing/unresolved counts, generated-market/direct-proposal conditional-pick support, offseason resolution timing, and Inbox/Conditional Victory receipt surfaces. `/trades` also renders Scenario Lock copy for saved `blockTrades` and disables only trade-committing buttons.
- The prior Player Comparison context slice remains shipped. `/compare` renders `Comparison Context`, labeling current contracted `game.players`, active salary-cap contract grading, route-local picker state, Player-A position-stat lens, and the no-write boundary for stats, contracts, profile history, timelines, archives, awards, records, news, and social posts.
- The prior Comp-pick explanation plus active-rule display slices remain shipped. `/draft` marks saved `isCompPick` rows and renders `Comp Pick Context`, explaining FA Round 3 `LOSE_FA` minus `SIGN_FA` inputs, showing active `comp_pick_limit`, normal draft-order integration, and shared league-news/Inbox/achievement consumers. No second ledger, save schema, award math, draft order, movement, sim math, or RNG changed.
- The prior Scenario blocked-action UX copy slice remains shipped. `/scenarios` active state now renders a `Blocked Action Guide` from enforced `getScenarioConstraintCoverage` items, labels blocked trade/acquisition/draft-pick attempts as uncommitted, explains relevant roster/pick/cap/bid/claim/practice-squad/free-agent/draft-board state remains unchanged, and gives allowed next moves. Scenario definitions, blocker enforcement paths, scenario start behavior, save schema, movement, sim math, and RNG were unchanged.
- The prior Scenario Planning-Lanes Clarity slice remains shipped. `/scenarios` now renders `Still open:` lanes from `getScenarioConstraintCoverage.allowedPlanningPaths` so active locks distinguish blocked commits from safe read-only planning routes. Scenario definitions, blocker enforcement paths, scenario start behavior, save schema, movement, sim math, and RNG were unchanged.
- The prior Player Timeline source clarity slice remains shipped, and the route now includes draft-recap memory. `/player/$playerId/timeline` renders a `Timeline Sources` panel that labels `getPlayerCareerTimeline`, `playerSeasonHistory`, current `game.players`, `playerArchive`, awards/records highlights, saved transaction rows, saved `draftRecaps`, and the display-only boundary. Opening the route does not write seasons, awards, records, milestones, transaction logs, draft recaps, player archives, or profile history.
- The prior League Pulse source clarity slice remains shipped. `/league-pulse` renders a `Pulse Sources` panel that labels saved `leagueRivalries`, saved `game.powerRankings`, `buildLeaguePulse` read-model ownership, heat-spike social posts as explicit-caller-only, route-local display caps, and the display-only boundary. Opening the route does not write rivalries, rankings, news, social posts, storyline threads, or history rows.
- The prior Power Rankings source clarity slice remains shipped. `/power-rankings` renders a `Ranking Sources` panel that labels saved `game.powerRankings`, the `updatePowerRankings` write owner, media-cycle scoring/blurbs, saved `previousRank`/`delta` movement fields, route-local presentation, and the display-only boundary. Opening the route does not write rankings, news, social posts, storyline threads, records, or media-cycle history.
- The prior Stat Central source clarity slice remains shipped. `/stat-central` renders a `Stat Sources` panel that labels current-season rows from live `game.players`, historical rows from `playerSeasonHistory`, retired/name/position fallback context from `playerArchive`, local route state for tabs/filters/comparison slots, and the display-only boundary. Opening the route does not write stats, records, news, social posts, history rows, or archive entries.
- The prior RecordBook source clarity slice remains shipped. `/records` renders a `Record Sources` panel that labels saved `game.records`, saved `activeRecordChases`, saved `recentBrokenRecords`, saved `recentMilestones`, local route filters/selection, and the no-render-time media boundary. Opening the route does not detect or write new records, recalculate chase receipts, add league news/social/storylines, or create ceremonies.
- The prior facility/medical source clarity slice remains shipped. `/settings` renders an `Operations Source` panel that labels saved `team.facilityState`, saved `facility.effect`, existing `upgradeFacility`, saved `team.medicalStaff`, game-level `availableMedicalStaff`, the offseason-only `hireMedicalStaff` gate, and shared `buildTeamOpsImpactReceipt` ownership. Rendering does not refresh medical candidates, process injury recovery, run progression, or resolve training camp.
- The prior scenario launch coverage badges slice remains shipped. `NewGameScreen` scenario cards reuse `getScenarioConstraintCoverage` through `ScenarioLaunchCoverageBadges`, showing `Open rules` for unrestricted scenarios and enforced blocker badges such as `Trade Actions enforced` / `Offseason Free Agency enforced` before a save is started. Start Challenge behavior, setup-state deletion, scenario definitions, save schema, and enforcement paths were unchanged.
- The prior press podium receipt clarity slice remains shipped. `PressConferenceModal` labels podium answers as saved `postGameUi.pressConferenceQueue` receipts and tone choices only; `respondToPressConference` is tested to write only `selectedTier`/`selectedResponse`, with no new press conferences, active effects, event logs, league news, social posts, or owner/player changes.
- The prior mentor-route receipt polish slice remains shipped. `/mentors` labels saved `activeMentors`/`mentorBudget`, the shared Team Ops receipt, `calculateMentorEffects`, and Hire/Release store actions; rendering does not run progression, update normal mentoring pairs, alter CPU teams, or rewrite training-camp receipts.
- The prior team-needs/free-agency guidance clarity slice plus team-needs scenario planning notice remain shipped. `/team-needs` labels saved `teamNeedsCache` versus derived reports, planning-only draft/FA target lanes, and planning-only Scenario Lock copy when saved draft, free-agency, or trade blockers are active; `/fa-targets` labels saved `faTargetBoard` snapshots, empty-board/Refresh Board rebuild behavior, watchlist-only toggles, `/free-agency` as the real bid/signing owner, per-target `FA Market Receipt` rows, and planning-only Scenario Lock copy when saved `blockFreeAgency` is active. No formulas, cache invalidation, watchlist persistence shape, bids, signings, roster movement, save schema, RNG, or sim math changed.
- The prior app-shell route-context guardrails remain shipped. `apps/web/src/app/currentAppRoute.ts` normalizes App/Chip route context for hash-history URLs, strips the `/MFD/` deploy base when no hash exists, preserves browser-history-style app paths, and leaves route rendering/active shell state owned by TanStack Router. `useChipEvents`, `ChipDock`, and `ErrorBoundary` fallback reads now reuse that helper.
- The prior weather route source clarity slice remains shipped. `/league/weather` labels saved `schedule.weather`, the persisted `dome`/`clear`/`rain`/`snow`/`wind` key space, route-local UI forecast profiles such as Heat Wave/Sunny/temperature/wind MPH/impact labels, and the no-render-mutation boundary.
- The prior media feed-source clarity slice remains shipped. `/newsroom` labels saved league news, saved route power rankings, and active storyline threads as sources; active storyline cards render Thread Receipt rows from saved thread source/timing/beats/lifecycle/no-write boundary, and `HeadlineModal` renders Story Receipt rows from the selected saved `game.leagueNews` row. `/news` labels saved `leagueNews`, local filters, and separate ticker/Breaking News surfaces; `/social` labels saved `socialFeed`, newest-first projection, source-only local filters, and no render-time post generation. The app-shell full-screen `BreakingNews` overlay also passes through saved queue item source and labels queue/dismiss boundaries.
- The prior HOF/archive clarity, snapshot-freshness, manual sync, sidecar JSON export/import, epilogue-detail, ceremony, and league-news epilogue slices remain shipped. `/franchise/hall` labels the browser sidecar, live save count, current snapshot count/last sync, sync boundary, and sidecar-only Archive JSON boundary; the detail modal displays existing valid epilogues; HOF ceremonies/news prefer existing epilogue headline/story. No cartridge change, sidecar timing change, sim math, RNG, or movement math changed.
- The prior scenario practice-squad acquisition gate remains shipped. `getScenarioConstraintCoverage`, `/scenarios`, and `NewGameScreen` launch cards list direct proposals/counters, accepted user deadline offers, draft-war-room trades, user draft-pick submissions, waiver claims, and practice-squad acquisitions as enforced where applicable. `blockTrades`, `blockDraft`, and `blockFreeAgency` gate the inspected store/engine paths without save schema, sim math, RNG, or movement math changes.
- The prior waiver/practice-squad clarity slice remains shipped. `selectWaiverWireBoard` exposes no-contract labels, pending/open claim state, week-advance clearance copy, action eligibility, and lifecycle notes; `WaiverWire.tsx` disables duplicate pending-claim actions. `selectPracticeSquadRows` joins saved squad entries through `game.players`, and `selectPracticeSquadCandidates` separates addable free agents from waiver-blocked or pending rows. No movement rules, save schema, RNG, or sim formulas changed.
- Previous endorsement source-of-truth consistency and schema tightening remain shipped. `PlayerSchema.endorsements` and top-level `endorsementOffers` now use typed endorsement schemas with compatibility sanitization; remaining roster-level tightening would require replacing the passthrough team roster save boundary deliberately.
- Best next documented slice: full 36-step release-gate refresh only when release-level proof is warranted, remote/protected CI follow-through, roster-minimum balance follow-up, or a new high-leverage product slice; do not redo shipped G3/G4/G5/G6/G7 proof paths unless a source change invalidates them.

Required first reads, in order:
1. `AGENTS.md`
2. `STATUS.md`
3. `CODEX_GAME_GUIDE.md`
4. `CODEX_IMPROVEMENT_PLAN.md`
5. `CODEX_GOAT_MARATHON_PROMPT.md`
6. `CODEX_FINISH_GAME_MARATHON_PROMPT.md`
7. `DESIGN.md`
8. `README.md`

Kickoff source checks before editing:
- Run `pwd` and confirm the repo root.
- Check whether `.git` exists before using git status or diff as truth.
- Confirm `SAVE_VERSION` in `packages/engine/src/config/difficulty.ts`.
- Confirm `packageManager` in root `package.json`.
- Re-read `STATUS.md` and treat it as the latest run ledger, then verify any important claim from source.
- Read the guide section for the subsystem you are about to touch before opening implementation files.
- Inspect existing tests before deciding whether to add, adjust, or extend coverage.

The marathon loop:
1. Build a candidate scoreboard with 4-7 slices from the current plan.
2. Score each candidate for user impact, correctness value, save/sim risk, implementation size, testability, rollback simplicity, and doc-drift risk.
3. Pick exactly one first slice and state why it beats the alternatives.
4. Write a slice contract before editing: source owner, read paths, commit/mutation path, state touched, tests to run, docs to update, rollback path, and risk class.
5. Implement the smallest complete vertical slice.
6. Add or update focused tests.
7. Run the narrowest relevant verification immediately.
8. Update `CODEX_GAME_GUIDE.md`, `CODEX_IMPROVEMENT_PLAN.md`, `STATUS.md`, and `CODEX_GOAT_MARATHON_PROMPT.md` or this file if the prompt/backlog changed.
9. Run targeted stale-doc/source `rg` checks for the changed concept.
10. Record a per-slice ledger in `STATUS.md`: what shipped, files changed, checks run, checks not run, residual risk, rollback, and best next independent slice.
11. If clean, rescore or pick the next independent slice and keep going.
12. Stop only for a real blocker, a product decision that source cannot answer, a high-risk save/sim migration without enough runway, or a verification failure you cannot resolve.

Candidate scoreboard template:
| Slice | Why now | User impact 1-5 | Correctness value 1-5 | Save/sim risk 1-5 | Size S/M/L | Test path | Docs touched | Rollback | Pick? |
| --- | --- | ---: | ---: | ---: | --- | --- | --- | --- | --- |

Preferred first slice: another independent read-only receipt/source slice, tightly scoped player/team-memory clarity, carefully bounded UI source-clarity, or route/command guardrails only for a newly found uncovered drift point
- Goal: improve source-backed read-model clarity without adding render-time mutations, save schema, sidecar writes, or route-shell churn.
- Read first: relevant `CODEX_GAME_GUIDE.md` route/source notes and current component tests.
- Safe deliverable: read-only copy/read-model clarity with focused component/store tests.

Strong follow-up slice queue after scenario coverage:
1. Hall of Fame archive clarity - shipped:
   - `/legacy` labels live save HOF entries; `/franchise/hall` labels the browser-local sidecar plus live count versus snapshot count/last sync; the detail modal displays existing valid epilogues; HOF ceremonies/news prefer existing epilogue headline/story.
   - Durable epilogue persistence, ballot persistence/defaulting, manual current-save sync, standalone sidecar JSON export/import, and complete-dynasty sidecar archive portability are shipped; remaining HOF/archive work is richer workflow/display only if it adds value.

2. Weather route polish - shipped:
   - Weather clarity is shipped for Inbox, Game Day, and `/league/weather`. The route now distinguishes saved `schedule.weather` conditions from UI-only `HEAT_WAVE`/Sunny/temperature/wind-profile/impact-label heuristics.
   - Keep saved weather keys to `dome|clear|rain|snow|wind` unless doing full type/schema/migration work.
   - No sim formula, weather generation, route registration, or save-schema changes were made.

4. Deeper player-memory context:
   - Profile depth, transaction memory, source clarity, and signature moments are shipped. Future work should improve timeline/comparison/bloodline/rivalry surfaces only when it adds new value from existing durable data first.
   - Cautions: player rivalries are sim-affecting; `selectPlayerCareerComparison` sorts input in place; adding durable history needs type/schema/migration/default/old-save tests.

5. Scenario constraint integrity:
   - Coverage copy plus direct proposal/counter, deadline offer, draft-war-room trade, user draft-pick, free-agency market/street-signing, waiver-claim, and practice-squad acquisition gating are shipped.
   - NewGameScreen launch coverage badges are shipped.
   - Active `/scenarios` blocked-action UX copy is shipped through `Blocked Action Guide`.
   - Active `/scenarios` planning-lane clarity is shipped through `allowedPlanningPaths` and `Still open:` rows.
   - Monday Briefing `Command Queue` action-point scenario notices are shipped through `ActionCenter`.
   - Route-level Scenario Lock copy is shipped on `/trades`, `/draft`, `/trade-deadline`, `/free-agency`, `/waivers`, and `/practice-squad`; planning-only Scenario Lock copy is shipped on `/team-needs`, `/trade-block`, and `/fa-targets`; remaining scenario work should target actual per-action blocking affordances only if the product needs them.

6. Comp-pick explanation - shipped:
   - `/draft` now marks saved `isCompPick` picks and renders `Comp Pick Context`.
   - It explains FA Round 3 transaction-log inputs, active `comp_pick_limit`, normal draft-order integration, and league-news/Inbox/achievement consumers.

7. Player Comparison context - shipped:
   - `/compare` now labels current `game.players`, active cap grading, route-local picker state, Player-A stat lens, and display-only boundaries.

6. Governance persistence:
   - Extend CBA vote history or threshold rules only as a full governance persistence slice.
   - Requires type, Zod schema, migration/default/fixture/import tests when durable state changes, plus focused CBA UI and store tests.
   - Commissioner-ruling schema compatibility is shipped; future ruling work should add a real issue-ruling path or discipline lifecycle.
   - Do not take this as a quick slice unless you can finish the full persistence path.

7. Hall of Fame archive clarity - shipped:
   - `/legacy` labels live save HOF entries; `/franchise/hall` labels the browser-local sidecar plus live count versus snapshot count/last sync; the detail modal displays existing valid epilogues; HOF ceremonies/news prefer existing epilogue headline/story.
   - Engine `HallOfFameEntrySchema` preserves typed `epilogue`, `HallOfFameBallotEntrySchema` persists ballot waitlists, and standalone HOF sidecar JSON plus complete-dynasty sidecar archive export/import are shipped; remaining HOF archive work should target richer workflow/display.

8. Newsroom/media receipts - source clarity shipped:
   - `/newsroom`, `/news`, and `/social` now label saved feed sources, local projections/filters, `breakingNewsQueue`, and ticker boundaries.
   - `HeadlineModal` now renders a read-only `Story Receipt` from the selected saved `game.leagueNews` item, `NewsItemSchema` id, story clock, linked ids, writer ownership, and no-write modal boundary.
   - `GameDayRecap` now labels saved `GameDayPackage.pressConference` as a read-only `Podium Receipt`, and `PressConferenceModal` labels podium answers as saved queue receipts/tone choices only. Remaining work should add a genuinely new saved media receipt or route-level explanation while keeping event envelopes, saved `eventLog`, route feeds, breaking-news queue, audio cues, and Chip events separate.
   - Do not mutate saved feeds from render paths.

9. Route/app-shell guardrails - route-context, player-link hash routing, and app lifecycle guards shipped:
   - `currentAppRoute.ts` now normalizes App/Chip route context for hash-history deploy URLs and strips `/MFD/` when no hash exists; `useChipEvents`, `ChipDock` fallback route handling, and `ErrorBoundary` fallback context reuse it.
   - `PixelPlayerLink` now hash-routes reusable player-card navigation to `/player/:id`, producing `/MFD/#/player/:id` under the deploy base.
   - `App.test.tsx` now locks `RootLayout` shortcut/route-shortcut ownership, shell overlays, audio queues, save reminders, and sidecar sync calls; `useKeyboard.test.ts` locks document listener cleanup.
   - `App.test.tsx` now guards production template route targets from router navigation, hash helpers, and literal `location.hash` assignments, matching `${...}` template segments only against registered dynamic route segments such as `$playerId`.
   - `App.test.tsx` now guards engine AGM recommendation `targetRoute` literals from `packages/engine/src/systems/agm.ts` so Monday Briefing's variable recommendation CTAs stay registered web routes.
   - `App.test.tsx` now guards Monday Briefing `ActionCenter` `route` values and Week Advance `fixRoute` values so command-deck variable CTAs stay registered web routes.
   - `App.test.tsx` now guards `selectOffseasonCalendar` route values from `selectors.ts` so `/week-advance` offseason calendar CTAs stay registered web routes.
   - `App.test.tsx` now guards Inbox message `link` values and `InboxTriage` selected-route fallbacks so Inbox variable CTAs stay registered web routes.
   - `App.test.tsx` now guards shared `EmptyState` JSX `actionRoute` props so empty-screen variable CTAs stay registered web routes.
   - Further route/app-shell work should strengthen source tests only where they prevent newly found route/nav/command/tutoring/lifecycle drift.
   - Bigger route work usually touches `App.tsx`, `NAV_ITEMS`, `NAV_GROUPS`, route registration, mobile nav, command palette, route coaching, tutorial coverage, and direct-route tests.

10. Franchise memory source labels - shipped:
   - `/franchise` now labels Homegrown as current roster plus `playerArchive` provenance without archive writes.
   - Continuity is labeled as browser-local `mfd.rosterContinuity.v1` starter snapshots rather than cartridge data.
   - RivalryHeatMap is labeled as authored team rivalry content plus runtime lookup, with W-L-T derived from saved completed schedule/playoff results.
   - Future durable all-time head-to-head rivalry history or portable sidecar export remains save/schema/sidecar lifecycle work.

11. Bloodlines source clarity - shipped:
   - `/legacy/bloodlines` now labels selector-derived active player and draft-prospect sources.
   - The route states it does not assign bloodlines or write family relationship edges.
   - Future durable bloodline/history changes remain save/schema/lifecycle work.

12. Coaching tree and relationship graph source clarity - shipped:
   - `/coaching/tree` now labels active league `team.staff` HC/OC/DC records, saved `game.coachingHistory`, `coach_retirement` event labels, `buildCoachingLegacy`, and mentor/disciple staff fields; it also renders `buildCoachingLegacy(...).notableProteges` as a read-only active-branch panel.
   - The route states it does not write staff moves, relationship edges, coaching history, or coach development.
   - `/coaching/relationships` now labels saved `game.relationships`, active staff/roster label resolution, route-local filters/layout, and the no-write boundary, and uses stable empty selector fallbacks for old/demo saves without relationship arrays.
   - Future live coach-development, poaching, or relationship-edge wiring remains engine/store/save lifecycle work.

13. Near-miss recap receipt surface - shipped:
   - `/season/recap` now renders saved `game.seasonNearMissReceipts` as `What-If Receipts` when present.
   - The route labels season-rollover generation, declined direct-trade inputs, passed-pick draft inputs, user free-agent bids that lose to signed CPU offers, and no render-time receipt generation or tracker mutation.
   - Future transient `EngineOutput.nearMissReceipts` surfacing remains a separate engine/store/UI slice.

14. Super Bowl storyline champion-casing - shipped:
   - `generateSuperBowlContext` now counts prior titles through a case-insensitive champion-finish helper.
   - Canonical lowercase `playoffFinish: 'champion'` rows now drive dynasty and first-title Super Bowl storylines.
   - No playoff simulation, bracket mutation, archive write, route rendering, save schema, sim math, or RNG changed.

15. Halftime modal copy alignment - shipped:
   - `HalftimeDecision.tsx` now labels the engine-backed `switch` `-1/+1` and `gamble` `+3/-2` team OVR deltas instead of stale percentage-efficiency copy.
   - `HalftimeDecision.test.tsx` guards the displayed copy and absence of old percentage text.
   - No sim formulas, save schema, RNG, movement, route registration, or week-advance commit path changed.

16. Draft-board source clarity - shipped:
   - `/draft` now renders `Draft Board Sources`, labeling saved `game.draftClass`, saved `offseasonState.scoutingState` intel/fallbacks, `selectWarRoomState` saved-versus-derived behavior, store action owners, route-local Offer Confidence, route-local Draft Market Receipt, and the non-durable reveal card boundary.
   - No prospect generation, pick mutation, war-room formulas, save schema, movement, sim math, or RNG changed.

17. Scouting-board source clarity - shipped:
   - `/scouting` now renders `Scouting Sources`, labeling saved `game.draftClass`, saved `offseasonState.scoutingState` intel, store/engine scouting action ownership, saved draft scouting watchlist versus browser-local `mfd.watchlist.v1` pins, authored scouting-report templates, and the no-render-mutation boundary.
   - No prospect generation, scouting formulas, scout pool refresh, workout spending, watchlist persistence shape, save schema, movement, sim math, or RNG changed.

18. Scouting Action Receipts - shipped:
   - `/scouting` now renders a route-local receipt after existing hire/fire/watchlist/film/combine/interview/pro-day/private-workout actions resolve, naming the action source, commit path, state touched, and pre-action context.
   - No saved receipt history, scouting formulas, scout pool refresh, prospect generation, workout-spend rule, save schema, movement, sim math, or RNG changed.

19. Commissioner Action Receipts - shipped:
   - `/commissioner` now renders route-local Rule Petition/Vote receipts after existing `petitionRuleChange` or `voteOnProposal` actions resolve, naming source, commit path, state touched, and pre-action rule context.
   - No durable vote history, second governance ledger, save schema, CBA vote behavior, rule-application behavior, sim math, RNG, movement, or route registration changed.

20. CBA Action Receipts - shipped:
   - `/cba` now renders route-local Vote/Advance/Lockout receipts after existing `voteOnCBA` or `advanceCBANegotiation` actions resolve, naming source, commit path, state touched, and pre-action bargaining context.
   - No durable vote history, second bargaining ledger, save schema, vote threshold changes, CBA term formulas, rule projection changes, labor math, sim math, RNG, movement, or route registration changed.

21. Franchise Action Receipts - shipped:
   - `/franchise` now renders route-local Stadium Upgrade/Naming Rights receipts after existing `upgradeStadium` or `acceptNamingRights` actions resolve, naming source, commit path, state touched, pre-action context, and no-save/no-sim/no-RNG boundaries.
   - No durable receipt history, save schema, stadium/naming formulas, relocation behavior, sim math, RNG, movement, or route registration changed.

22. Offseason Calendar Readout - shipped:
   - `/free-agency` now mirrors the existing shared `selectOffseasonCalendar` read model beside market actions.
   - `/week-advance` now adds `Offseason Command Snapshot` from the same calendar plus saved offseason, roster-contract, and cap fields.
   - It labels saved phase/offseason/CBA/expansion calendar status and the no-render-write boundary without adding a second calendar model or changing phase logic, bids, signings, player movement, cap totals, save schema, sim math, RNG, or commit paths.

21. Halftime receipt source note - shipped:
   - `GameDayRecap.tsx` now detects saved `Halftime hell:` summaries in `GameDayPackage.activeEffectSummaries` and renders a `HALFTIME DECISION RECEIPT` source note.
   - No package writer, weekly-summary writer, week-advance mutation, broadcast generation, save schema, sim math, or RNG changed.

22. Cap rule-awareness regression guard - shipped:
   - `architecture-boundaries.test.ts` now blocks production year-only `getSalaryCap`/`getCapFloor` calls outside seed-time roster generation, protecting active `salary_cap_growth` and `cap_floor_pct` display context.
   - No cap formulas, save schema, sim math, movement, or mutation path changed.

20. Structured halftime/broadcast receipt hook - shipped:
   - `buildHalftimeDecisionReceipt` parses saved `Halftime hell:` rows from `GameDayPackage.activeEffectSummaries`.
   - `GameDayRecap`, `GameBroadcast`, `PlayByPlay`, and `BroadcastPresentation` consume that read model through the matching saved game-day package.
   - No package writer, weekly-summary writer, week-advance mutation, broadcast generation, save schema, sim math, RNG, route registration, or movement path changed.

21. Custom trade-deadline recognition - shipped:
   - `game-store.ts` now uses the active `trade_deadline_week` for post-engine interruption routing after normal `advanceWeek` and `resolveHalftimeDecision`.
   - `/trade-deadline` now renders Scenario Lock copy from saved `blockTrades`, disables deadline Accept buttons, and leaves Reject, clock advance, and finalization available.
   - No trade generation, deadline clock, save schema, sim math, RNG, route registration, movement, or package writer path changed.

22. Private-workout/pro-day explanation - shipped:
   - `/scouting` prospect cards now explain saved `proDayRating`, confidence/visible-grade tightening, no workout-slot spend, private-workout slot consumption, saved `privateWorkoutRatings`, top-rating verification, and risk/ceiling-band unlock behavior.
   - No scouting action, prospect generation, workout spending, save schema, movement, sim math, RNG, or route registration changed.

12. Team-needs/free-agency guidance clarity - shipped:
   - `/team-needs` now labels saved-cache-versus-derived selector ownership, planning-only draft/FA target lanes, and planning-only Scenario Lock copy for saved draft/free-agency/trade blockers.
   - `/team-needs` now labels saved CPU `Strategy History` rows from `game.eventLog` and `game.leagueNews` without rerunning AI helpers or writing state.
   - `/fa-targets` now labels saved target-board snapshot ownership, empty-board/Refresh Board rebuild behavior, watchlist-only toggles, `/free-agency` as the real bid/signing owner, and planning-only Scenario Lock copy when saved `blockFreeAgency` is active.
   - `/free-agency` now labels scenario-locked external acquisition attempts, disabling open-market bids and street signings while leaving re-sign negotiation and market advancement available.
   - `/free-agency` now labels saved bid-resolution results from `offseasonState.freeAgencyBids` in a read-only `Bid Resolution Summary`.
   - `/game-day` now labels saved latest `GameDayPackage` source inputs in a read-only `Postgame Source Receipt`.
   - Cache invalidation, target scoring, watchlist persistence redesign, bids, signings, or roster movement remain separate engine/store/save work.

13. Mentor-route receipt polish - shipped:
   - `/mentors` now labels saved `activeMentors`/`mentorBudget`, the shared Team Ops receipt, `calculateMentorEffects`, and Hire/Release store actions.
   - Rendering `/mentors` does not run progression, update normal mentoring pairs, alter CPU teams, or rewrite training-camp receipts.
   - Facility/medical source clarity is shipped in `/settings`; mentor math/progression changes remain engine/test work.

12. Press podium receipt clarity - shipped:
   - `PressConferenceModal` now labels podium answers as saved queue receipts/tone choices only.
   - `respondToPressConference` is tested to write only `selectedTier`/`selectedResponse`, without adding press conferences, active effects, event logs, league news, social posts, or owner/player changes.
   - Making podium answers gameplay-visible remains a separate engine/store contract.

13. Scenario launch coverage badges - shipped:
   - `NewGameScreen` scenario cards now show `Open rules` or enforced blocker badges from `getScenarioConstraintCoverage` before a save is started.
   - Start Challenge behavior, setup-state deletion, scenario definitions, save schema, and enforcement paths were unchanged.

14. Facility/medical source clarity - shipped:
   - `/settings` now labels saved `team.facilityState`, saved `facility.effect`, existing `upgradeFacility`, saved `team.medicalStaff`, game-level `availableMedicalStaff`, the offseason-only `hireMedicalStaff` gate, and shared `buildTeamOpsImpactReceipt` ownership.
   - No candidate-pool refresh, injury recovery, progression, training-camp resolution, sim math, save schema, or store mutation path changed.

15. RecordBook source clarity - shipped:
   - `/records` now labels saved `game.records`, saved `activeRecordChases`, saved `recentBrokenRecords`, saved `recentMilestones`, local route filters/selection, and the no-render-time media boundary.
   - No record detection/write, chase recalculation, league-news/social/storyline generation, ceremony generation, save schema, sim math, RNG, or store mutation path changed.

16. Stat Central source clarity - shipped:
   - `/stat-central` now labels current-season rows from live `game.players`, historical rows from `playerSeasonHistory`, retired-player fallback context from `playerArchive`, local route state for tabs/filters/comparison slots, and the display-only boundary.
   - No stat/history/archive writes, leaderboard formula changes, record detection/write, league-news/social/storyline generation, save schema, sim math, RNG, or store mutation path changed.

17. Power Rankings source clarity - shipped:
   - `/power-rankings` now labels saved `game.powerRankings`, `updatePowerRankings` ownership, media-cycle scoring/blurbs, saved `previousRank`/`delta`, route-local presentation, and the display-only boundary.
   - No ranking writes, media-cycle history writes, formula changes, record detection/write, league-news/social/storyline generation, save schema, sim math, RNG, or store mutation path changed.

18. League Pulse source clarity - shipped:
   - `/league-pulse` now labels saved `leagueRivalries`, saved `game.powerRankings`, `buildLeaguePulse` ownership, explicit-caller-only heat-spike posts, route-local display caps, and the display-only boundary.
   - No rivalry writes, ranking writes, heat-spike social posts, formula changes, record detection/write, league-news/social/storyline generation, save schema, sim math, RNG, or store mutation path changed.

19. Player Timeline source clarity - shipped:
   - `/player/$playerId/timeline` now labels `getPlayerCareerTimeline`, `playerSeasonHistory`, current `game.players`, `playerArchive`, awards/records highlights, and the display-only boundary.
   - No timeline generation changes, season writes, awards, records, milestones, player-archive mutation, profile-history mutation, save schema, sim math, RNG, or store mutation path changed.

20. Scenario blocked-action UX copy - shipped:
   - `/scenarios` active state renders a `Blocked Action Guide` from enforced `getScenarioConstraintCoverage` items.
   - Trade, acquisition, and draft-pick blockers are labeled as uncommitted attempts with safe next moves.
   - No scenario definitions, enforcement gates, save schema, movement, sim math, RNG, or store mutation path changed.

21. Comp-pick explanation - shipped:
   - `/draft` now marks saved `isCompPick` rows and explains why compensatory picks appear.
   - No award math, draft order, second ledger, save schema, movement, sim math, RNG, or store mutation path changed.

22. Player Comparison context - shipped:
   - `/compare` now labels comparison source/context and no-write boundaries.
   - No comparison math, player stat/contract writes, save schema, sim math, RNG, news, or social writes changed.

20. Endorsement persistence follow-up:
   - Only do this if you deliberately choose save-schema work.
   - Add an `EndorsementDealSchema`, decide malformed old-save policy, update migration/defaults/import handling, add old-save tests, and verify cartridge round trips.
   - Do not rework accepted-deal visibility; that slice is already shipped.

Priority ladder:
- P0: correctness, save compatibility, deterministic sim integrity, broken routes, impossible decisions, misleading UI claims.
- P1: high-frequency playable-loop clarity: waivers, cap, trades, draft, free agency, offseason, post-week, team ops.
- P2: dynasty depth: achievements, player identity, rivalries, HOF/archive clarity, news/media, coaching/owner/AI identity.
- P3: tooling/refactors/polish: route extraction, static guards, visual checks, content volume, architecture cleanup.

Risk rules:
- Save/data change: must include TypeScript type, Zod schema, migration, deterministic new-save default, old-save/fixture/import tests, and docs.
- Sim/RNG change: no `Math.random()`; use existing RNG channels; include before/after formula notes, sample outputs, deterministic tests, and playtest/smoke reasoning.
- Store mutation: clone source state, call engine/store helper, keep roster/player/team/free-agent/cap indexes synchronized, commit through existing path, test JSON-loaded save shapes when reference identity matters.
- UI clarity: source copy from existing state/helpers. Never imply a mutation, formula, guarantee, or lifecycle that is not actually wired.
- Browser sidecar: keep it outside `GameState`; document key ownership, lifecycle, export/import behavior, and cleanup/reset expectations.
- Route shell: update route registration, nav, mobile, command palette, route coaching/tutorial decisions, source tests, and direct-route smoke as needed.

Verification matrix:
- Docs-only: targeted `rg`/`sed` review is enough; say no package tests were needed.
- Engine system: focused Vitest first; broaden to `pnpm --filter @mfd/engine test` when shared behavior changes.
- Web store/selectors: focused `apps/web/src/app/store` tests plus affected feature tests.
- UI route: focused component tests and browser verification when layout/interaction changed and a local target is feasible.
- Save/data: save migration/golden/import tests plus any web persistence tests.
- Sim math/RNG: deterministic same-seed tests, `bash scripts/check-math-random.sh`, and `pnpm test:perft` or a clear reason if not run.
- Broad frontend: package typecheck/build or focused package equivalents; for deploy-equivalent checks use Chip-enabled build and built-page smoke where relevant.
- If `pnpm` is unavailable, try package-local binaries only after documenting the tool issue. Report the exact command used and the signal it provides.

Documentation requirements after every slice:
- Update `CODEX_GAME_GUIDE.md` for source ownership, route wiring, save schema, week-advance behavior, store patterns, content loaders, sidecar ownership, or source boundary changes.
- Update `CODEX_IMPROVEMENT_PLAN.md` for completed, rejected, narrowed, or newly discovered slices.
- Update `STATUS.md` with a clear run ledger.
- Update `CODEX_GOAT_MARATHON_PROMPT.md` and/or `CODEX_FINISH_GAME_MARATHON_PROMPT.md` when the next prompt should change because the backlog, current facts, best next slice, or safety cautions changed.

Definition of done for this marathon:
- Minimum: one complete vertical slice shipped with focused tests, docs, verification, residual risk, rollback, and next-slice recommendation.
- Strong: two or more independent slices shipped cleanly, with no carried verification failures or stale docs.
- Excellent: a run that makes the game more understandable and playable immediately while reducing future implementation risk.
- Never call the whole game "finished" unless source, tests, routes, save behavior, and user-facing workflows support that claim. Instead, keep shipping the next highest-leverage verified slice.

Required final response shape:
1. Understanding
2. Plan
3. Patch
4. Verification
5. Risks / Rollback

Final response must include:
- What shipped.
- Files changed.
- Tests/checks run and exact results.
- Tests/checks not run and why.
- Residual risks.
- Rollback guidance.
- Best next independent slice.

Keep moving. Finish the first slice, prove it, document it, then continue.
```

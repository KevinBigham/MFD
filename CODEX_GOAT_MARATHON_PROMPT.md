# GOAT Marathon `/goal` Prompt For The Next Codex Window

Latest finish-game prompt: use the compact paste block in `CODEX_FINISH_GAME_MARATHON_PROMPT.md` when the next run should be an implementation marathon. It starts from the shipped Game Day Player Arc Follow-Up slice, stays under the `/goal` limit, and keeps the expanded section as reference material.

Latest shipped slice note: Game Day Player Arc Follow-Up makes `/game-day` render a read-only panel from saved `GameDayPackage.topPerformers`, `recordsMoments`, and `milestoneMoments`. It de-duplicates player ids, links saved player rows to profiles, preserves record/milestone narratives and top-performer stat lines, and routes only to existing Player Development or Record Book screens. Prior Draft Recap Class Follow-Through, Week Advance Rookie Class Follow-Up, Draft Recap Player Profile Links, Player Timeline/Profile Draft Recap Memory, Draft Post-Draft Recap CTA, Draft War-Room Durable Accepted-Trade Receipt, Browser Accepted Offer Proof, Real-Backed Generation, and Live Order Ownership remain shipped. This latest slice does not add a new `GameState` field, save schema, migration, `SAVE_VERSION`, game-day package writer, player-history/profile/timeline writer, record/milestone writer, scrapbook/franchise-book writer, roster/contract/depth mutation, week-advance behavior, sim math, RNG channel, route registration, dependency, or sidecar. Prior GOAT Release Sentinel Benchmark remains shipped: `node scripts/release-gate.mjs --only goat-release-sentinel` passed in 11.5s with 4/4 seasons, 113 weeks, 56 medium roster-minimum anomalies, 0 high, and every named budget PASS. Latest full G7 proof remains the prior 35/35 run; the current 36-step plan has focused sentinel proof but no full 36/36 rerun yet.

Paste the full text block below into a fresh Codex chat when you want an implementation marathon on Mr. Football Dynasty. This is the canonical fresh-window handoff: it turns `CODEX_IMPROVEMENT_PLAN.md` into a concrete execution contract that forces source re-confirmation, candidate scoring, implementation, verification, documentation sync, and continued progress while the run remains clean.

```text
/goal You are continuing work on Mr. Football Dynasty in `/Users/tkevinbigham/MFD/MFD-main`.

Mission: make concrete, verified progress toward turning this browser football dynasty sim into the GOAT football dynasty game. This is an implementation marathon, not a planning-only run. Read the docs, inspect the live source, choose the highest-leverage safe vertical slice, ship it with tests/docs, then immediately continue into additional independent slices while the run remains clean.

Success target:
- Minimum win: one complete, source-backed vertical slice shipped with focused tests, relevant doc updates, verification results, residual-risk notes, rollback guidance, and a next-slice recommendation.
- Strong run: two or more independent slices shipped cleanly without carrying failing tests, stale docs, save/schema uncertainty, or unresolved source contradictions from one slice into the next.
- GOAT run: improve something users can actually feel in the playable dynasty loop while also making the codebase safer for future work.

Core operating mode:
- Be precise, cautious, implementation-focused, and test-driven.
- Do not stop at analysis when a safe implementation path exists.
- Treat docs as maps and source/tests as truth. If they disagree, update the docs after confirming the source.
- Preserve deterministic simulation, save compatibility, route integrity, and existing architecture boundaries.
- Prefer wiring existing engine systems into clearer user-facing workflows before inventing new systems.
- Do not invent product requirements. If source reality is ambiguous, make the smallest safe assumption, state it, and proceed only when the user-facing or save/data risk is low.
- Use small reversible slices. If a slice gets risky, narrow it instead of forcing a broad rewrite.
- After each clean slice, keep going if there is still budget and the next slice is independent.
- Keep a visible checklist. Update statuses as you complete audit, implementation, tests, docs, and next-slice selection.
- Bias toward playable progress users can feel: clearer decisions, stronger receipts, richer dynasty memory, safer route/store behavior, better first-run guidance, or one fully wired flavor/content slice.

Required first reads, in order:
1. `AGENTS.md`
2. `CODEX_GAME_GUIDE.md`
3. `CODEX_IMPROVEMENT_PLAN.md`
4. `CODEX_GOAT_MARATHON_PROMPT.md`
5. `STATUS.md`
6. `DESIGN.md`
7. `README.md`

Kickoff audit before editing:
- Confirm `pwd` is `/Users/tkevinbigham/MFD/MFD-main`.
- Confirm whether `.git` exists before relying on git status/diff.
- Read `STATUS.md` for any current branch/session notes, but re-confirm important facts from source.
- Confirm `SAVE_VERSION` in `packages/engine/src/config/difficulty.ts`; it was documented as `36`. Watch for save-version drift across guide, plan, fixtures, launch copy, and this marathon prompt.
- Confirm SAVE_VERSION from packages/engine/src/config/difficulty.ts exactly when running the save-version drift guard.
- Confirm package manager from `package.json`; it was documented as `pnpm@9.15.9`.
- Confirm root script contracts before using broad gates: recursive `build`/`test`/`lint`/`typecheck`, `test:perft` as engine `playtest:all`, `test:shadow` for shadow baselines, and `test:grading` for the mocked release-grader test.
- Re-read the `CODEX_IMPROVEMENT_PLAN.md` area scorecard and candidate slices.
- Map the first slice to the relevant `CODEX_GAME_GUIDE.md` subsystem section before touching files.
- Inspect the source and existing tests for the selected slice.
- Classify the slice as one or more of: save/data, sim math, store action, UI route, content, tooling, docs, architecture.

First-hour sequence:
1. Read the required docs and the selected subsystem guide section.
2. Build the candidate scoreboard before editing anything.
3. Pick one first slice and state the source files/tests you will inspect.
4. Inspect those files, then either confirm the slice or narrow it.
5. Create a short checklist with audit, implementation, tests, docs, and handoff items.
6. Start editing only after the source/test boundary is clear.

Slice contract before editing:
- Name the source owner, read path, commit path, state touched, focused tests, docs touched, and rollback path for the selected slice.
- State whether the slice is read-only clarity, a real mutation path, a persistence/schema change, sim math, route shell work, content, tooling, or docs-only.
- For clarity/receipt slices, prove the UI copy is sourced from existing state/helpers and does not imply a mutation or formula that is not actually wired.
- For mutation slices, identify the engine/store helper that owns the change and the tests that prove roster/player/team/free-agent/cap/save state remains synchronized.
- If audit reveals a P0 drift or misleading doc that would steer implementation wrong, fix that first, record it, then rescore the candidate list.

Candidate scoreboard template:
| Slice | Why now | User impact 1-5 | Correctness risk 1-5 | Save/sim risk 1-5 | Test path | Docs touched | Rollback | Pick? |
| --- | --- | ---: | ---: | ---: | --- | --- | --- | --- |

Marathon cadence:
1. Build a candidate scoreboard with 3-5 slices from `CODEX_IMPROVEMENT_PLAN.md`.
2. Score each candidate on user impact, correctness risk, save/determinism risk, testability, doc drift risk, and rollback simplicity.
3. Pick exactly one first slice and state why it beats the alternatives.
4. Finish that slice end-to-end: source, tests, docs, verification, residual-risk note.
5. Write a slice run ledger before continuing: shipped files, tests run, tests not run, docs changed, residual risk, rollback path, and best next independent slice.
6. If clean, pick the next independent slice from the scoreboard or rescore with new findings.
7. Repeat until you hit a real blocker, a high-risk boundary that needs user direction, or the run budget is exhausted.

Priority ladder:
1. P0 correctness and safety: save compatibility, deterministic sim integrity, route breakage, browser/engine boundary violations, impossible decisions, misleading UI claims.
2. P1 playable-loop clarity: decision forecasts, receipts, cap/trade/draft/game-plan clarity, week-to-week next actions, roster/team-ops clarity.
3. P2 world depth: dynasty memory, rivalries, player identity, coaching identity, owner/AI personality, Chip guidance, content flavor, media/analytics context.
4. P3 tooling and architecture: static guards, deterministic playtest metrics, smoke/e2e coverage, route/store extraction, worker-ready sim boundaries.

Slice-selection rubric:
- Score 2-4 candidate slices on user impact, correctness risk, save/determinism risk, testability, doc drift risk, and rollback simplicity.
- Pick one first slice. Pick 2-4 small slices for the full run only if they are genuinely independent and easy to verify.
- If the best slice touches save schema, sim math, week advance, route shell, or broad store architecture, do one deep slice instead of several shallow ones.
- Default toward read-only forecast/receipt/UI wiring when no P0 issue is found.
- Prefer work users can feel immediately: clearer action consequences, stronger post-week guidance, safer route/store/content guardrails, or a fully wired living-world detail.
- Do not start a second slice while tests/docs for the current slice are still unresolved.
- Stop before broad persistence, sim-balance, or architecture work unless the full migration/test/rollback path is clear.

Go/no-go rules:
- Go when the slice has a clear source owner, an existing route/store/engine boundary, a focused test path, and a rollback that is mostly file-level.
- Narrow when the slice crosses save schema, week advance, sim balance, imported content schemas, route shell registration, or browser sidecars.
- Stop for user direction when the correct behavior is a product decision, when existing docs and source conflict in a way source inspection cannot resolve, or when verification would require a missing external service.
- Do not call a docs-only audit a shipped implementation slice unless it corrects real drift that would mislead future work.

Suggested opening scoreboard seeds:
- Waiver/practice-squad clarity and route-local action receipts are shipped: claim intent, pending result, clear-to-free-agency, add-to-practice-squad states, scenario locks, waiver claim receipts, and practice-squad Add/Elevate/Release receipts are covered without changing movement rules.
- Achievement or player-memory clarity: improve a high-emotion route using existing durable data and no new save fields; do not redo shipped Dynasty Chronicle HOF epilogue context, Player Profile `Career Memory`, `Transaction Memory`, `Signature Moments`, or profile source panels.
- Team-ops/coaching receipt expansion - shipped pieces: Settings, `/training-camp`, `/mentors`, and Monday Briefing use `buildTeamOpsImpactReceipt` for read-only operations receipts; `/settings` labels saved facility/medical sources plus upgrade/hire gates and now shows route-local Operations Action Receipts after existing facility/medical commits; `/coaching` shows a read-only `Coaching Decision Receipt`, a route-local `Clinic Run Receipt` after durable `addClinicXP` commits, and a route-local `Skill Activation Receipt` after durable `setHeadCoachSkillSelection` commits; `/roster` shows route-local Training Assignment Receipts after durable `assignTraining` commits plus Roster Action Receipts after selected-player cut/trade-block/restructure/IR commits; `/coaching/tree` shows notable proteges; `/locker-room` shows route-local meeting/rally receipts returned after saved locker-room commits; playoff champion rollover now runs season-end coach development plus deterministic OC/DC poaching through existing engine helpers; CPU head-coach carousel hires now mirror `team.coachingStaff.hc`. Future team-ops work should add new value before touching progression math.
- Draft/scouting clarity and live-order trust: comp-pick context with active `comp_pick_limit` value, `Draft Board Sources`, Big Board `Pick Forecast`, route-local `Draft Pick Receipt`, war-room `Offer Confidence`, per-offer `Draft Market Receipt`, active-scenario `Scenario Lock` copy, real-backed generated war-room offers, accepted live war-room order ownership, and browser proof for accepting a future-sweetener offer from `/draft` are shipped; Inbox comp-pick receipts also show the active limit; `Scouting Sources`, private-workout/pro-day impact explanations, and route-local Scouting Action Receipts are shipped on `/scouting`. Future draft/scouting work should add new value without inventing a second draft board or bypassing `makeDraftPick`, `advanceWeek`, `advanceDraft`, `acceptDraftTradeOffer`, or `rejectDraftTradeOffer`.
- Route/app-shell guardrail - route-context, companion/error fallback normalization, app lifecycle guards, autosave toast week-baseline guard, launch setup-mode boundary copy, recap direct-route ownership, `/season/recap` direct-route coaching, `/draft-recap` direct-route coaching, `/relocate` route coaching, broadcast-suite route coaching for `/broadcast` / `/presentation` / `/play-by-play` / `/game-flow`, film-room route coaching for `/film-room`, Super Bowl route coaching for `/super-bowl`, owner-promises route coaching for `/owner` / `/handshakes`, analytics-evidence route coaching for `/analytics` / `/stat-central`, player-profile route coaching for dynamic `/player/$playerId`, player-timeline route coaching for dynamic `/player/$playerId/timeline`, player-development route coaching for `/player-development`, player-comparison route coaching for `/compare`, player-rivalries route coaching for `/rivalries`, league-weather route coaching for `/league/weather`, scenario-constraints route coaching for `/scenarios`, locker-room route coaching for `/locker-room`, front-office route coaching for `/front-office`, endorsements route coaching for `/endorsements`, mentors route coaching for `/mentors`, training-camp route-coaching anchors for `/training-camp`, commissioner-governance route coaching for `/commissioner`, awards-hub route coaching for `/awards`, franchise-legends route coaching for `/legends`, game-day route coaching for `/game-day`, schedule route coaching for `/schedule`, watch-list route coaching for `/watch-list`, trade-block route coaching for `/trade-block`, market-planning route coaching for `/team-needs` / `/free-agency` / `/fa-targets`, roster-churn route coaching for `/waivers` / `/practice-squad`, direct-only route-coaching decision coverage, `PixelPlayerLink` hash-router navigation, TeamLogo resolver/dimension/fallback plus public-logo inventory coverage, and command-palette roster-player search are shipped: add or strengthen a source test only where it prevents a newly found route, nav, shell-effect, route-coaching, command-palette, asset, or deploy-base drift.
- Weather/media/context clarity: saved media source clarity is shipped across `/newsroom`, `/news`, `/social`, storyline thread receipts, podium receipts, and the app-shell `BreakingNews` overlay source/queue boundary. Continue only with a genuinely new saved weather, media, or analytics receipt with no formula or persistence change.

Strong source-backed candidate slices:
1. Decision clarity: extend the established forecast/receipt/source pattern from `buildContractDecisionForecast`, `buildTradeDecisionForecast`, `buildPrepDecisionForecast`, `buildFreeAgencyDecisionForecast`, `buildTeamOpsImpactReceipt`, and `buildPostWeekMoment` into another high-frequency workflow such as roster cuts, draft picks, training, facilities, waiver claims, or practice squad. `/free-agency` full forecast windows plus route-local action receipts, `/game-plan` weekly-prep source clarity, and `/film-room` receipt-source clarity plus route coaching are shipped, so do not redo them as display slices.
2. Weekly loop: reuse or extend `buildPostWeekMoment` in Monday briefing/action-center only if it adds real next-action clarity beyond the Week Advance panel.
3. Team operations: add route-local receipts or command-center clarity only where it goes beyond the shipped Settings source panels and operations action receipts, `/training-camp` readiness/carryover plus route-coaching anchors, `/mentors`, Monday Briefing source panels, Locker Room action receipts, Coaching Clinic Run Receipt, Coaching Skill Activation Receipt, and Roster selected-player action receipts. Do not change saved math unless focused engine tests demand it.
4. Route shell safety: continue route/nav/app-lifecycle hardening only where source tests reduce real future breakage around `App.tsx`, `NAV_ITEMS`, route registration, command routes, mobile nav, route coaching, or shell effects.
5. Deterministic tooling: add richer deterministic playtest metrics only if they are canonical, host-noise-free, and backed by direct same-seed comparison.
6. Chip and guidance: event-specific `gameComplete` / `seasonEnd` weekly guidance copy is shipped; future work should add route guidance only if route coaching, first-ten onboarding, legacy tutorial fallback, or pending-decision flows need clearer source-backed variants. Preserve the difference between Chip route beats, first-ten receipts, weekly dialogue, and engine-backed tutorial state.
7. Content and flavor: standalone ATL, BAL, BOS, CHI, CIN, CLE, DAL, DEN, DET, KC, NYC, PHI, PIT, SEA, and SF stadium-tradition slices are shipped with schema, loader, runtime consumer, cue consumption, tests, and precedence rules. Scale content volume only with the same guardrails and a real consumer.
8. Analytics and media: source clarity is shipped on `/newsroom`, `/news`, `/social`, app-shell `BreakingNewsTicker`, embedded `PowerRankingsTicker`, postgame podium responses, `/records`, `/stat-central`, `/analytics`, `/power-rankings`, `/league-pulse`, `/draft` comp-pick context and draft-board source panel, `/inbox` comp-pick active-limit receipt copy, `/compare` source context, `/trades` conditional-pick context, owner-pressure clarity, Monday Briefing scenario-lock notices, Monday Action Center's compact `buildPostWeekMoment` receipt, and the broader Week Advance post-week receipt deck. Media route coaching is split across `league-pulse`, `league-news`, `newsroom`, and `social-feed`, and analytics route coaching is split through `analytics-evidence` for `/analytics` and `/stat-central`. Continue with another saved-receipt explanation using derived selectors without render-time mutations. Record-chase and user record-watch schedule alignment is shipped: both use the public `getRegularSeasonWeekCount(game)` helper instead of fixed 17-game or `schedule_weeks - 1` assumptions.
9. Dynasty memory: current-save rivalry W-L-T, total-games, and latest-meeting derivation is shipped on `/franchise`; build durable all-time rivalry head-to-head history only after deciding whether existing archives are complete enough or a persistent field plus migration is required.
10. Governance and AI: improve owner pressure, staff identity, coordinator specialties, coaching clinic, GM reputation, or CPU intent only with focused tests around downstream cap/trade/draft/free-agency/weekly-prep effects.
11. App lifecycle - guardrails shipped: `App.test.tsx` locks `RootLayout` shortcut/route-shortcut ownership, shell overlays, audio queues, save reminders, year-rollover sidecar calls, and the autosave toast week-baseline cue; `appShellLifecycle.test.ts` locks `resolveAutosaveToastStep`; `useKeyboard.test.ts` locks shortcut semantics and document listener cleanup; `useChipEvents`, `ChipDock`, and `ErrorBoundary` route-context fallbacks now reuse `resolveCurrentAppRoute`. Continue only where source inspection finds a real uncovered lifecycle gap such as boot/session state, error fallback context, or a new overlay/sidecar bridge.
12. Engine guard coverage: when an important live engine module is weakly covered, add focused tests that lock current contracts before deeper feature work.
13. Franchise-tag and cap-rule display slices are shipped. `/contracts` now exposes active-rule tag context and calls `actions.applyFranchiseTag`; the store clones/commits through the engine helper, syncs roster/player-map references and cap totals, snapshots undo, autosaves, and labor pressure counts every `franchiseTags` entry with legacy fallback. Remaining cap/tag work should target seed-time salary scaling with new-save/balance coverage, team-tag save-schema strictness, richer receipts, or formula/balance changes only with full safety coverage.
13a. Waiver Claim Submission Receipts - shipped: `/waivers` now renders a route-local `Waiver Claim Receipt` after existing `actions.submitWaiverClaim -> submitWaiverClaimEngine -> commitGame` resolves, labeling claim intent, current priority/board contract context, delayed award/loss/clearance resolution, touched state, and no-extra-write boundaries without changing waiver processing, priority order, movement, contracts, cap sync, saves, sim, RNG, or route registration.
13b. Draft Pick Submission Receipts - shipped: `/draft` now renders a route-local `Draft Pick Receipt` after existing `actions.makeDraftPick -> makeDraftPickEngine -> applyDraftSelection -> commitGame` resolves, labeling selected prospect/pick context, pre-pick forecast, touched draft/roster/player/news/archive/press-conference/audio/autosave state, and no-extra-write boundaries without changing draft order, prospect generation, rookie creation, movement formulas, save schema, sim, RNG, or route registration.
13c. Practice Squad Action Receipts - shipped: `/practice-squad` now renders route-local receipts after existing Add, Elevate, or Release actions resolve, labeling slot/elevation context, touched practice-squad/free-agent/roster/player/transaction/autosave state, commit paths, and no-extra-write boundaries without changing practice-squad limits, scenario gates, movement behavior, contracts, save schema, sim, RNG, or route registration.
14. Comp-pick clarity slice - shipped: `/draft` marks saved `DraftPick.isCompPick` rows and renders a `Comp Pick Context` panel explaining Free Agency Round 3 `LOSE_FA` minus `SIGN_FA` inputs, the active `comp_pick_limit` value for the current year, normal draft-order integration, and shared league-news/Inbox/achievement surfaces. `/inbox` Comp Picks Awarded receipts now also show the active comp-pick limit value passed from `InboxTriage`. No new ledger, schema, award math, draft order, movement, sim math, or RNG changed.
15. Trade acquisition clarity - shipped: `/trades` renders `Conditional Pick Context` from saved `game.conditionalPicks`, counts user-team incoming/outgoing/unresolved rows, labels generated-market support/direct-proposal support, and shows the active `trade_deadline_week` value in the Trade Window metric detail. Direct proposals can include unresolved owned conditional picks: `getTradeableAssets` / `getTradeTargets` expose them, `trade-negotiation.ts` values/transfers them, and Trade Center keys/labels them as conditional-pick assets. `/trades` also renders Scenario Lock copy when saved `blockTrades` is active, disables generated-offer Accept, direct proposal Submit, and Accept Counter, and leaves Reject, Reject Counter, package building, and trade-block scanning available. The web store now returns before generated-offer accepts, direct proposal creation/submission, or counter accepts under `blockTrades`, so direct calls do not snapshot undo, cue audio, autosave, or commit. `/trades` renders route-local `Trade Action Receipt` panels after existing generated-offer accept/reject, direct proposal submit, and counter accept/reject actions resolve. Trade Finder acceptance labels use the same percent scale as the progress bar, and each visible saved Trade Finder suggestion now renders a read-only `Generated Offer Receipt` from partner, package, need, acceptance, and value-gap fields before the user loads it into the builder. `/trade-block` renders `Trade Block Source`, labeling `selectLeagueTradeBlock`, CPU `findTradeTargets` output, route-local filters/sort, `/trades` as the commit path, and the no-proposal/no-movement/no-route-write boundary; it also renders per-target `Market Receipt` copy from saved CPU intent plus seeker/valuation fields, renders planning-only Scenario Lock copy when saved `blockTrades` is active while keeping scanning available, and resolves to unanchored `trade-market-radar` route coaching. `/trade-deadline` renders read-only `Deadline Deal Receipt` copy for saved completed-deal rows while Finalize remains the only path that applies completed deals and moves players or picks.
16. Owner-pressure clarity, owner-promises route coaching, and Handshake Promise Receipt slices - shipped: `/owner` now labels saved mandates, mandate handshake mirrors, no-double-application behavior, saved week summaries, and Inbox/season-report receipt consumers; `/handshakes` separates setup owner-mandate mirrors, committed player promises, and ordinary owner demands, then shows a route-local `Promise Receipt` after the existing `actions.makePromise -> makePlayerPromise -> commitGame` promise action resolves; `/owner` and `/handshakes` now resolve to unanchored `owner-promises` Chip beats. `selectOwnerMandates` is memoized by game identity for stable old/demo-save fallbacks.
17. Governance source clarity - shipped on `/commissioner`, `/cba`, and `/league-rules`: `/commissioner` renders `Governance Sources` for saved commissioner-state selectors, petition/rule-vote commit actions, `selectCBAState`, `/cba` as the bargaining commit route, `LEAGUE_RULE_DEFINITIONS` / `/league-rules`, and the no-render-write ruling boundary. It also shows route-local Rule Petition/Vote receipts after existing `petitionRuleChange` or `voteOnProposal` actions resolve; those receipts are not durable vote history. `/cba` renders `CBA Sources` for saved `game.cbaState`, displayed `negotiationState` proposals, approve/reject/abstain `voteOnCBA`, `advanceCBANegotiation`, `/league-rules` as the effective-rule history view, and an `Outside CBA terms` row for schedule weeks, trade deadline week, overtime format, and comp-pick limit commissioner-lane rules; it also shows route-local Vote/Advance/Lockout receipts after existing CBA actions resolve and the current simple-majority approval threshold in the Owner Decision panel. `/league-rules` renders `Rule Registry Sources` for saved `game.leagueRules`, selector diffs/history, `/commissioner` and `/cba` commit owners, and the `getActiveRule` / `getRuleValueForYear` effective-year boundary. `/cba` and `/league-rules` are now first-class `LEAGUE` nav items rather than direct-only routes, so desktop nav, mobile More, and command-palette screen search expose them directly; route coaching maps them to unanchored `cba` and `league-rules` Chip beats. Commissioner-ruling schema compatibility is shipped: `CommissionerRulingSchema` matches the live TypeScript/UI fields and normalizes legacy `description` / `approvalImpact` rows across `commissionerState.rulings` and `commissionerDisciplineLog`. CBA abstain persistence is shipped: `NegotiationStateSchema`, `voteOnCBA`, and `/cba` support `approve` / `reject` / `abstain`; neutral CPU owner scores also record as `abstain`; generated CBA news reports approve-reject-abstain counts. Remaining governance work is still higher risk: rule-proposal AI votes are yes/no only, thresholding behavior remains store-local through `majorityThreshold`, durable CBA vote history is not added, and failed CBA votes clear `currentProposal`, `ownerVotes`, and `userVote`.
- Weekly-prep source clarity is shipped on `/game-plan`: the route renders `Weekly Prep Sources` for `selectCurrentOpponentReport`, `selectCurrentOpponentIntel`, route-local `WeeklyPrepPlan` draft state, `buildPrepDecisionForecast`, Save Weekly Prep & Sim, Skip With Auto Prep, and the no-render simulation boundary. Do not redo this as a display slice; future game-plan work should add new behavior or receipts with focused sim/save safety.
- Film-room source clarity and route coaching are shipped on `/film-room`: the route renders `Film Room Sources` for `selectLatestFilmRoomReport`, `selectFilmRoomHistory`, `buildFilmRoomReport`, `franchise-week.ts` write ownership, the 24-report cap, and the no-render report-generation boundary, and `resolveRouteKey` maps `/film-room` to unanchored `film-room` Chip beats. Do not redo this as a display slice or route-coaching slice; future film-room work should add new behavior or durable history only with focused save/sim safety.
18. Scenario constraint integrity slice: make scenario blocker copy and enforcement match. The source-backed coverage pass plus enforcement follow-ups are shipped: `blockTrades` gates generated/accepted market trades, direct proposal creation/submission/counter acceptance, accepted user deadline offers, and draft-war-room trade offers; `blockDraft` gates user draft-pick submissions; `blockFreeAgency` gates offseason bids, street free-agent signing, waiver claims, and practice-squad acquisitions. `getScenarioConstraintCoverage` now also exposes `allowedPlanningPaths`, and `/scenarios` renders `Still open:` rows so active locks distinguish blocked commits from safe read-only planning lanes. NewGameScreen scenario cards show source-backed constraint badges before launch, `/scenarios` active state renders a Blocked Action Guide explaining that blocked attempts stay uncommitted with allowed next moves, Monday Briefing `ActionCenter` shows active scenario-lock notices near the command queue, `/trades`, `/draft`, `/trade-deadline`, `/free-agency`, `/waivers`, and `/practice-squad` render route-level Scenario Lock copy for blocked buttons, and `/team-needs`, `/trade-block`, plus `/fa-targets` render planning-only Scenario Lock copy while remaining non-acquisition planning routes. Trade Center Reject/Reject Counter/package-building remain available, Trade Deadline Reject/clock/finalize remain available, Free Agency re-sign negotiation and market advancement remain available, and practice-squad release/elevation are internal roster-management paths rather than blocked external acquisitions.
19. Weather clarity slice - shipped: `features/shared/weatherImpactCopy.ts` owns shared saved-weather copy; Inbox now surfaces rain/snow/wind advisories with `/league/weather` CTAs; Game Day recap renders a `Weather Impact` panel for saved rain/snow/wind packages and labels saved `GameDayPackage.weather`, shared `getWeatherImpactCopy`, plus the no-generation/no-rerun/no-RNG boundary; `/league/weather` labels saved `schedule.weather`, the persisted `dome`/`clear`/`rain`/`snow`/`wind` key space, route-local UI forecast profiles, no render-time weather mutation, and each card's saved/dome/UI-only source; and `/league/weather` now resolves to unanchored `league-weather` Chip route beats. Future weather formula/key work still requires full balance or save-schema safety coverage.
20. Achievement clarity follow-up: the safe `/franchise/achievements` UI slice is shipped. The standalone route now uses `getAchievementProgress`, readable progress/goal copy, progress bars for locked visible achievements, and mystery copy for locked hidden achievements without changing persistence or unlock math. Achievement condition schema tightening is also shipped: `AchievementCondition.type` and `AchievementConditionSchema.type` use `ACHIEVEMENT_CONDITION_TYPES`, known catalog rows with stale conditions repair to catalog defaults, and malformed custom rows with unknown metric ids are filtered. Comeback Kings metric unlockability is shipped: `hidden:comeback_kings` now counts existing saved comeback named-game dynasty events won by the user team after the schedule-aware playoff boundary, bucketed by event year for one postseason run. Remaining achievement work is higher risk only if it adds durable achievement events, new hidden metrics, or richer named-game persistence.
21. Player-memory slice - profile, timeline, comparison, development, rivalry, bloodline, and draft-recap follow-through clarity shipped: `PlayerProfile.tsx` has `Career Memory` from existing profile history/saved rivalries, `Transaction Memory` from saved user-team `txLog` rows, and `Signature Moments` from existing `awardsHistory`, `game.records`, profile career stats, saved player rivalries, transaction rows, matching draft-recap picks, and lineage/archive context. `PlayerTimeline.tsx` labels timeline/archive/current-player/transaction/draft-recap sources and now renders saved transaction-log memory plus matching saved draft recap memory for the route player. `/draft-recap` now joins selected saved recap picks to current `game.players` plus `selectTeams` in a read-only `Class Follow-Through` panel without writing history. `/compare` labels current contracted `game.players`, active salary-cap contract grading, route-local picker state, Player-A position-stat lens, and the no-write boundary, `/player-development` lets the user choose any current roster player through route-local state and wires that selected player into existing report/projection/breakout/coach-impact read models with no-write source copy, `/rivalries` labels saved `playerRivalries`, social-post projection, week-advance ownership, live sim-bonus source semantics, and the no-derived-sidecar boundary, and `/legacy/bloodlines` labels selector/read-only family sources. Design-system player links hash-route to `/player/:id` under `createHashHistory()`. Future profile/timeline/comparison/bloodline/rivalry work should still use existing durable data first. Read the guide's player identity boundary first: profile legacy history can be synthesized, profile injury history is current-only, player rivalries are sim-affecting, draft bloodlines add only a +1 parent-team loyalty nudge plus dynasty/family edges, and career comparison currently caps at four players. Add schema/migration/default/old-save tests if you add durable history.
22. Hall of Fame archive clarity and ballot persistence - shipped: `/legacy` labels Hall of Fame entries as the current save archive, saved `game.ballotWaitlist` / `game.ballotEliminatedIds` counts, a `Ballot Watch` list from saved waitlisted candidates, ballot pressure summary rows for average saved vote / 70%+ support / year-5 candidates / top watch, and links to `/franchise/hall`; `/franchise/hall` labels `mfd.hallOfFame.v1`, `readHallOfFameArchive()`, live `game.hallOfFame` count, current snapshot count/last sync, and explicit `Sync Current Save` full-snapshot sidecar writing. HOF ballot persistence/defaulting is shipped: `HallOfFameBallotEntrySchema`, v35 migration normalization, current schema defaults, new seed/demo/test/playtest defaults, web cartridge import normalization, and `/legacy` copy make the existing deterministic ballot branch live for production-shaped saves. Durable HOF epilogue persistence is shipped, `HallOfFamerDetailModal` renders existing displayable `HallOfFameEntry.epilogue` category/headline/story, HOF ceremonies/news prefer existing epilogue headline/story with old highlight/peak fallback, and Dynasty Chronicle HOF events reuse existing epilogue category/headline/story in timeline/detail context. HOF sidecar JSON portability is shipped: `/franchise/hall` exports/imports versioned `mfd.hallOfFame.archive.v1` JSON or raw sidecar payloads for `mfd.hallOfFame.v1` only, with no `GameState`, autosave, cartridge, sim, or RNG write. Future HOF/archive work should focus on richer workflow only if it adds new value, not source labels, ballot persistence/defaulting, ballot count/detail/pressure display, manual refresh, standalone HOF sidecar export/import, complete sidecar archive portability, or cartridge epilogue durability.
23. Awards Hub source clarity slice - shipped: `/awards` labels saved `game.awardsHistory` through `selectAwardsHistory`, saved `AwardsHistoryEntry.ceremony` headline/intro fields, saved winner/runner-up/score/narrative rows, route-local season scrubber state, and the no-render-write boundary. No award generation, ceremony writes, season-history patching, player mutation, records/HOF/archive changes, media publication, achievement unlocks, save schema, sim math, RNG, movement, or route ownership changed.
24. Super Bowl presentation source clarity and route coaching slices - shipped: `/super-bowl` labels saved playoff bracket/result/champion/user-team/year inputs, deterministic presentation helpers, authored `getAwardSpeech` podium copy, browser-local `mfd-celebration-dismissed:<teamId>:<year>` dismissal state, and the no-render-write boundary; it also resolves to unanchored `super-bowl` Chip beats. No playoff bracket write, game-result write, ceremony write, awards/franchise-history/archive/media/achievement write, save schema, sim math, RNG, movement, or route ownership changed.
25. MVP Plaque Wall source clarity slice - shipped: `/franchise/mvps` labels saved `game.awardsHistory` through `selectAwardsHistory`, current `selectUserTeam`, `selectFranchiseEras` labels derived from franchise history, `buildMvpPlaqueAwards` user-team filtering of MVP/OPOY/DPOY/Coach of the Year rows, saved `winnerStats`, rounded award `score`, route-local filter/sort controls, and the no-render-write boundary. No award generation, career-stat mutation, ceremony write, season-history patch, era write, player/record/HOF/archive/media/achievement write, save schema, sim math, RNG, movement, or route ownership changed.
26. Dynasty Scrapbook source clarity and auto-authoring - shipped: `/franchise/scrapbook` labels `deriveDynastyId(game)`, browser-local `mfd.scrapbook.v1`, `readScrapbookForDynasty`, `summarizeScrapbook`, app-shell year-rollover write ownership through `syncScrapbookAtYearRollover` / `buildSeasonRecap` / `buildScrapbookEntry` / `appendScrapbookEntry`, pending playoff-lore merge semantics, route-local export status, and browser-only PNG export. `buildScrapbookEntry` now also folds saved league news, saved `dynastyTimeline` `named_game` events, saved bloodline `draft_pick` events, saved individual award winners, saved story-arc stage beats, saved season-report grades/overviews, record notes, HOF inductions, and heated league rivalries for the recap team/year into season notable moments. No scrapbook sidecar schema, sidecar write trigger, pending-lore clear rule, season-recap rebuild trigger, `GameState` update, media generation, era detection, achievement award, award formula, awards-night ceremony behavior, story-arc generation, season-report generation, offseason writes, league-news recording, draft/bloodline formula, save schema, sim math, RNG, movement, or route ownership changed.
27. Dynasty Chronicle source clarity slice - shipped: `/franchise/chronicle` labels `deriveDynastyId(game)`, `computeDynastyChronicle`, saved `franchiseHistory` / `hallOfFame` / `coachingHistory`, scrapbook and playoff-lore sidecar reads, filter/presenter helpers, route-local filter chips/detail modal state, and browser-only PNG export. HOF chronicle entries now reuse existing saved epilogue category/headline/story for timeline/detail context when present, with the old name/position fallback when absent. No franchise-history write, Hall of Fame write, coaching-history write, scrapbook write, playoff-lore write, ceremony/record/media/achievement write, save schema, sim math, RNG, movement, or route ownership changed.
28. GM Career and Rookie-of-Year source clarity slices - shipped: `/franchise/career` labels browser-local `mfd.careerMeta.v1`, `readCareerMeta`, validated/recomputed career totals, `deriveDynastyId(game)`, career write owners, and `GmCareer` route ownership. Its Rookie of the Year panel labels browser-local `mfd.rookieOfYear.v1`, `readRookieOfYearEntries(dynastyId)`, `syncRookieOfYearAtYearRollover` / `computeRookieOfYear`, dynasty scope, and the no-render-write/no-sim boundary. No career-meta write, award recompute, sidecar write, dynasty finalization, autosave, save-schema, sim, RNG, movement, or route-registration behavior changed.
29. Trophy Room source clarity slice - shipped: `/franchise/trophy-room` labels saved `game.franchiseHistory` champion seasons, saved ceremonies through `selectCeremonies`, `buildTrophyRoomChampionships` title-to-ceremony pairing and fallback copy, route-local `filterMode`, recent-window filtering, dynasty-run grouping, selected trophy modal state, and the no-render-write boundary. No championship award, ceremony generation, franchise-history update, media write, achievement unlock, sidecar write, save-schema, sim, RNG, movement, or route-registration behavior changed.
30. Era Hall source clarity slice - shipped: `/franchise/eras` labels `selectFranchiseEras` detected dashboard eras over saved `game.franchiseHistory`, `buildEraHallEntries` presentation rows, `selectUserTeam` current-era roster snapshots, the present-day fallback, and separate user-named era write ownership through `actions.nameDynastyEra` / `startDynastyEra`. No era naming, era start/end write, `team.era` write, franchise-history update, championship award, save-schema, sim, RNG, movement, sidecar, or route-registration behavior changed.
31. Franchise Hub source clarity and action receipts - shipped: `/franchise` labels `selectFranchiseDashboard`, `selectUserTeam`, `selectFranchiseEras`, `selectStadiumDealOffers`, `selectCanRelocate`, read-only child modules, `buildCoachingLegacy`, and explicit write buttons for `upgradeStadium`, `acceptNamingRights`, and `/relocate`. Stadium and naming-rights buttons now show route-local receipts after the existing store actions resolve, with source, commit path, state touched, pre-action context, and no-save/no-sim/no-RNG boundaries. No durable receipt history, stadium/naming formula, relocation behavior, doctrine award, era detection, franchise-history update, save-schema, sim, RNG, movement, sidecar, or route-registration behavior changed.
32. Franchise Book source clarity and win-label slices - shipped: `/franchise/book` labels `buildFranchiseBook(game, userTeam.id)`, saved `game.franchiseHistory`, saved `game.userDynastyEras`, saved `game.dynastyTimeline`, signature-player reads from `game.playerArchive` and `game.playerSeasonHistory`, authored `era-templates.json` / `book-commentary.json`, route-local TOC state, browser-only print controls, and the no-render-write boundary. Its chapter-margin `WIN %` value renders saved `chapter.record.winPct` as an explicit percentage, so `0.682` displays as `68.2%`. No dynasty-event write, era start/name write, franchise-history update, player mutation, news write, save-schema, sim, RNG, movement, sidecar, or route-registration behavior changed.
33. Season Recap source clarity slice - shipped: `/season/recap` labels `buildSeasonRecap(game, team.id)`, `SeasonRecapCard`, saved `game.seasonNearMissReceipts`, explicit `Export PNG` / `Copy Text` helper imports through `exportRecapAsPng` and `copyRecapAsText`, browser download/clipboard behavior, and the no-render-write boundary. No season archive, near-miss receipt generation, league-year advancement, player mutation, media write, save-schema, sim, RNG, movement, sidecar, or route-registration behavior changed.
23. Offseason calendar clarity follow-up: the safe read-only slice is shipped. `selectOffseasonCalendar` derives the `/week-advance` Offseason Calendar panel from existing phase/offseason/draft/training/CBA/expansion state, shows re-signing, FA rounds, draft, draft recap, training camp, preseason, and paused CBA/expansion rows, and uses route-only CTAs. `/free-agency` now mirrors the same shared read model beside market actions with no-render-write copy. `/week-advance` now renders `Offseason Command Snapshot` from the shared calendar plus saved `offseasonState`, user-team roster contracts, and team cap fields to summarize active owner, expiring core, cap room, and market load. No second calendar model, save schema, phase advancement, bid/round resolution, roster/cap mutation, sim formula, or mutation path changed. Future offseason work should not add persisted calendar state unless it deliberately becomes a save-schema/migration/default/old-save/import slice.
24. Endorsement follow-up slices - shipped: accepted endorsement visibility/revenue is repaired, and `PlayerSchema.endorsements` plus top-level `endorsementOffers` now use typed endorsement schemas with legacy sanitization/default coverage. The accept path resolves the roster player first, writes the accepted deal/morale update back into `game.players`, removes the pending offer, and has store selector coverage proving active deals/revenue and Player Profile agree for JSON-loaded save shapes. `/endorsements` now also shows route-local Endorsement Action Receipts after existing Accept/Decline actions resolve, labeling saved pending-offer ownership and no-extra-write boundaries. Future endorsement work should only tighten roster-embedded player schema as part of a broader team roster persistence slice, or add genuinely new route value.
25. Team-needs/free-agency guidance clarity slice - shipped: `/team-needs` labels the saved `teamNeedsCache` versus derived report boundary and calls draft/FA target rows planning lanes only; it also renders planning-only Scenario Lock copy when saved draft, free-agency, or trade blockers are active while leaving the report and comparison rooms available. `/team-needs` now shows read-only CPU Intent plus an `Intent Ledger` and `Strategy History` for the selected comparison team, using saved `team.philosophy`, canonical `team.gmStrategy`, cache-aware needs, cap, needs, trade-block flags, saved `game.eventLog` `gm_strategy_shift` rows, and saved `game.leagueNews` team-philosophy signal rows. `/fa-targets` labels saved `faTargetBoard` snapshot ownership, empty-board/Refresh Board rebuild behavior, watchlist-only toggles, and `/free-agency` as the real bid/signing owner; visible target rows now render read-only `FA Market Receipt` copy from fit score, sign probability, projected salary, market demand, and competing-team fields. `/free-agency` now renders a read-only `Bid Resolution Summary` from saved `offseasonState.freeAgencyBids` won/lost rows after round resolution. No formulas, cache invalidation, watchlist persistence shape, bids, signings, roster movement, save schema, RNG, or sim math changed.
26. Mentor-route receipt polish slice - shipped: `/mentors` labels saved `activeMentors`/`mentorBudget`, the shared Team Ops receipt, `calculateMentorEffects`, and Hire/Release store actions. Rendering `/mentors` does not run progression, update normal mentoring pairs, alter CPU teams, or rewrite training-camp receipts.
27. Press podium receipt clarity slice - shipped: `PressConferenceModal` labels podium answers as saved `postGameUi.pressConferenceQueue` receipts and tone choices only. `respondToPressConference` is tested to write only `selectedTier`/`selectedResponse` and not add press conferences, active effects, event logs, league news, social posts, or owner/player changes.
28. Scenario launch coverage badges slice - shipped: `NewGameScreen` scenario-launch cards reuse `getScenarioConstraintCoverage` through `ScenarioLaunchCoverageBadges`, showing `Open rules` for unrestricted scenarios and enforced blocker badges such as `Trade Actions enforced` / `Offseason Free Agency enforced` before a save is started. Start Challenge behavior, setup-state deletion, scenario definitions, save schema, and enforcement paths were unchanged.
29. Facility/medical source clarity slice - shipped: `/settings` labels saved `team.facilityState`, saved `facility.effect`, existing `upgradeFacility`, saved `team.medicalStaff`, game-level `availableMedicalStaff`, the offseason-only `hireMedicalStaff` gate, and shared `buildTeamOpsImpactReceipt` ownership. Rendering does not refresh medical candidates, process injury recovery, run progression, or resolve training camp.
30. RecordBook source clarity slice - shipped: `/records` labels saved `game.records`, saved `activeRecordChases`, saved `recentBrokenRecords`, saved `recentMilestones`, local route filters/selection, and the no-render-time media boundary. Opening the route does not detect or write records, recalculate chase receipts, add news/social/storylines, or create ceremonies.
31. Stat Central source clarity slice - shipped: `/stat-central` labels current-season rows from live `game.players`, historical rows from `playerSeasonHistory`, retired-player fallback context from `playerArchive`, local route state for tabs/filters/comparison slots, and the display-only boundary. Opening the route does not write stats, records, news, social posts, history rows, or archive entries.
32. Power Rankings source clarity slice - shipped: `/power-rankings` labels saved `game.powerRankings`, `updatePowerRankings` ownership, media-cycle scoring/blurbs, saved `previousRank`/`delta`, route-local presentation, and the display-only boundary. Opening the route does not write rankings, news, social posts, storyline threads, records, or media-cycle history.
33. League Pulse source clarity slice - shipped: `/league-pulse` labels saved `leagueRivalries`, saved `game.powerRankings`, `buildLeaguePulse` ownership, explicit-caller-only heat-spike posts, route-local display caps, and the display-only boundary. Opening the route does not write rivalries, rankings, news, social posts, storyline threads, or history rows.
34. Scenario blocked-action UX copy slice - shipped: `/scenarios` active state renders a `Blocked Action Guide` from enforced `getScenarioConstraintCoverage` items. It labels trade, acquisition, and draft-pick blockers as `ATTEMPT STAYS UNCOMMITTED`, explains the related rosters/picks/cap/bids/claims/practice-squad/free-agent/draft-board state remains unchanged, and gives safe next moves without changing enforcement, save schema, movement, sim math, or RNG.
35. Scenario planning-lanes clarity slice - shipped: `/scenarios` active coverage now renders `Still open:` rows from `getScenarioConstraintCoverage.allowedPlanningPaths`, listing safe read-only planning lanes that remain available under active blockers without changing enforcement, scenario definitions, save schema, movement, sim math, or RNG.
36. Comp-pick explanation and active-rule display slices - shipped: `/draft` marks saved `isCompPick` picks, explains why they were awarded from FA Round 3 transaction logs, shows the active `comp_pick_limit` value, and points to normal draft order, league news, Inbox, and `Comp Pick Master` as shared saved-pick consumers.
37. Player Comparison context slice - shipped: `/compare` renders `Comparison Context` for current contracted `game.players`, active salary-cap contract grading, route-local picker state, Player-A position-stat lens, and the display-only boundary without writing stats, contracts, history, archives, awards, records, news, or social posts.
38. Action-point scenario notices slice - shipped: Monday Briefing `ActionCenter` reads active `game.scenarioState` through `getScenarioConstraintCoverage`, renders `Scenario locks active` beside the command queue, labels enforced trade/free-agency/draft locks, says locked actions do not commit, and links to `/scenarios` without changing actions, enforcement, save schema, sim math, or RNG.
39. Broader post-week receipt expansion - shipped: `buildPostWeekMoment` now carries saved `GameDayPackage` stakes, rivalry heat, special-teams swings, podium follow-up, and coaching notes into Week Advance's existing `Post-Week Command Deck` sections without changing package writers, weekly summaries, week-advance mutation, media generation, save schema, sim math, or RNG.
40. App lifecycle guardrail coverage - shipped: `App.test.tsx` now source-guards `RootLayout` ownership of global keyboard setup, route shortcut registration/cleanup, hotkey help metadata, shell overlays, audio queue playback/clearing, save reminders, year-rollover sidecar sync calls, and the autosave toast week-baseline cue; `appShellLifecycle.test.ts` locks `resolveAutosaveToastStep` so the same week cannot retrigger the cosmetic save cue. `useKeyboard.test.ts` source-guards `useGlobalKeyboard` document listener add/remove cleanup. Companion route-context guardrails are also shipped: `useChipEvents`, `ChipDock` fallback route handling, and `ErrorBoundary` fallback context reuse `resolveCurrentAppRoute` for hash/base normalization. No route registration, nav metadata, save schema, sim, RNG, movement, or persistence writer changed.
- Acquisition route identity guard - shipped: `App.test.tsx` locks `/trades`, `/trade-block`, `/free-agency`, `/fa-targets`, `/team-needs`, `/waivers`, `/practice-squad`, `/scouting`, and `/draft` to their distinct market, planning, movement, scouting, and draft components.
- Player route identity guard - shipped: `App.test.tsx` locks `/player/$playerId`, `/player/$playerId/timeline`, `/compare`, `/rivalries`, and `/player-development` to their profile, timeline, comparison, rivalry, and development components.
- Coaching decision and development receipts - shipped: `/coaching` renders a read-only `Coaching Decision Receipt` from staff vacancies, best market fit, highest retention risk, scheme continuity, and clinic leverage while leaving Hire/Fire/Promote/Refresh/Apply/Run Clinic/Activate tier buttons as the only commits. Completed Run Clinic actions render a route-local `Clinic Run Receipt`; completed Activate tier actions render a route-local `Skill Activation Receipt` with coach, branch, tier label, and prior active branch context.
- Meta/system route identity guard - shipped: `App.test.tsx` locks `/about`, `/credits`, `/faq`, `/dynasty`, and `/settings` to launch, backup, and settings surfaces while `nav-items.test.ts` keeps the meta nav group complete.
- Governance/league route identity guard - shipped: `App.test.tsx` locks `/owner`, `/commissioner`, `/cba`, `/league-rules`, `/standings`, `/analytics`, and `/power-rankings` to distinct owner, governance, rules, standings, analytics, and ranking surfaces.
- Core/team/gameplay route identity guard - shipped: `App.test.tsx` locks roster, watch list, locker room, depth chart, training camp, mentors, endorsements, week advance, handshakes, game day, game plan, schedule, and Super Bowl routes to their command surfaces.
- Franchise business/scenario route identity guard - shipped: `App.test.tsx` locks `/relocate`, `/expansion-draft`, and `/scenarios` to relocation, expansion draft, and scenario setup surfaces.
- Special direct route identity guard - shipped: `App.test.tsx` locks `/franchise/achievements` and `/league/weather` to standalone achievement gallery and league forecast surfaces while preserving root-tree registration checks.
- Remaining direct route identity guard - shipped: `App.test.tsx` now references every registered route variable and locks `/inbox`, `/social`, `/franchise/book`, `/film-room`, `/trade-deadline`, and `/franchise/scrapbook` to their intended inbox, social, book, film-room, deadline, and scrapbook surfaces.
- Legacy/archive route identity guard - shipped: `App.test.tsx` locks `/legacy`, `/legacy/named-games`, `/legacy/bloodlines`, `/awards`, `/records`, `/franchise`, `/legends`, `/franchise/hall`, `/franchise/trophy-room`, `/franchise/eras`, `/franchise/mvps`, `/franchise/playoff-lore`, and `/franchise/chronicle` to their distinct memory/archive components.
41. Franchise memory source labels and rivalry saved-result readout - shipped: `/franchise` labels `HomegrownMeter` as current roster plus `playerArchive` provenance without archive writes, `ContinuityMeter` as browser-local `mfd.rosterContinuity.v1` starter snapshots rather than cartridge data, and `RivalryHeatMap` as authored rivalry content plus runtime team lookup with W-L-T, total games, and latest meeting derived from saved completed schedule/playoff results. No dashboard read model, sidecar write helper, archive write, rivalry write, save schema, sim math, or RNG changed.
41. Bloodlines source clarity - shipped: `/legacy/bloodlines` now labels `selectBloodlineFamilies` as the route source, separates active `game.players` from draft prospects with saved `bloodline` fields, explains active roster OVR/age/team versus scout-grade rookie OVR signal, and states the route does not assign bloodlines or write family relationship edges.
42. Coaching source clarity and notable-protege readout - shipped: `/coaching` now renders `Coaching Source Context` for `selectCoachingStaff`, `selectCoachingMarket`, route-local scheme preview state, saved clinic/skill selections, commit buttons, and no-render-write boundaries. `/coaching/tree` labels active league `team.staff`, saved `game.coachingHistory`, coach-retirement events, `buildCoachingLegacy`, mentor/disciple fields, and no route writes; it also renders `buildCoachingLegacy(...).notableProteges` as active branch rank, role, team, and title-count rows. `/coaching/relationships` labels saved `game.relationships`, active staff/roster labels, route-local filters/layout, no route writes, and stable empty fallbacks for old/demo saves.
43. Near-miss receipt recap surface - shipped: `/season/recap` now renders saved `game.seasonNearMissReceipts` as `What-If Receipts` when present, labels season-rollover generation, declined direct-trade inputs, passed-pick draft inputs, user free-agent bids that lose to signed CPU offers, and the no-write boundary for receipt generation or tracker mutation.
44. Super Bowl storyline champion-casing fix - shipped: `generateSuperBowlContext` now counts prior championships through a case-insensitive champion-finish helper, so canonical lowercase `playoffFinish: 'champion'` rows drive dynasty and first-title Super Bowl storylines.
45. Halftime modal copy alignment - shipped: `HalftimeDecision.tsx` now describes the engine-backed `switch` `-1/+1` and `gamble` `+3/-2` team OVR deltas instead of stale percentage-efficiency copy, with focused SSR coverage and no sim/save/week-advance changes.
46. Draft-board source clarity - shipped: `/draft` now renders `Draft Board Sources`, labeling saved `game.draftClass`, saved `offseasonState.scoutingState` intel/fallbacks, `selectWarRoomState` saved-versus-derived behavior, existing store action owners, route-local `Offer Confidence`, route-local `Draft Market Receipt`, and the non-durable reveal card boundary.
47. Scouting-board source clarity - shipped: `/scouting` now renders `Scouting Sources`, labeling saved `game.draftClass`, saved `offseasonState.scoutingState` intel, store/engine scouting action ownership, saved draft scouting watchlist versus browser-local `mfd.watchlist.v1` pins, authored scouting-report templates, and the no-render-mutation boundary.
48. Scouting Action Receipts - shipped: `/scouting` now renders route-local receipts after existing hire/fire/watchlist/film/combine/interview/pro-day/private-workout store actions resolve, naming source, commit path, state touched, and pre-action context without adding saved receipt history, save schema, scouting formulas, scout pool refresh, workout-spend changes, movement, sim, or RNG.
49. Commissioner Action Receipts - shipped: `/commissioner` now renders route-local Rule Petition/Vote receipts after existing `petitionRuleChange` or `voteOnProposal` actions resolve, naming source, commit path, state touched, and pre-action rule context without adding durable vote history, a second governance ledger, save schema, CBA vote behavior, rule-application behavior, sim, or RNG.
50. CBA Action Receipts - shipped: `/cba` now renders route-local Vote/Advance/Lockout receipts after existing `voteOnCBA` or `advanceCBANegotiation` actions resolve, naming source, commit path, state touched, and pre-action bargaining context without adding durable vote history, a second bargaining ledger, save schema, vote threshold changes, CBA formulas, rule projection changes, sim, or RNG.
51. Franchise Action Receipts - shipped: `/franchise` now renders route-local Stadium Upgrade/Naming Rights receipts after existing `upgradeStadium` or `acceptNamingRights` store actions resolve, naming source, commit path, state touched, pre-action context, and no-save/no-sim/no-RNG boundaries without adding durable receipt history, save schema, stadium/naming formulas, relocation changes, sim, or RNG.
52. Halftime receipt source note - shipped: `GameDayRecap.tsx` now detects saved `Halftime hell:` summaries in `GameDayPackage.activeEffectSummaries` and renders a `HALFTIME DECISION RECEIPT` source note.
53. Cap rule-awareness regression guard - shipped: `architecture-boundaries.test.ts` now blocks production cap read-model/runtime callers from using year-only `getSalaryCap`/`getCapFloor` calls outside seed-time roster generation, preserving active `salary_cap_growth` and `cap_floor_pct` display context.
54. Structured halftime/broadcast receipt hook - shipped: `buildHalftimeDecisionReceipt` parses saved `Halftime hell:` rows from `GameDayPackage.activeEffectSummaries`, and `GameDayRecap`, `GameBroadcast`, `PlayByPlay`, and `BroadcastPresentation` consume the same read model through the matching saved game-day package.
- Game Day podium receipt - shipped: `/game-day` renders a read-only `Podium Receipt` from saved `GameDayPackage.pressConference`, labels the matching `buildPostWeekMoment` `Podium Follow-Up` hook, and does not record press conferences, add timed effects, write event/news/social feeds, or answer the podium queue during render.
- Postgame Source/Decision Receipts - shipped: `/game-day` renders read-only `Postgame Source Receipt` and `Postgame Decision Receipt` panels from the saved `GameDayPackage` returned by `selectLatestGameDayPackage`, labeling saved package identity/result, turning points/autopsy leverage, top performers, prep, health, carryover, next-focus rows, and the no-replay/no-package-write/no-score/injury/movement/press/autosave/RNG boundary.
- Postgame Decision Receipt - shipped: `/game-day` renders a read-only `Postgame Decision Receipt` from saved `GameDayPackage` prepGrade, coachingNotes, carryForwardRecommendations, injuryNotes, activeEffectSummaries, and autopsy.nextFocus, labeling prep, health, carryover, and next-week focus without recalculating Film Room, applying training, adjusting fatigue, changing injuries/morale, answering press, autosaving, or touching RNG.
- Named-Game Postgame Memory CTA - shipped: `/game-day` renders a `Named Games` CTA and saved-source receipt when the latest package/result carries `namedGame`, linking to `/legacy/named-games` without re-running `detectNamedGame`, rewriting results, repairing timeline rows, changing scores, simulating, or touching RNG.
- Position Coach Lifecycle Actions - shipped: `/coaching` can initialize missing saved `team.positionCoaches` rooms, upgrade one existing role, render route-local `Position Coach Receipt` panels only after explicit store actions commit, show saved tenure, and label championship rollover as the source for that tenure. Championship rollover advances existing saved position-coach tenure through `advancePositionCoachSeason`. No save shape, migration, progression formula, HC/OC/DC staff path, clinic XP, skill-selection, coaching market, route registration, sim math, or dependency changed.
- TeamLogo public logo guard - shipped: `TeamLogo.test.tsx` locks route-relative `logos/${icon}.png`, 512:279 derived dimensions, alt fallback, silent missing-file hide behavior, and every selectable `getTeamOptions()` icon resolving to a shipped logo file. This is a route/app-shell visual guard only; no production runtime, asset file, route, save, sim, or RNG behavior changed.
- Broadcast source clarity is shipped: `/broadcast` renders `Broadcast Sources` for transient selected-game UI context, `selectBroadcastByGameId`, saved result broadcast read models, `selectGameDayPackageByBroadcastGameId`, `buildHalftimeDecisionReceipt`, deterministic `buildBroadcastCommentary`, route-local tabs/ghost lines/MVP radar, and the no-render mutation boundary.
- Presentation source clarity is shipped: `/presentation` renders `Presentation Sources` for `selectLatestBroadcast`, `buildBroadcastPresentation`, `selectGameDayPackageByBroadcastGameId(null)`, `buildHalftimeDecisionReceipt`, route-local beat controls, and the no-render mutation boundary.
- Play-by-Play source clarity is shipped: `/play-by-play` renders `Play-by-Play Sources` for `selectLatestBroadcast`, saved/rebuilt `BroadcastOutput.quarters`, route-local drive expansion, copied/sorted/capped highlights, ghost/commentary texture rows, and the no-render mutation boundary.
- Game Flow source clarity is shipped: `/game-flow` renders `Game Flow Sources` for `selectLatestBroadcast`, pure `analyzeGameFlow`, `PixelEkg` win-probability inputs, derived momentum/efficiency/turning-point reads, and the no-render mutation boundary.
51. Custom trade-deadline interruption recognition - shipped: `game-store.ts` now reads the active `trade_deadline_week` for post-engine interruption routing after both normal `advanceWeek` and `resolveHalftimeDecision`, with custom Week 10 store tests and browser smoke coverage. `/trade-deadline` also renders Scenario Lock copy when saved `blockTrades` is active, disables deadline Accept buttons, and leaves Reject, clock advance, and finalization available without changing offer generation, valuation, movement, save schema, sim math, or RNG.
52. Trade Deadline source clarity and receipts - shipped: `/trade-deadline` renders `Deadline Sources` in active and idle states, labeling saved `game.tradeDeadlineState`, no render-time offer generation, store-owned Accept/Reject/Advance/Finalize actions, and the Finalize boundary where completed-deal rows actually move players or picks. It renders read-only `Deadline Deal Receipt` copy from saved completed-deal team/player/pick/grade/timestamp/splash fields, and it renders route-local `Deadline Action Receipt` panels after existing Accept, Reject, or Advance Clock actions resolve. Finalize intentionally keeps no route-local receipt because it resumes week advance and navigates to `/game-day`.
53. Expansion Draft source clarity - shipped: `/expansion-draft` renders `Expansion Sources` in active and idle states, labeling saved `game.expansionDraftState`, route-local protection checkbox state, `protectExpansionPlayers`, the non-mutating preview feed boundary, and `finalizeExpansionDraft` as the league mutation that creates the new team, moves selected players/contracts, rebuilds the schedule, clears expansion state, commits, and routes back to `/franchise`.
54. Relocation source clarity - shipped: `/relocate` renders `Relocation Sources`, labeling `selectUserTeam`, `selectCanRelocate`, `selectRelocationDestinations`, route-local selected destination and confirmation-modal state, no route-open write, and `relocateTeam(destinationId)` as the confirmed store commit that applies engine relocation, charges cost through franchise cap space, syncs relocated roster players, commits, and routes back to `/franchise`.
52. Private-workout/pro-day scouting explanation - shipped: `/scouting` prospect cards now explain pro-day `proDayRating`, confidence/visible-grade tightening, no workout-slot spend, private-workout slot consumption, saved `privateWorkoutRatings`, top-rating verification, and risk/ceiling-band unlock behavior from existing saved intel.
53. Free-agency decision forecast - shipped: `buildFreeAgencyDecisionForecast` reads existing re-sign, open-market-bid, and street-signing scoring/gating semantics, and `/free-agency` renders Decision Forecast rows beside the existing buttons with immediate impact, season impact, future risk, resolution, source, warnings, status, and confidence without changing bids, signings, roster movement, save schema, sim math, RNG, or commit paths.
54. Free-agency scenario-lock notice - shipped: `/free-agency` reads saved `scenarioState.activeScenario.constraints.blockFreeAgency`, renders Scenario Lock copy, disables open-market bid and street-signing buttons, and leaves in-house re-sign negotiation plus market advancement available. No scenario definitions, enforcement, bids, signings, roster movement, save schema, sim math, RNG, or commit paths changed.
55. Offseason Calendar Readout - shipped: `/free-agency` mirrors the existing shared `selectOffseasonCalendar` read model beside market actions, labels saved phase/offseason/CBA/expansion calendar status plus the no-render-write boundary, and does not add a second calendar model or change phase logic, bids, signings, movement, save schema, sim math, RNG, or commit paths.
56. Free Agency Action Receipts - shipped: `/free-agency` renders route-local `Free Agency Action Receipt` panels after existing re-sign offer, open-market bid, or street-sign actions resolve. Receipts label player/offer/round or phase context, forecast status, touched re-sign/bid/roster/cap/audio/autosave state, commit paths, and no-extra-write boundaries without durable receipt history, save shape, bid or negotiation formula changes, cap formula changes, phase advancement, sim math, RNG, route registration, or package ownership changes.
57. Free Agency Bid Resolution Summary - shipped: `/free-agency` renders a read-only `Bid Resolution Summary` from saved `offseasonState.freeAgencyBids` rows after `resolveFreeAgencyRound` marks bids `won` or `lost`, labeling user wins, user losses, CPU-only results, saved winner scores, bid counts, and any user lost-bid score without re-scoring bids, resolving the round, submitting offers, moving players, changing cap totals, autosaving, or rerolling outcomes.
58. CPU Strategy History Display - shipped: `/team-needs` renders read-only `Strategy History` rows for the selected CPU comparison team from saved `game.eventLog` `gm_strategy_shift` rows plus saved `game.leagueNews` team-philosophy signal rows, without reevaluating strategy, applying philosophy, writing news, refreshing teamNeedsCache, generating offers, moving assets, autosaving, or rerolling outcomes.
59. Postgame Source/Decision Receipts - shipped: `/game-day` renders read-only saved-package receipt rows from `GameDayPackage` id/week/result, autopsy/turning points, top performers, prep, health, carryover, active-effect, injury-note, and next-focus fields without replaying games, creating packages, changing scores, altering injuries, moving players, answering press, autosaving, or touching RNG.
60. Offseason Command Snapshot - shipped: `/week-advance` renders read-only offseason command rows from `selectOffseasonCalendar`, saved `offseasonState`, user-team roster contracts, and team cap fields without advancing offseason, submitting bids, resolving free-agency rounds, re-signing players, moving roster assets, mutating cap totals, autosaving, or touching RNG.
59. Trade Center Action Receipts - shipped: `/trades` renders route-local `Trade Action Receipt` panels after existing generated-offer accept/reject, direct proposal submit, and counter accept/reject actions resolve. Receipts label package and forecast/status context, touched offer/proposal/roster/pick/conditional-pick/cap/audio/autosave state, commit paths, and no-extra-write boundaries without durable receipt history, save shape, valuation or cap formula changes, scenario/deadline gate changes, sim math, RNG, route registration, or package ownership changes.
60. Newsroom Headline Story Receipt - shipped: `/newsroom` `HeadlineModal` renders read-only `Story Receipt` rows from the selected saved `game.leagueNews` item, `NewsItemSchema` id, year/week, linked team/player ids, engine/news writer ownership, and the no-write modal boundary. No story generation, `leagueNews` append, breaking-news dismissal, week advance, autosave, roster/player mutation, save schema, sim math, RNG, route registration, or package ownership changed.
54. Roster-cut decision clarity - shipped: `/roster` selected-player modal renders a Cut Forecast from standard-cut `calcDeadMoney`, cap hit, room delta, active roster relief, waiver path, and `buildCutAdvisor` reason; confirmation copy labels dead money and waiver movement without changing `cutPlayer`, movement, cap formulas, save schema, sim math, RNG, or commit paths.
55. Draft-pick confidence forecast - shipped: `/draft` Big Board rows render a Pick Forecast from current pick round, projected round, need fit, saved scouting confidence/risk/ceiling/character, and source ownership before `makeDraftPick`; no prospect generation, pick mutation, reveal persistence, war-room formulas, save schema, sim math, RNG, or commit paths changed.
56. Facility upgrade forecast - shipped: `/settings` facility rows render an Upgrade Forecast from saved `team.facilityState`, saved `facility.effect`, upgrade budget/costs, and `getFacilityLevelEffect`; `upgradeFacility` remains the only commit path with no facility formula, progression, training-camp, save schema, sim, RNG, or movement changes.
57. Medical-staff hiring forecast - shipped: `/settings` staff candidates render a Hiring Forecast from saved `team.medicalStaff`, game-level `availableMedicalStaff`, phase gate, salary, and recovery/prevention deltas; `hireMedicalStaff` remains the only commit path with no medical pool refresh, injury recovery, progression, save schema, sim, RNG, or movement changes.
58. Training-camp readiness/carryover clarity - shipped: `/training-camp` renders Camp Readiness from saved `game.phase`, saved `trainingCampResults`, the shared Team Ops receipt, and `advanceFranchiseWeek` as the only camp write path; no camp resolution, progression math, injury generation, save schema, sim, RNG, movement, or store action changed.
59. Training Camp route-coaching alignment and anchors - shipped: `/training-camp` Chip beats now target `Camp Readiness` and `Ops Carryover`; copy frames the route as a read-only camp report owned by `advanceFranchiseWeek` plus `buildTeamOpsImpactReceipt` instead of route-local drills. No camp resolution, progression math, injury generation, save schema, sim, RNG, movement, or route ownership changed.
60. Dynasty Save/Load spotlight anchors - shipped: `/dynasty` Chip beats now target `Portable Backup` and the Local Save Slots plus Import Cartridge restore grid; copy frames backup/restore as route guidance over existing handlers only. No cartridge build/export, local slot save/load/delete, import validation, sidecar import, save schema, sim, RNG, movement, or route ownership changed.
61. Settings spotlight anchors - shipped: `/settings` Chip beats now target `Simulation Preferences` and `Operations Source`; copy frames comfort/gameplay controls and facility/medical source boundaries as route guidance over existing handlers only. No settings toggle behavior, facility upgrade, medical hire, candidate refresh, injury recovery, training-camp resolution, save schema, sim, RNG, movement, or route ownership changed.
62. Mentors spotlight anchors - shipped: `/mentors` Chip beats now target the active/budget/effects summary and `Network Source`; copy frames alumni mentor state, compatible effects, Team Ops receipt, and Hire/Release ownership over existing handlers only. No mentor hire/release behavior, budget change, progression, CPU-team mentor behavior, normal mentoring-pair behavior, training-camp receipt, save schema, sim, RNG, movement, or route ownership changed.
59. Franchise Legends farewell-tour visibility - shipped: `/legends` renders saved `game.farewellTours` for the current user team in an `Active Farewell Tours` panel, adds the saved farewell-tour source to `Franchise Legend Sources`, and labels Player Profile's `startFarewellTour` action as the tour-start owner. No farewell-tour writer, retired-jersey writer, Hall of Fame logic, route registration, save schema, sim math, RNG, movement, or sidecar behavior changed.
60. Farewell-tour store schedule guard - shipped: `game-store.test.ts` now verifies that `actions.startFarewellTour` saves a tour with three or more scheduled future moments and a `final_game` moment from a live seed schedule, not just an empty top-level tour row. No store action, engine formula, schedule generation, save schema, sim math, RNG, movement, or sidecar behavior changed.
61. Current-save rivalry latest-meeting clarity - shipped: `/franchise` `RivalryHeatMap` now shows total games and latest saved meeting result/score/year/week from the same completed schedule/playoff results that drive W-L-T. No durable all-time field, persistence, sidecar, save schema, sim math, RNG, movement, or route registration changed.
62. Autosave toast week-baseline guard - shipped: `RootLayout` now uses `resolveAutosaveToastStep` to update the remembered week before showing the cosmetic autosave toast, preventing repeated same-week retriggers after a week-change render. No save schema, `SAVE_VERSION`, persistence writer, route registration, nav metadata, sidecar, sim math, RNG, movement, or package ownership changed.
63. Playtest phase-boundary schedule awareness - shipped: `detectPhaseBoundaries` now reads `getRegularSeasonWeekCount(context.state)` so generated/configured 17/18/19-week playtest frames are judged against their own regular-season boundary. Focused tests cover 19-week regular-season continuation and 17-week playoff start. No harness fixture, report shape, save schema, sim math, RNG, movement, or route behavior changed.
64. Owner refresh commit path - shipped: `refreshOwner` now clones the current game, lets `updateOwnerApproval` write the single owner-history receipt, and commits through `commitGame(nextGame)` so owner approval refreshes autosave through the standard durable store path. Focused store coverage proves clone safety, one history row, and `autosaveDynasty(nextGame)`. No save schema, owner approval formula, sim math, RNG, movement, or route behavior changed.
65. Set phase commit path - shipped: `setPhase` now clones the current game, writes only `game.phase`, and commits through `commitGame(nextGame)` so saved phase changes autosave through the standard durable store path. Focused store coverage proves clone safety and `autosaveDynasty(nextGame)`. No save schema, phase-transition formula, sim math, RNG, movement, or route behavior changed.
66. Setup progress commit path - shipped: `advanceSetup`, `goBackSetup`, `applySetupChoice`, and `toggleSetupDrilldown` now clone the current game and commit through `commitGame(nextGame)` so setup wizard progress autosaves through the durable store path. Focused store coverage proves setup choice, phase advance, drilldown, and back-step commits without source-object mutation. No save schema, setup helper formula, sim math, RNG, movement, or route behavior changed.
67. Contract/trade-block commit path - shipped: direct `restructure`, `backload`, and `toggleTradeBlock` actions now clone current state and commit through `commitGame(nextGame)`. Contract actions keep existing helper formulas, sync player-map contracts, refresh cap totals/league cap space, and autosave; trade-block toggles mirror into `GameState.players` when present. Focused store coverage proves autosave, cap sync, player-map sync, and source-object clone safety.
68. Direct saved-game mutation source guard - shipped: `game-store.source.test.ts` now fails if a `set((s) => ...)` block touches `s.game` outside `commitGame`, new-game/load/autosave hydration, or undo restore. No runtime behavior, save schema, sim math, RNG, movement, route registration, or package ownership changed.
69. Store navigation target route guard - shipped: `App.test.tsx` now scans hard-coded `navigateTo('...')` calls in `store/game-store.ts` and fails if a target is not registered in the `rootRoute.addChildren([...])` tree. No runtime behavior, route registration, nav metadata, save schema, sim math, RNG, movement, or package ownership changed.
70. Shipped scenario coverage catalog guard - shipped: `scenario-challenge.test.ts` now checks every shipped scenario from `getAvailableScenarios()` so each enabled constraint flag maps to an enforced `getScenarioConstraintCoverage` row with enforced paths and no uncovered paths. No scenario definition, enforcement, UI, save schema, sim math, RNG, movement, or package ownership changed.
71. Repeat-player Fast Lane launch gate - shipped: `NewGameScreen` renders Full Setup / Fast Lane setup-path choices for New Dynasty. Fast Lane unlocks only from browser-local `mfd:first-ten-completed`, selected starts persist `mfd:setup-run-mode`, and Fast Lane replaces only the initial setup state through `createFastLaneSetupState`. Scenario, convention demo, continue, and import flows remain unchanged; no save schema, route registration, sim, RNG, movement, or package ownership changed.
72. In-save scenario setup bypass - shipped: `actions.startScenarioChallenge` deletes `setupState` after `startScenarioEngine`, so `/scenarios` starts match NewGame scenario starts and enter the post-setup app instead of reopening first-run setup. Store coverage locks active scenario state, preserved user franchise identity, and setup bypass. No scenario definitions, constraints, grading, save schema, sim, RNG, movement, or route registration changed.
73. Long-Horizon Quality Benchmark Proof - shipped: `runPlaytest` accepts optional higher `maxSteps`, progress callbacks, and sampled save round-trip cadence while preserving the default 800-step fast/shadow cap; `quality-benchmarks.ts` defines opt-in `goat-25y` and `goat-50y` profiles with named anomaly budgets; and `scripts/playtest-report.ts --benchmark ...` can run, dry-run, or print per-season progress for full profile runs. Focused engine tests, engine typecheck, 25/50 dry-run contracts, and full 25/50 benchmark runs passed with 0 high anomalies and all budgets PASS.
74. GOAT Release Sentinel Benchmark - shipped: `quality-benchmarks.ts` defines `goat-release-sentinel`, a 4-season `SPEEDRUNNER` seed-42 profile with `MAX_PLAYTEST_STEPS`, `saveRoundTripEvery=10`, the same named budget areas as the opt-in GOAT stress profiles, roster-minimum budget 120, and zero high-severity tolerance. `scripts/release-gate.mjs` runs it as a `determinism` step, so `node scripts/release-gate.mjs --only goat-release-sentinel` gives release-gate long-horizon budget proof without promoting the 25/50-season profiles. Focused benchmark tests, release-gate tests, dry-run selection, and the focused gate passed; the benchmark completed 4/4 seasons, 113 weeks, 56 medium roster-minimum anomalies, 0 high, and all budgets PASS in 11.5s. Full 36/36 gate proof remains a future refresh when warranted.
75. Draft War-Room Live Order Ownership - shipped: accepted live current-year war-room offers now validate source pick ledgers, transfer real team `draftPicks`, update matching live `offseasonState.draftOrder` owner/id entries, and return the original state for stale/no-op accepts so the web store skips war-room rebuild, trade audio, commit, and autosave. Focused engine/store tests and engine/web typechecks passed; no save schema, `SAVE_VERSION`, offer generation, prospect generation, pick-value math, sim, RNG, route registration, UI layout, dependencies, sidecars, or web assets changed.
76. Draft War-Room Real-Backed Offer Generation - shipped: generated trade-down offers now require real live draft-order rows backed by source `draftPicks`, no longer synthesize fallback team/pick packages, and include future round sweeteners only when the source team owns a transferable future pick. Focused engine coverage proves live-backed offers still generate, missing source ledgers do not generate, and owned future sweeteners transfer through `applyDraftTradeOffer`; engine typecheck passed.
77. Draft War-Room Browser Accepted Offer Proof - shipped: `SMOKE_DRAFT_WAR_ROOM_TRADE=1` opens `/draft`, verifies a route-generated source-backed offer and `Draft Market Receipt`, accepts pick #9 plus an owned future round 3 sweetener for pick #1 through the rendered button, verifies live draft-order and `draftPicks` transfers in autosave, hard-reloads, and sees zero browser errors. This is smoke tooling and rebuilt assets only; no save schema, `SAVE_VERSION`, accepted-offer logic, generation logic, route source, sim, or RNG changed.
78. Draft War-Room Durable Accepted-Trade Receipt - shipped: accepted draft-war-room trades now append a deterministic `draft-trade-*` `leagueNews` trade item through existing `recordNewsItem` after validated pick transfers and live draft-order updates. Focused engine tests cover simple and future-sweetener receipt content, and `SMOKE_DRAFT_WAR_ROOM_TRADE=1` verifies the saved receipt after autosave hard reload. No new save field, schema, migration, `SAVE_VERSION`, route registration, sidecar, sim, or RNG changed.
79. Draft Post-Draft Recap CTA - shipped: `/draft` now renders `Draft Closure` during `phase = post_draft`, routes to `/draft-recap` when the current-year recap is saved, or calls existing `advanceWeek -> finalizePostDraft` before routing when the recap is not archived yet. Focused `DraftBoard` tests, web typecheck, production build, and `/draft` route smoke passed. No new save field, schema, migration, recap writer, draft mutation, sim, RNG, route registration, or sidecar changed.
80. Player Profile Draft Recap Signature Memory - shipped: `/player/$playerId` reads saved user-team `draftRecaps` through `selectDraftRecaps`, labels that source in `Profile Sources`, and renders a matching current-player pick as `Draft Class Memory` in `Signature Moments` with class grade, pick slot, projected pick, value delta, and verdict. Focused `PlayerProfile` tests, web typecheck, production build, and dynamic player-route smoke passed. No save field, schema, migration, recap writer, player-history writer, sim, RNG, route registration, movement, or sidecar changed.
81. Player Timeline Draft Recap Memory - shipped: `/player/$playerId/timeline` reads saved user-team `draftRecaps` through `selectDraftRecaps`, labels that source in `Timeline Sources`, and renders a matching route-player pick as `Draft Class Memory` with class grade, pick slot, projected pick, value delta, and verdict. Focused `PlayerTimeline` tests, web typecheck, production build, and dynamic timeline-route smoke passed. No save field, schema, migration, recap writer, timeline writer, sim, RNG, route registration, movement, or sidecar changed.
82. Draft Recap Player Profile Links - shipped: `/draft-recap` renders saved recap picks in best value, biggest reach, steals, league highlights, and the Pick By Pick table through `PixelPlayerLink` callbacks to the existing player profile route. Focused `DraftRecap` tests, web typecheck, production build, and `/draft-recap` route smoke passed. No save field, schema, migration, recap writer, draft mutation, profile/timeline writer, sim, RNG, route registration, movement, or sidecar changed.
83. Week Advance Rookie Class Follow-Up - shipped: `/week-advance` renders saved draft recap rookies in a read-only `Rookie Class Follow-Up` panel during training camp, preseason, or regular-season Weeks 1-4, with profile links and route-only CTAs to Player Development, Training Camp, and Draft Recap. Focused `WeekAdvance` tests, web typecheck, production build, and `/week-advance` route smoke passed. No save field, schema, migration, recap writer, training/depth mutation, week-advance behavior, sim, RNG, route registration, movement, or sidecar changed.
84. Draft Recap Class Follow-Through - shipped: `/draft-recap` renders selected saved recap picks in a read-only `Class Follow-Through` panel by joining de-duplicated top pick, best value, reach watch, and steal follow-up rows to current `game.players` and `selectTeams`. Focused `DraftRecap` tests, web typecheck, production build, and `/draft-recap` route smoke passed. No save field, schema, migration, recap writer, player-history writer, roster/contract/depth mutation, sim, RNG, route registration, movement, or sidecar changed.
85. Game Day Player Arc Follow-Up - shipped: `/game-day` renders saved top performers, record breakers, and milestone players in a read-only `Player Arc Follow-Up` panel by de-duplicating `GameDayPackage.topPerformers`, `recordsMoments`, and `milestoneMoments` by player id. It links players to profiles, keeps saved stat lines/narratives, and routes only to Player Development or Record Book. Focused `GameDayRecap` tests, web typecheck, production build, and `/game-day` route smoke passed. No save field, schema, migration, game-day package writer, player-history/timeline writer, record/milestone writer, scrapbook writer, sim, RNG, route registration, movement, or sidecar changed.
59. Player-profile source clarity, signature moments, selector stability, and hash links - shipped: `/player/$playerId` renders Profile Sources from `selectPlayerProfileBundle`, season/profile rows, `playerArchive`, saved `playerRivalries`, saved user-team `txLog` rows, and saved user-team `draftRecaps`. It also renders `Signature Moments` from existing awards, records, profile career stats, rivalries, transaction rows, matching draft-recap picks, and lineage/archive context; `selectPlayerRivalries(playerId)` is memoized by player id and `state.game` identity; and `PixelPlayerLink` hash-routes reusable player links to `/player/:id`. No save schema, durable history, draft-recap/award/record/transaction/rivalry mutation, sim, RNG, sidecar, or movement changed.
60. Schedule source clarity - shipped: `/schedule` renders Schedule Sources from `selectTeamSchedule`, `selectWeekSchedule(selectedWeek)`, saved broadcast/flex/result fields, and byes, with a display-only boundary. Opening the route does not generate schedules, assign broadcasts, flex games, weather, results, records, news, or game-day packages.
61. Standings source clarity and dynamic seed display - shipped: `/standings` renders Standings Sources from `selectStandings`, `selectPlayoffPicture`, `selectStatLeaders`, and local division/seed/streak signals, derives its seed-count badge from playoff-picture output, and marks every displayed playoff seed after the locked top group as bubble. Opening the route does not simulate games, advance weeks, write playoff brackets, update power rankings, create records, or generate news/social posts.
62. Analytics source clarity, win-rate label, and turnovers-per-game label - shipped: `/analytics` renders Analytics Sources from `selectAdvancedStats`, `selectAnalyticsLeaders`, `selectRoster` plus `calculatePlayerEfficiency`, `selectWeeklyTrend`, and `selectPlayerComparison`, with a display-only boundary. Its Team Overview labels `successRate` as `Win Rate` because `calculateAdvancedStats` divides team wins by games played, and labels `turnoverRate` as `Turnovers / Game` because `calculateAdvancedStats` divides turnovers lost by games played. Opening the route does not recalculate season stats, advance weeks, write player history, create records, update rankings, or generate news/social posts.
63. Analytics/Stat Central route coaching - shipped: `/analytics` and `/stat-central` resolve to unanchored `analytics-evidence` Chip beats that frame both screens as evidence boards over live stats, histories, and derived helpers without writing stats, records, rankings, news, history rows, or archive entries.
64. Scenario Constraint route coaching - shipped: `/scenarios` resolves to unanchored `scenario-constraints` Chip beats that point to active locks, enforcement badges, safe planning lanes, and the challenge-start confirmation boundary without expanding scenario enforcement or starting a challenge.
65. Locker Room route coaching - shipped: `/locker-room` resolves to unanchored `locker-room` Chip beats that point users to culture, tension, captain, and cooldown context before spending meeting or rally actions.
66. Front Office route coaching - shipped: `/front-office` resolves to unanchored `front-office` Chip beats that point users to contract selection, pure projections/previews, forecast warnings, and the Apply/Cut commit boundary.
66. Front Office Contract Action Receipts - shipped: `/front-office` now renders a route-local `Contract Action Receipt` after existing restructure, backload, standard-cut, or post-June-1 cut actions resolve, labeling preview cap/dead-money context, commit path, state touched, and no-extra-write boundaries without changing contract formulas or movement behavior.
66. Cap Lab Execution Receipts - shipped: `/cap-lab` now renders a route-local `Cap Lab Execution Receipt` after existing `actions.executeCapMoves(sandbox)` resolves, labeling queued moves, preview cap-space result, warnings, commit path, state touched, and no-extra-write boundaries without changing cap formulas, batch execution, social-post behavior, saves, sim, RNG, or movement rules.
67. Mentors route coaching - shipped and now anchored by Mentors spotlight anchors: `/mentors` resolves to `mentors` Chip beats that point users to active slots, budget, compatible roster effects, saved mentor state, and the Hire/Release commit boundary.
68. Endorsements route coaching - shipped: `/endorsements` resolves to unanchored `endorsements` Chip beats that point users to active deals, requirements, pending offseason offers, and the Accept/Decline commit boundary.
69. Commissioner route coaching - shipped: `/commissioner` resolves to unanchored `commissioner-governance` Chip beats that point users to active proposals, CBA status, rule context, and the Petition/Vote commit boundary.
70. Awards route coaching and spotlight anchors - shipped: `/awards` resolves to `awards-hub` Chip beats that point users to saved award classes, winners/runners-up comparison, and the route-local season scrubber; both beats now have unique Awards Hub spotlight anchors.
71. Franchise Legends route coaching - shipped: `/legends` resolves to unanchored `franchise-legends` Chip beats that point users to saved franchise history, rings, rafters, Hall rows, coaches, eras, and the local Hall detail modal boundary.
72. Player Comparison route coaching - shipped: `/compare` resolves to unanchored `player-comparison` Chip beats that point users to current contracted player reads, Player A's stat lens, active-cap contract grade rows, cap-hit rows, and the no-profile/no-record write boundary.
73. Player Rivalries route coaching - shipped: `/rivalries` resolves to unanchored `player-rivalries` Chip beats that point users to saved feud intensity/tier/history/team context, saved social-post trash-talk projection, and Week Advance ownership of feud creation, decay, and sim-bonus context.
74. Player Profile route coaching - shipped: dynamic `/player/$playerId` profile routes resolve to unanchored `player-profile` Chip beats that point users to Profile Sources, Career Memory, Signature Moments, Transaction Memory, owner-workflow action buttons, and the display-only no history/archive/award/record/contract/rivalry write boundary. Concrete `/player/<id>` paths are covered while `/player/<id>/timeline` stays uncoached for its own slice.
75. Player Timeline route coaching - shipped: dynamic `/player/$playerId/timeline` routes resolve to unanchored `player-timeline` Chip beats that point users to archived seasons, current-season rows, transaction memory, awards/highlights, Back To Profile and Stat Central navigation, and the display-only no season/record/transaction/archive write boundary.
76. League Weather route coaching - shipped: `/league/weather` resolves to unanchored `league-weather` Chip beats that point users to saved schedule weather, route-local forecast profiles, and Week Advance/Game Day ownership of generated weather, saved results, and receipts.
77. Player Development route coaching - shipped: `/player-development` resolves to unanchored `player-development` Chip beats that point users to selected roster player reports, projection curves, breakout candidates, coach-context narrative, and the no progression/training-camp/archetype/coach-effect/player-history write boundary.
78. Draft Recap route coaching - shipped: `/draft-recap` resolves to unanchored `draft-recap` Chip beats that point users to saved recap rows, class grade, best value, reaches, steals, league highlights, route-local year selection, `finalizePostDraft` writer ownership, and the no render-time generation/repair boundary.
79. Relocation route coaching - shipped: `/relocate` resolves to unanchored `relocation` Chip beats that point users to eligibility, destination cost, market, fanbase, prestige, impact preview reads, and the confirmed `Move Franchise` commit path through `relocateTeam`.
63. Depth Chart source clarity, starter commit persistence, and starter-target semantics - shipped: `/depth-chart` renders Depth Chart Sources from `selectRoster`, saved `Team.roster[].isStarter` flags, durable `setStarter`, `detectPositionBattles(roster)`, `selectSpecialTeams`, returner assignment actions, and a display-only boundary. `setStarter` commits roster starter flags, mirrors `GameState.players[playerId].isStarter` when present, and completes depth-chart tutorial progress on promotes. `apps/web/src/lib/depth-chart-starters.ts` owns the shared 22-flag shell/depth-chart urgency target, which is not exact formation validation. Opening the route does not auto-set starters, auto-assign special teams, simulate games, distribute snaps, change contracts, enforce lineup legality, update roles, or write player history.
64. Watch List source clarity - shipped: `/watch-list` renders Watch List Sources from browser-local `mfd.watchlist.v1`, `buildWatchListRows` row resolution, separate saved scouting/FA watchlists, board-level `updatedAt`, and a display-only boundary. Opening the route does not mutate `GameState`, move players, change saved scouting or FA watchlists, alter draft/free-agent boards, or write player/archive records.
65. Locker Room source clarity and action receipts - shipped: `/locker-room` renders Locker Room Sources from `selectLockerRoom`, saved `team.lockerRoom`, existing meeting/rally/captain action owners, route-local `eligibleBench`, `CAPTAIN_PERK_EFFECTS`, and a display-only boundary. Meeting/rally actions commit saved locker-room state through the existing store paths and then render route-local receipts; opening the route itself does not tick weekly culture, resolve tensions, call meetings, trigger rallies, elect captains, write narratives/events, or apply sim bonuses.
66. Inbox source clarity - shipped: `/inbox` renders Inbox Sources from `buildInboxMessages`, selector-gathered route inputs, the shell badge boundary where `computeNavBadges` does not badge `/inbox`, local `selectedMsg`/filter/apology-modal state, CTA navigation boundaries, the owner-personality mailbox split, regenerated read flags, and a display-only boundary. The apology-tour replay modal also renders `Tour Sources` for the saved thread, authored beat copy, modal-local beat index, and delivery ledger. Opening the route or replaying the modal does not advance weeks, resolve actions, mutate proposals, clear waivers, write news/social, mark messages read, write sidecars, alter shell badges, or run sim/RNG.
67. Monday Briefing source clarity, Team Ops Carryover, and Action Center post-week receipt - shipped: `/` renders Briefing Sources from `MondayBriefing` selectors, saved `game.dashboardState`, widget read models, `ActionCenter` guidance inputs, local customize-modal drafts, and a display-only boundary. It also renders read-only `Team Ops Carryover` from `buildTeamOpsImpactReceipt(game, team.id)` over saved facility, medical, mentor, and training-camp state, plus a compact saved last-week receipt from `buildPostWeekMoment(game.weekSummaries.at(-1), latest game-day package)` with a `/week-advance` full-deck CTA. Opening the route does not advance weeks, simulate games, generate weather/news, update power rankings, save layouts, pin widgets, write achievements, upgrade facilities, hire staff, resolve camp, move players, build packages, mutate reports, or run sim/RNG.
68. Command-palette roster-player search and keyboard ownership - shipped: `RootLayout` passes `Search screens, actions, roster players...` into `MfdCommandPalette`, builds app commands from `NAV_ITEMS` screens, capped `selectRoster` player commands that navigate to `/player/$playerId`, and `action-advance-week`, then owns `Cmd/Ctrl+K` open/close toggling through `toggleCommandPalette` while disabling the palette's default document listener with `globalShortcutEnabled={false}`. Reusable team category support remains design-system capacity until a real team-detail command route is chosen.
69. Production component navigate target guard - shipped: `App.test.tsx` source-scans production `apps/web/src` literal `navigateTo('/...')` calls and fails if any component/helper target is missing from the registered route tree. The prior store `navigateTo` guard remains. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, or package ownership changed.
70. Production router navigate target guard - shipped: `App.test.tsx` source-scans production literal TanStack router `navigate({ to: '/...' })` / `router.navigate({ to: '/...' })` calls and fails if any component/app-shell target is missing from the registered route tree. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, or package ownership changed.
71. Production hash-helper navigate target guard - shipped: `App.test.tsx` source-scans production hash-helper `navigateTo('/...')` / `goTo('/...')` call sites and fails if any local hash-navigation wrapper target is missing from the registered route tree. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, or package ownership changed.
72. Dynamic route template target guard - shipped: `App.test.tsx` source-scans production template route targets from router navigation, hash helpers, and literal `location.hash` assignments. Template segments such as `${playerId}` only pass where the matching registered route segment is dynamic, keeping `/player/${playerId}` and `/player/${player.id}/timeline` aligned with `/player/$playerId` and `/player/$playerId/timeline`. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, UI layout, sidecar, or package ownership changed.
73. AGM recommendation route target guard - shipped: `App.test.tsx` source-scans engine AGM recommendation `targetRoute` literals in `packages/engine/src/systems/agm.ts` and fails if Monday Briefing's variable AGM CTA route values are not registered in the app route tree. No runtime behavior, engine behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, UI layout, sidecar, or package ownership changed.
74. Command deck route object guard - shipped: `App.test.tsx` source-scans Monday Briefing `ActionCenter` `route` values and Week Advance `fixRoute` values, failing if their variable command/fix CTA routes drift outside the registered app route tree. No runtime behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, UI layout, sidecar, or package ownership changed.
75. Offseason calendar route object guard - shipped: `App.test.tsx` source-scans `selectOffseasonCalendar` route values from `apps/web/src/app/store/selectors.ts`, failing if `/week-advance` offseason calendar CTA routes drift outside the registered app route tree. No runtime behavior, selector output, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, UI layout, sidecar, or package ownership changed.
76. Inbox message link route guard - shipped: `App.test.tsx` source-scans Inbox `buildInboxMessages` `link` values and `InboxTriage` selected-route fallbacks, failing if variable Inbox CTA routes drift outside the registered app route tree. No runtime behavior, inbox output, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, UI layout, sidecar, or package ownership changed.
77. EmptyState actionRoute prop guard - shipped: `App.test.tsx` source-scans literal JSX `actionRoute` props that feed shared `EmptyState` variable `navigateTo(actionRoute)` CTAs, failing if an empty-screen CTA route drifts outside the registered app route tree. No runtime behavior, EmptyState behavior, route registration, nav metadata, save schema, `SAVE_VERSION`, sim, RNG, movement, UI layout, sidecar, or package ownership changed.
78. Roster selected-player action receipts - shipped: `/roster` renders a route-local `Roster Action Receipt` after existing selected-player Cut, Trade Block toggle, Restructure, Place on IR, or Activate from IR commits resolve. Receipts label the result, touched roster/player/waiver/cap/autosave state, exact store/engine commit source, and no-extra-write boundary without adding durable receipt history, save shape, `SAVE_VERSION`, roster movement rules, cap/contract formulas, route registration, sidecars, sim math, RNG, report shape, or package ownership changed.
79. League weather source labels - shipped: `/league/weather` card rows now distinguish saved `schedule.weather`, stadium-dome fallback, and UI-only forecast-profile sources, and `WeatherForecast.test.tsx` covers the exported schedule-to-forecast helper. This changes no weather generation, saved weather key space, forecast-profile formula, schedule write, saved result, save schema, `SAVE_VERSION`, sim math, RNG, route registration, sidecars, or package ownership.
80. Revenge Lines Loader Wiring - shipped: `packages/content/narrative/revenge-lines.json` is no longer dormant; it is schema-validated by `RevengeLinesContentSchema`, loaded through `content-loader.ts`, exported by `getRevengeLineTemplates`, and consumed by `buildBroadcastCommentary` for revenge pregame/in-game/recap flavor with deterministic selection/interpolation. No save schema, `GameState`, route registration, sim math, RNG state, movement, sidecar, or package ownership changed.
81. Revenge Newsline Recap Consumption - shipped: `buildBroadcastCommentary` consumes the loader-owned `postgame.newsline` bucket as recap `Newswire headline:` texture only when the revenge subject's current team beats the former team. No saved news rows, broadcast persistence, save schema, `GameState`, route registration, sim math, RNG state, movement, sidecar, or package ownership changed.
82. Trick-Play Planning Boundary Clarity - shipped: `/game-plan` labels selected trick-play ids as planned weekly-prep data in `Weekly Prep Sources` and trick-play tab copy, while `trick-plays.test.ts` still proves `game-sim.ts`, `game-flow.ts`, and `franchise-week.ts` do not execute trick-play helpers. No save schema, `GameState`, route registration, sim math, RNG state, movement, sidecar, or package ownership changed.
83. Trade Center Near-Miss Boundary Clarity - shipped: `/trades` labels generated offer rejections as offer-status-only and direct proposal/counter rejections as possible `nearMissTracker.declinedTrades` inputs only when the user requested partner players. No tracker logic, trade formula, movement, save schema, `GameState`, route registration, sim math, RNG state, sidecar, or package ownership changed.
84. Inbox Shell Badge Boundary Clarity - shipped: `/inbox` labels that shell `computeNavBadges` covers trades, depth chart, game plan, and handshakes only; Inbox urgent and decision counts are route-local header/filter counts, not top-nav or mobile-tab badges. No shell badge derivation, inbox generation, save schema, `GameState`, route registration, sim math, RNG state, movement, sidecar, or package ownership changed.
85. Scenario Sources Boundary - shipped: `/scenarios` labels the engine scenario catalog read, `getScenarioConstraintCoverage` display helper, explicit `actions.startScenarioChallenge -> createSeedGameState -> startScenario -> commitGame` start path, and no-render-write boundary. No scenario definitions, enforcement paths, start behavior, save schema, `GameState`, route registration, sim math, RNG state, movement, sidecar, or package ownership changed.
86. Call Your Shot Result Source Boundary - shipped: Game Day's `CallYourShotResult` panel labels `resolveCallYourShot`, saved `GameResult.callYourShotResult`, recap `GameDayPackage.callYourShotResult`, and the read-only display boundary. No call-your-shot math, week advance, save shape, `GameState`, route registration, sim, RNG, movement, sidecar, audio, or package ownership changed.
87. Chip Onboarding Route Target Guard - shipped: `App.test.tsx` source-guards `FIRST_TEN_MINUTE_ONBOARDING_BEATS[*].route` from `apps/web/src/features/companion/onboardingMachine.ts` against the registered `App.tsx` route tree. This is test/docs only; no runtime behavior, route registration, save shape, `GameState`, sim, RNG, movement, sidecar, or package ownership changed.
88. Chip Onboarding Spotlight Anchor Guard - shipped: `spotlightAnchors.test.ts` resolves first-ten onboarding routes through `resolveRouteKey` and requires each non-null `FIRST_TEN_MINUTE_ONBOARDING_BEATS[*].spotlightTarget` to appear exactly once on its owning route screen. This is test/docs only; no runtime behavior, route registration, save shape, `GameState`, sim, RNG, movement, sidecar, or package ownership changed.
89. Complete-dynasty sidecar archive - shipped: `/dynasty` exports/imports `mfd.dynastySidecars.archive.v1`, bundling `mfd.hallOfFame.v1`, `mfd.scrapbook.v1` including pending playoff lore, `mfd.rookieOfYear.v1`, `mfd.rosterContinuity.v1`, `mfd.careerMeta.v1`, and current-save `mfd.rivalries.v1` derived rivalry heat. Import validates every included payload before replacing sidecars, defaults older sidecar archives without rivalry data to an empty derived payload, and stays separate from `.mfd` cartridges, Dexie slots, `GameState`, autosave, save schema, `SAVE_VERSION`, sim, RNG, movement, and route registration. Do not redo complete sidecar portability; future sidecar work should add a genuinely new archive scope with validation or a separate persistence safety case.
90. Standalone PHI Stadium Tradition - shipped: `packages/content/stadiums/phi-liberty-bell.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('PHI')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, or package ownership changed.
91. Standalone BAL Stadium Tradition - shipped: `packages/content/stadiums/bal-crab-pot.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('BAL')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, or package ownership changed.
92. Standalone SF Stadium Tradition - shipped: `packages/content/stadiums/sf-mother-dough.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('SF')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, or package ownership changed.
93. Standalone SEA Stadium Tradition - shipped: `packages/content/stadiums/sea-feedback.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('SEA')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, or package ownership changed.
94. Standalone DET Stadium Tradition - shipped: `packages/content/stadiums/det-bass-drop.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('DET')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
95. Standalone CHI Stadium Tradition - shipped: `packages/content/stadiums/chi-deep-freeze.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('CHI')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
96. Standalone PIT Stadium Tradition - shipped: `packages/content/stadiums/pit-furnace.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('PIT')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
97. Standalone NYC Stadium Tradition - shipped: `packages/content/stadiums/nyc-meter.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('NYC')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
98. Standalone BOS Stadium Tradition - shipped: `packages/content/stadiums/bos-kettle.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('BOS')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
99. Standalone CLE Stadium Tradition - shipped: `packages/content/stadiums/cle-power-chord.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('CLE')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
100. Standalone DAL Stadium Tradition - shipped: `packages/content/stadiums/dal-corral.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('DAL')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
101. Post-Setup Route Smoke Matrix - shipped: `scripts/smoke-test-post-setup-route.mjs` now keeps the legacy `SMOKE_POST_SETUP_ROUTE` / `SMOKE_POST_SETUP_TEXT` single-route override and adds `SMOKE_POST_SETUP_ROUTES_JSON` for a validated list of initialized route/text checks after one convention-demo launch. Parser tests cover defaults, legacy overrides, matrix parsing, and malformed input; the live smoke was run across `/league/weather`, `/roster`, and `/depth-chart`. No app runtime behavior, route registration, save schema, `GameState`, sim math, RNG, movement, sidecar, or package ownership changed.
102. Standalone ATL Stadium Tradition - shipped: `packages/content/stadiums/atl-orchard.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('ATL')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
103. Standalone CIN Stadium Tradition - shipped: `packages/content/stadiums/cin-sty.json` is schema-validated, statically imported by `content-loader.ts`, preferred by `getTeamStadiumContent('CIN')` before embedded team-stadium fallback, and consumed by broadcast pregame copy with cue lines. This is read-only authored content plus tests/docs; no `GameState`, save schema, migration, route registration, sim math, RNG, movement, sidecar, audio behavior, or package ownership changed.
104. Week-Advance Browser Smoke Action - shipped: `scripts/smoke-test-post-setup-route.mjs` now supports `SMOKE_ADVANCE_WEEK=1`, keeps the default route smoke behavior, launches the convention demo once, opens `/week-advance`, clicks the actual advance action, and verifies a post-advance route state without browser errors. Parser tests lock the opt-in env flag. No app runtime behavior, route registration, save schema, `GameState`, sim math, RNG, movement, sidecar, or package ownership changed.
105. Portable Cartridge Browser Round Trip - shipped: `scripts/smoke-test-post-setup-route.mjs` now supports `SMOKE_CARTRIDGE_ROUND_TRIP=1`, stubs clipboard capture in the temporary browser profile, copies a `/dynasty` cartridge, advances the demo away from the exported week, resolves the halftime gate with Stick when it appears, pastes the captured backup into `Import Backup Code`, and verifies Week 14 is restored. The existing week-advance smoke now treats halftime as an intermediate gate before a committed advance. Parser tests lock the new opt-in env flag. No app runtime behavior, route registration, save schema, `GameState`, sim math, RNG, movement, sidecar, or package ownership changed.
106. Local Save Slot Browser Round Trip - shipped: `scripts/smoke-test-post-setup-route.mjs` now supports `SMOKE_LOCAL_SAVE_SLOT_ROUND_TRIP=1`, creates a manual Week 14 save slot on `/dynasty`, advances the demo away from it, resolves the halftime gate with Stick when it appears, selects the manual slot, confirms `Load`, and verifies Week 14 is restored. Parser tests lock the new opt-in env flag. No app runtime behavior, route registration, save schema, `GameState`, sim math, RNG, movement, sidecar, or package ownership changed.
107. Cartridge File Browser Round Trip - shipped: `scripts/smoke-test-post-setup-route.mjs` now supports `SMOKE_CARTRIDGE_FILE_ROUND_TRIP=1`, captures the actual `Download .mfd` Blob in the temporary browser profile, advances the demo away from it, resolves the halftime gate with Stick when it appears, imports the captured Blob back through the hidden upload input as a `File`, and verifies Week 14 is restored. Parser tests lock the new opt-in env flag. No app runtime behavior, route registration, save schema, `GameState`, sim math, RNG, movement, sidecar, or package ownership changed.
108. Direct Post-Import Route Smoke - shipped: `scripts/smoke-test-post-setup-route.mjs` now supports `SMOKE_POST_IMPORT_ROUTE_SMOKE=1`, defaults to the copied-cartridge import path, then opens `/contracts` and verifies `Contract Sources` plus the restored Week 14 state in the same app session. `SMOKE_POST_IMPORT_ROUTE` / `SMOKE_POST_IMPORT_TEXT` override the post-import route/text pair, and parser tests lock defaults, overrides, and malformed inputs. No app runtime behavior, route registration, save schema, `GameState`, sim math, RNG, movement, sidecar, or package ownership changed.
109. New Dynasty Setup Entry Smoke - shipped: `scripts/smoke-test-post-setup-route.mjs` now supports `SMOKE_NEW_DYNASTY_SETUP_ENTRY=1`, branches before the convention-demo route flow, clicks `Start Dynasty` from a clean profile, and verifies the setup shell/header/content/stepper plus cold-open and next-decision copy. Parser tests lock the opt-in env flag. No app runtime behavior, route registration, save schema, `GameState`, sim math, RNG, movement, sidecar, or package ownership changed.
110. Contract Restructure Browser Smoke - shipped: `scripts/smoke-test-post-setup-route.mjs` now supports `SMOKE_CONTRACT_RESTRUCTURE=1`, opens `/contracts` from the convention demo, selects a clear contract row using existing PixelTable row/cell attributes, clicks `Restructure`, and verifies the same row becomes `Restructured`. Parser tests lock the opt-in env flag. No app runtime behavior, route registration, save schema, `GameState`, sim math, RNG, movement, sidecar, or package ownership changed.
111. Post-Import Hard Reload Persistence - shipped: launch-screen and `/dynasty` validated text/file imports now write the imported game through `autosaveDynasty(imported)` before `actions.loadGame(imported)`, keeping storage failures non-mutating and leaving `loadGame` itself non-autosaving. `SMOKE_POST_IMPORT_HARD_RELOAD=1` reloads after import, clicks `Continue Latest Autosave`, and verifies `/contracts` plus Week 14 survive; both copied-cartridge and `.mfd` import variants passed. No save schema, `SAVE_VERSION`, migration, route registration, sim math, RNG, movement, sidecar schema, or package ownership changed.
112. Contract Backload Browser Smoke - shipped: `scripts/smoke-test-post-setup-route.mjs` now supports `SMOKE_CONTRACT_BACKLOAD=1`, opens `/contracts` from the convention demo, tries clear contract rows until the existing `Add Void Years` action changes the same row's cap hit, and verifies `Cap Space` remains visible. Parser tests lock the opt-in env flag; live smoke backloaded Jalen Banks-QB1 from `$9.3M` to `$6.8M`. No app runtime behavior, route registration, save schema, `GameState`, contract formula, sim math, RNG, movement, sidecar, or package ownership changed.

Subsystem safety cards:
- Save/GameState: read "Persistence And Data Ownership". Persistent fields require TypeScript types, Zod schema, migration chain, `SAVE_VERSION`, deterministic new-save defaults, old-save fixtures/tests, and docs. Zod defaults are parse-time compatibility, not real seed defaults. Successful portable copy/download records `lastPortableExportYear`; manual local slots and the app-shell save reminder do not. `DynastyCartridge.source.test.ts` source-guards cartridge export receipt ordering and manual-slot separation. `validateGameState` is a pure dev/test/debug validator, not a universal commit gate or repair pass; it checks roster size, OVR validity/bounds, duplicate roster players, regular-season games-played sanity, numeric cap fields, roster players missing from `game.players`, roster/global `teamId` agreement, and live player-pool ownership across rosters, practice squads, free agency, and the waiver wire, but not cap formula correctness or retired/archive-style null-team records outside live pools.
- Save fixture inventory: checked-in historical fixtures currently include `v1.json`, `v10.json`, `v20.json`, `v30.json`, `v31.json`, `v32.json`, `v33.json`, and `v34.json`; v35/current-v36 coverage is generator-backed. Do not mistake `v33.json` for current source truth.
- Synthetic engine fixtures: `packages/engine/src/systems/test-helpers.ts` is test-only, not the production seed factory. If changing fixture defaults, generated current-save policy, broad subsystem initialization, or `makeLeagueState`, keep `test-helpers.test.ts`, `golden-saves.test.ts`, `save-version-drift.test.ts`, the guide, the plan, and this prompt aligned.
- Cartridges/Dexie/sidecars: preserve old plain-JSON `mfd-cartridge.v1` compatibility. Keep browser-local sidecars out of GameState unless doing a deliberate migration/import/export slice. Failed imports must be non-mutating. Wall-clock timestamps are metadata/startup only. Portable export payloads are built before receipt recording, so the just-created cartridge is pre-receipt state while browser current/autosave state receives the receipt afterward. `persistence.ts` owns parse -> migrate -> schema validation -> `ensureAgentsInitialized`; Dexie stores plain cartridge strings plus metadata; `newGame` clears incoming-dynasty sidecars and immediately autosaves, while `loadGame` syncs career meta without autosaving or clearing sidecars. Explicit launch-screen and `/dynasty` text/file import handlers autosave the validated imported game before calling `loadGame`, so `Continue Latest Autosave` survives a hard reload after import; failed validation or autosave write failure leaves the current active game unchanged. `/dynasty` complete sidecar archives use separate `mfd.dynastySidecars.archive.v1` JSON, validate every included sidecar before replacement, and do not call `loadGame`, write Dexie save slots, or alter `.mfd` compatibility. Normal New Dynasty keeps seed `setupState` and enters the App setup gate; launch scenarios and in-save `/scenarios` starts delete `setupState`; convention demo uses a dedicated schema-validated Week 14 state with no `setupState`; Fast Lane New Dynasty is now a repeat-player setup shortcut gated by browser-local `mfd:first-ten-completed` and swaps only the initial setup state before `newGame`.
- RNG/sim math: all sim randomness flows through `packages/engine/src/rng`. Do not use `Math.random()` in sim code. Name constants, show before/after formulas, include 3-5 sample outputs, sanity-check min/max/typical ranges, and add deterministic tests. Keep `game-sim.test.ts`, `game-sim.balance.test.ts`, `game-sim-types.test.ts`, and `season-stats.test.ts` green when changing sim output, stat application, context fields, or imports. Team season stats are persistent broad-consumer data: live mutation flows through `franchise-week-helpers.ts` after `applyPlayerLines`, annual reset happens in `finalizePostDraft`, and standings/analytics/records/awards/news/reports/history consume `team.seasonStats`. `tickInjuries` is exported/tested but not production-wired; current weekly recovery uses `processInjuryRecovery`. The playtesting harness has a synthetic 18-week fixture for benchmark/shadow stability; do not treat it as live schedule-rule evidence.
- Weather/atmosphere: saved weather is only `dome`, `clear`, `rain`, `snow`, and `wind`; `WeatherForecast.tsx` adds UI-only glyph/profile variants like `HEAT_WAVE`, temperatures, wind MPH, and impact tiers from saved schedule state and home city. `regional-weather.ts` owns team-region odds and generated weather; unknown team ids fall back to temperate; dome stadiums/regions always return `dome`; `ensureWeeklyWeather` writes only missing regular-season matchup weather using `RNG.play`. The older exported `generateWeatherForGame` helper is not the inspected live regular/playoff path. Playoff simulation currently generates weather directly into sim/result payloads instead of pre-persisting it on playoff matchups. `game-sim.ts` owns rain/snow/wind gameplay penalties, and `atmosphere.ts` owns sim-affecting crowd/home-field bonuses. Monday Briefing uses `selectWeather` for snow/wind/rain/standard copy; Inbox creates rain/snow/wind weather advisories; Game Day packages copy `GameResult.weather`; `/league/weather` does not call the generator, maps missing saved weather to DOME/HEAT_WAVE/SUNNY presentation, and now labels saved `schedule.weather` versus UI-only profiles. Treat formula changes as balance work with `regional-weather.test.ts`, `franchise-week-helpers.test.ts`, `game-sim.balance.test.ts`, `atmosphere.test.ts`, `WeatherForecast.test.tsx`, `weatherGlyphSvg.test.tsx`, and UI read-model tests as appropriate.
- Halftime/broadcast: halftime choices are save-backed, deterministic, and sim-affecting. `previewHalftimeDecision` runs a cloned preview sim for the user matchup; `stick` has no modifier; `switch` applies second-half game-plan direction plus `-1` first-drive / `+1` sustained team OVR deltas; `gamble` applies `+3` first-drive / `-2` later-drive deltas. `HalftimeDecision.tsx` copy now matches those engine constants. The selected choice is visible through `GameDayPackage.activeEffectSummaries`; `buildHalftimeDecisionReceipt` parses saved `Halftime hell:` rows; and `GameDayRecap`, `GameBroadcast`, `PlayByPlay`, and `BroadcastPresentation` consume that read model. There is still no dedicated typed halftime receipt save field or broadcast-generation payload; that would be a separate schema/generation slice. The trade-deadline ordering is now rule-aware across halftime suppression, engine deadline initialization, and store post-engine interruption routing.
- Week advance/store: preserve clone -> engine/helper -> `commitGame` patterns, autosave/undo semantics, `sim.ts` async boundary, memoized selectors, and stored-vs-derived ownership. `commitGame` honors browser-local `autosaveEnabled`, selected undo snapshots are JSON-cloned, week advance is intentionally non-undoable, and direct `set` exceptions are not templates for saved gameplay mutations. Generated 17/18/19-week schedules drive the regular-season cutoff, full team schedule rows, schedule route dropdowns, weekly-summary phase labels, derived rivalry playoff-history boundaries, and playoff start through `getRegularSeasonWeekCount`; do not reintroduce fixed Week 18/19 playoff-boundary constants. `buildPostWeekMoment` is the pure Week Advance receipt read model over saved `WeeklySummary` plus latest `GameDayPackage`; it now projects package stakes, rivalry heat, special teams, podium, and coaching notes without writing package content. `buildRookieClassFollowUp` is a pure Week Advance display helper over saved `draftRecaps` for opening-window route CTAs; it must not assign training, mutate depth charts, advance weeks, or generate recaps. `GameDayRecap` labels saved `GameDayPackage.pressConference` with a read-only `Podium Receipt` tied to that podium hook. `buildSeasonSchedule` is deterministic and rule-sized; live flex/broadcast mutation belongs to `franchise-week.ts` via `flexSchedule`/`assignBroadcasts`, while `getFullSchedule`/`getWeekSchedule` are selector read models and `TeamSchedule.tsx` is display-only. Playoff brackets are seeded from current records/divisions/point differential plus `playoff_seeds_per_conf`, and `advancePlayoffBracket` mutates/appends rounds lazily. `playoffMomentum` is saved and sim-affecting through team OVR bonuses before playoff `simulateGame`, while `playoffBracket` is still `z.any().nullable()` in the save schema. Keep `post-week-moment.test.ts`, `WeekAdvance.test.tsx`, `franchise-week-helpers.test.ts`, `franchise-week.test.ts`, `playoff-bracket.test.ts`, `playoff-momentum.test.ts`, `store/sim.test.ts`, and the architecture-boundary direct-sim-call guard green when changing week helper glue.
- App shell/routes: `App.tsx` owns major route/nav/shell wiring. Keep hash history for GitHub Pages. `apps/web/src/app/currentAppRoute.ts` owns App/Chip route-context normalization and strips the `/MFD/` deploy base when no hash route is present; `PostSetupApp`, `useChipEvents`, `ChipDock` fallback route handling, and `ErrorBoundary` fallback context reuse it. Keep `currentAppRoute.test.ts`, companion tests, and `ErrorBoundary.test.tsx` aligned with hash/base behavior. Reusable player-card links use `PixelPlayerLink` and must hash-route to `/player/:id`, producing `/MFD/#/player/:id` under the deploy base. Update route registration, `NAV_ITEMS`, `NAV_GROUPS`, mobile behavior, command palette, nav badge inputs, tutorial coverage, route coaching, direct-route tests, and spotlight anchors together. Monday Briefing is registered at `/`; Week 1 tutorial now targets `/` with `screen:/`, `TutorialStepSchema` normalizes stale serialized `week1-briefing` rows during save validation, and `/briefing` remains only a route-coaching alias. Canonical league analytics/governance routes include `/league-pulse`, `/stat-central`, `/cba`, and `/league-rules`; do not use stale `/league/pulse` or `/stats` spellings. Shell nav badges currently cover trades, depth-chart starters, game-plan phase urgency, and active handshakes only; `/inbox` labels that no-shell-badge boundary in Inbox Sources, and Chip pending decisions are also separate. App command-palette items currently come from `NAV_ITEMS`, capped selector-backed user-roster player commands, plus `action-advance-week`; team command search is not wired yet. `Cmd/Ctrl+K` is app-owned through `toggleCommandPalette`, and `RootLayout` disables the reusable palette's default shortcut listener with `globalShortcutEnabled={false}`. Mobile primary tabs are hard-coded to `/`, `/roster`, `/game-plan`, and `/week-advance`, with the More drawer built from `NAV_GROUPS` and `NAV_ITEMS`. Engine progressive unlock helpers exist in `packages/engine/src/config/navigation.ts` and are exported through `@mfd/engine/config`; the rule table is reconciled to every current primary `NAV_ITEMS` path/label, preserves existing Week 4, midseason, and phase gates, omits `/briefing` plus direct-only routes, and is still not consumed by `App.tsx` for shell hiding. `/dynasty` route coaching resolves to anchored `dynasty-save-load`; `/settings` resolves to anchored `settings`. Treat shell lifecycle cues as UI only unless source says otherwise: `AutosaveToast` is a week-change cue using `resolveAutosaveToastStep`, save reminders navigate to `/dynasty` without recording export receipts, and the post-setup `ErrorBoundary` passively displays normalized current route/save version/last five `window.__MFD_DEV_LOGS__` entries. App lifecycle guardrails now source-test `RootLayout` shortcut registration/cleanup, shell overlays, audio queue handling, save reminders, command source ownership, autosave toast baseline ownership, and sidecar-sync ownership, plus `useGlobalKeyboard` document listener cleanup.
- League analytics/read models: keep standings, analytics, Stat Central, saved power rankings, media-cycle power rankings, and League Pulse as derived read models unless a real historical receipt is needed. Standings use a simple win-pct/point-differential/team-id sort and rule-aware season/playoff helpers; the route now derives displayed seed count and seed-bubble signals from playoff-picture output. Analytics formulas, stat leader caps, current-vs-history Stat Central behavior, media-cycle ranking score components, and League Pulse tier thresholds are source-owned balance/read-model contracts; update focused engine/UI tests and docs if changing them. Do not create records, news, social posts, or rankings from route render paths.
- First-run setup: preserve the setup gate, `PHASE_ORDER`, setup sidecar keys, full-vs-fast-lane mode distinction, AGM/setup content aliases, day-one narrative pack ownership, coach/scout materialization into staff mirrors, and Chip setup stage actions together. `day-one-narrative.ts` is deterministic presentation/read-model data used by `FranchiseSetupWizard`, `ChooseAGMPhase`, intel support copy, and fast-lane diagnosis; committed choices still flow through `applySetupDecision` and `finalizeSetup`. `DayOneBetLedger` is UI-local summary state, not separately saved. `NewGameScreen` now persists the selected New Dynasty setup mode immediately before `actions.newGame`: Full Setup keeps the seeded setup state, while unlocked Fast Lane calls `createFastLaneSetupState` and writes `mfd:setup-run-mode = fast_lane`. The wizard still defaults a missing/invalid mode to mounted `full`, and `finalizeSetupRun` marks `mfd:first-ten-completed` for full/missing/invalid mode while still skipping explicit `fast_lane`. `franchise-setup.test.ts` guards the exported fast-lane shape, `setupPersistence.test.ts` guards full-vs-fast-lane marker semantics, `day-one-narrative.test.ts` guards pack determinism/recommendations/opener context, `stageActionRegistry.test.ts` guards every engine setup phase to wizard-stage action mapping, and `NewGameScreen` tests guard locked/unlocked setup-path UI plus scenario bypass; update these deliberately if setup phases, spotlight targets, Chip first-run beats, or launch modes change.
- Franchise setup components: phase components are stateless UI over engine read models and wizard/store callbacks. `ChoiceDeltaBadges` displays `ChoiceForecastPreview` output and should not own forecast formulas; `HireScoutPhase` reads scout candidates/AGM reactions but delegates selection; `TeachingTooltip` is local tip-index state; and `PhaseTransitionOverlay` is transient copy/animation, not setup advancement. Keep commit logic in `applySetupDecision` / `completeSetup` / `finalizeSetup`.
- Small helper boundaries: `dynastyIndicator.ts` is Chip Dock label formatting only, `onboardingReveal.ts` is deterministic reveal animation state, `game-store.test-helpers.ts` is intentionally non-suite store-test infrastructure, and `coachArchetypeGlyphSvg.tsx` is a `/coaching/tree` visual helper rather than an engine coach-taxonomy owner.
- Scenarios/franchise business: `scenario-challenge.ts` owns code-defined scenario definitions, start mutations, progress/grading, season advancement, `getScenarioConstraints`, and `getScenarioConstraintCoverage`. Current scenarios are `rebuild`, `cap_hell`, `dynasty_or_bust`, `expansion`, and `the_savant`; only `the_savant` currently blocks trades/free agency, all shipped scenarios leave `blockDraft: false`, and none set `forcedDifficulty`. Scenario `difficulty` is catalog/display metadata unless `forcedDifficulty` is set; scenario display difficulty uses `all_pro`/`hall_of_fame`, while saved `GameState.difficulty` uses `allpro`/`legend`. `startScenario(..., _rng)` currently accepts but does not consume RNG, so setup is deterministic from base state and scenario id; web launches still create a fresh `Date.now()` seed for new saves. New seeded games leave `scenarioState` undefined; save schema accepts optional `scenarioState` and defaults missing constraint booleans to false. Scenario completion grades are unique by scenario id and scored 60 primary / 20 bonus / 20 efficiency. Current blocker enforcement is route-specific: generated trade-market offers/acceptance, direct proposal creation/submission/counter acceptance, accepted user deadline offers, and draft-war-room trade offers check `blockTrades`; user draft-pick submissions check `blockDraft`; and free-agent bid/street signing, waiver claims, and practice-squad acquisitions check `blockFreeAgency`. `/scenarios`, `NewGameScreen` scenario-launch cards, and Monday Briefing `ActionCenter` now use `getScenarioConstraintCoverage` for source-backed coverage copy/badges/notices; active `/scenarios` also renders a Blocked Action Guide from enforced coverage items. If changing scenario constraints, test actual engine/store/UI paths, not only `getScenarioConstraints` or `getScenarioConstraintCoverage`. Stadium/relocation/expansion work is also persistence-sensitive: naming-rights revenue pays during offseason refresh, relocation eligibility is enforced by selector/UI plus a store-side `canRelocate` and selected-cost re-check before the engine mutation, relocation preserves `team.id` while syncing roster players back into `game.players`, and expansion pauses offseason advancement until the draft is finalized into a new non-user team plus rebuilt schedule.
- UI/design system: preserve shared UI/design-system patterns, `data-mfd-*`, `data-nav`, keyboard/focus behavior, mobile layout, reduced-motion expectations, reusable command-palette category order, the UI preference persisted-vs-transient field split, malformed-preference fallback behavior, browser-local watch-list sidecar semantics, generated Inbox read-state boundaries, and saved home-dashboard layout semantics. `@mfd/design-system` exports only `./components` and `./tokens`; shared components must stay app-agnostic and should not import stores, route helpers, content resolvers, or save/game logic. `PixelTable` owns TanStack sorting plus token-CSS card mode through `data-mfd-table-*`; `MfdCommandPalette` is reusable UI while app command items come from `NAV_ITEMS`, capped `selectRoster` player commands, and `Advance Week`; `RootLayout` now passes `Search screens, actions, roster players...` so app copy matches the current source. Team commands should wait for a clear team-detail destination. `RootLayout` owns the inline app `commandItems` source and `Cmd/Ctrl+K` toggling, passes `globalShortcutEnabled={false}` into the reusable palette, and `nav-items.test.ts` guards the screen/player/action sources plus absence of app `team` command categories. Chip pose/art/motion maps live in the shared component but PNG assets live under `apps/web/public/assets/chip`; `Spotlight` mutates DOM targets by ref or `data-spotlight-target`; `pixelUi.tsx` is the app bridge for hash navigation, team colors, and command callouts. Browser-local watch-list `updatedAt` is one board-level metadata stamp; stale/missing IDs are rendered for recovery instead of auto-pruned, and `/watch-list` now labels this source boundary in `Watch List Sources`; do not redo it as a display slice. `/inbox` opening state is local display state today, not a durable read-receipt sidecar or `ownerPersonalityInbox` consumer; inbox messages are pure selector projections returned in builder insertion order, `read` is a regenerated display hint, action-required messages only get CTAs when `message.link` or the Film Room/Prep Desk/Ownership sender fallback applies, and urgent/decision counts are route-local header/filter counts rather than shell badges. `/inbox` now labels these source and shell-badge boundaries in `Inbox Sources`, and `ApologyTourModal` labels its saved-thread/authored-beat/local-replay/delivery-ledger boundary in `Tour Sources`; do not redo either as display slices. `game.dashboardState` stores layout ids/names/widget ids/columns plus pinned widget ids; `MondayBriefing.tsx` owns widget labels/rendering/local drafts, while `dashboard-config.ts` owns defaults, slug replacement, dedupe, and reorder filtering. `/` now labels this source boundary in `Briefing Sources`; do not redo it as a display slice. Use focused component tests for touched surfaces.
- Design-system inventory cautions: Chip's public 36-pose surface is PNG-first through `CHIP_POSES`, `CHIP_POSE_ART`, `CHIP_POSE_MOTION_PROFILE`, and `resolveChipPoseArt`; SVG fragments under `components/Chip/poses` are fallback/vector renderers, and `TelestratorX` is only a tiny SVG prop fragment. `PixelEkg.stories.tsx` is Storybook-only visual sample data, not runtime EKG semantics or a test gate.
- Content: authored JSON is live only after loader import, schema/key validation, helper/export ownership, runtime consumption, tests, and docs. `packages/content/stadiums` now has `.gitkeep` plus loader-owned `atl-orchard.json`, `bal-crab-pot.json`, `bos-kettle.json`, `chi-deep-freeze.json`, `cin-sty.json`, `cle-power-chord.json`, `dal-corral.json`, `den-bull-market.json`, `det-bass-drop.json`, `kc-smokehouse.json`, `nyc-meter.json`, `phi-liberty-bell.json`, `pit-furnace.json`, `sea-feedback.json`, and `sf-mother-dough.json`; standalone stadium content wins by team id before embedded team stadium fallback, broadcast pregame consumes standalone cue lines, and weather/relocation/naming-rights/PA stay separate. Direct JSON imports are quarantined exceptions in `content-schemas.test.ts`, while `narrative/revenge-lines.json` is loader-owned and consumed by broadcast commentary, including win-gated postgame newsline recap texture; no authored JSON is currently classified as dormant. Public assets are separate web/design-system contracts: Chip art is mapped by the design-system 36-pose atlas with full/inline PNG conventions, TeamLogo reads `logos/${icon}.png` and hides missing images, audio `.ogg` requests are sanitized by `AudioController`, and active PWA manifest/screenshots live under `apps/web/public`, not root `public`.
- Content inventory cautions: `CODEX_GAME_GUIDE.md` lists the current 32 `packages/content/teams/*.json` filenames. Adding/renaming one needs the file, static `content-loader.ts` import/list entry, schema/load tests, runtime consumers, and doc sync.
- Engine exports: browser runtime imports should use exported `@mfd/engine` surfaces or documented subpaths. `packages/engine/package.json` exports source barrels for `.`, `./rng`, `./types`, `./events`, `./config`, and `./save`; `main`/`types` still point at `dist` for historical package-build output. Web tsconfig/Vite aliases resolve into source for workspace builds, but that wildcard is not permission to import private `systems/*` modules. `architecture-boundaries.test.ts` scans browser runtime code for private engine imports, keeps direct `advanceFranchiseWeek` / `previewHalftimeDecision` behind `store/sim.ts`, bars production companion code from engine live-event envelope symbols even though `@mfd/engine/events` is exported, and now requires production engine modules to stay browser-API-free. Avoid private engine source imports in app code unless the guard is deliberately updated with docs/tests.
- Acquisition/cap/trade/FA: keep previews pure, movement through engine/store helpers, roster/player/free-agent/team cap totals synchronized, and route ownership distinct (`/contracts`, `/cap-lab`, `/front-office`, trade routes, waiver/practice-squad/team-needs routes). Cap rule-awareness for the main display/read-model paths is shipped: cap visualization, extension projection, dynasty-window scoring, setup cap/window briefings, Monday Briefing, `selectCapProjection`, `/contracts`, `/compare`, and `/front-office` pass `GameState` or show active cap context, and `architecture-boundaries.test.ts` now guards against production year-only `getSalaryCap`/`getCapFloor` calls outside seed-time roster generation. `/front-office` also labels Contract Tool Sources for roster source, pure preview helpers, salary-cap rule context, and commit boundary. Seed-time salary scaling remains default-only unless a deliberate new-save/balance slice changes it. Player-agent state is save/weekly-loop sensitive: `game.agents`, `Player.agentId`, and `offseasonState.reSignDecisions` are serialized; `submitReSignOffer`/`negotiateContract` stages accepted/countered/declined negotiation state; accepted user deals are applied during `advanceOffseason`; unresolved user expiring players with an offer can carry holdout counters in `careerStats`; and `franchise-week.ts` processes user-team holdouts with `RNG.ai`, possible agent media leaks, and eventual walk-to-free-agency behavior. Franchise-tag web/store wiring is shipped: `franchise-tag.ts` owns tag types/salary/application; `/contracts` reads active `franchise_tag_limit` and `tag_types_allowed`, previews tender salary, and calls `actions.applyFranchiseTag`; the store clones current state, rejects ineligible/duplicate tags, calls the engine helper with `GameState`, mirrors roster/player-map references, refreshes cap totals, snapshots undo, and autosaves; `team.franchiseTags` is the newer optional array, `team.franchiseTag973` is the legacy mirror, migration v19 backfills arrays, `ContractSchema` validates `contract.franchiseTag`, `TeamPersistedSchema` passes team fields through, and `labor-relations.ts` counts every `franchiseTags` entry with legacy fallback. Comp picks are normal `DraftPick` records with `isCompPick`; `calculateCompPicks` runs after free-agency round 3, rebuilds current-year comp picks from team `LOSE_FA` minus `SIGN_FA` logs, honors active `comp_pick_limit`, and feeds `/draft`, news, inbox, and achievement surfaces from saved `team.draftPicks`. Conditional picks are top-level saved `game.conditionalPicks` with schema/default/migration coverage; they resolve during `advanceOffseason`; generated market offers can value/transfer unresolved conditional picks through `trade-market.ts`; and direct proposals/counters can expose, value, and transfer unresolved owned conditional picks through `getTradeableAssets`, `getTradeTargets`, and `trade-negotiation.ts`, with Trade Center labeling generated and direct support. Accepted generated offers, accepted direct proposals/counters, and finalized completed deadline deals call shared `syncTeamCapTotals(game, team)` for both involved teams after movement, recomputing `capUsed` from post-trade roster contract hits plus `deadCap` and `capSpace` from rule-aware `getSalaryCap(game.year, game)`; finalized completed deadline player moves also call `syncPlayerArchiveEntry` so player archive team-history stints capture the buyer. `/cba` now labels `comp_pick_limit` outside CBA term projection while Commissioner petitions can still target it as a petitionable league rule. Deadline AI deal compensation derives from current dynasty draft-pick inventory, not launch-year 2026. Generated offers, direct proposals, Trade Center closure/read-model copy, and Inbox reminders honor configured `trade_deadline_week`; `/trades` also displays the active rule value in Trade Window detail. Practice Squad UI, roster-limit enforcement, street free-agent signing sync, the "Full House" achievement, and Roster Management presentation already use active/custom league rules or synchronized roster/player/free-agent/cap state: successful `signStreetFreeAgent` now sets roster/global `teamId`, contract team, cap used/space, jersey/locker-room state, archive stint, and free-agent removal while still avoiding `SIGN_FA` comp-pick logs. Waiver/practice-squad clarity and awarded-contract repair are shipped: `selectWaiverWireBoard` exposes contract labels, pending/open claim state, clearance copy, action eligibility, lifecycle notes, and Scenario Lock copy; contractless awarded claims now receive a one-year minimum deal from `getMinSalary(player.yearsExp)`, existing claimed contracts update to the winning team id, and claimant cap totals are synchronized. Elevated practice-squad return refresh is shipped: `returnElevatedPlayers` refreshes roster/locker-room state after returned elevations, and focused engine coverage proves stale captain/tension rows tied to a returned elevation are cleared. Waiver claim-results scope clarity is shipped: `selectClaimResults` still scopes claim wins/losses to the user team, but cleared waiver rows carry a `League-wide clearance` qualifier because saved clearance entries are not team-specific, and `/waivers` renders that qualifier in Claim Results. Near-miss receipts are saved/returned engine receipts now rendered by `/season/recap` when saved `game.seasonNearMissReceipts` exists: direct user proposal rejections, passed-pick draft flow, and user free-agent bids that lose to signed CPU offers can feed `nearMissTracker`; season rollover writes latest receipts and deletes the tracker; and the web route does not consume transient `EngineOutput.nearMissReceipts` directly. Trade reputation read models are split: `trust-aging.ts` summarizes `tradeState.gmTrustByTeam`/recent-trade classifications, while `gm-reputation.ts` has its own transaction/reputation model; neither moves assets or changes acceptance by itself.
- Inbox comp-pick receipt follow-up: `InboxTriage` passes active `comp_pick_limit` into `buildInboxMessages`, and generated `Comp Picks Awarded` receipts show that active limit alongside saved `isCompPick` rows without changing awards, read receipts, save shape, movement, sim math, or RNG.
- Endorsement schema tightening - shipped: `PlayerSchema.endorsements` and top-level `game.endorsementOffers` parse through typed `EndorsementDealSchema` / `EndorsementRequirementSchema`, malformed legacy rows are sanitized, and v18 migration defaults missing `active` flags. No `SAVE_VERSION` bump, offer generation/acceptance, revenue, morale, sim, RNG, UI, or movement changed.
- Supersession note for the media/events boundary below: any older statement that endorsement arrays are still `z.array(z.any())` is stale for `PlayerSchema.endorsements` and top-level `endorsementOffers`; team roster shape is still behind the broader passthrough team save boundary.
- Free-agency scenario-lock route clarity: `/free-agency` now renders Scenario Lock copy when saved `blockFreeAgency` is active, disables open-market bid and street-signing buttons, and keeps in-house re-sign negotiation plus market advancement available because those are not external free-agent additions.
- Team-needs/free-agency/game-day route clarity: `/team-needs` now labels saved `teamNeedsCache` versus derived reports, planning-only target lanes, scenario-lock planning copy, selected-team CPU Intent, local comparison-team intent filters sourced from saved `team.philosophy` plus canonical `team.gmStrategy`, and `Strategy History` from saved `game.eventLog` / `game.leagueNews` rows; cap posture, cache-aware needs, and roster trade-block flags stay display-only. `/fa-targets` now labels saved `faTargetBoard` snapshots, refresh behavior, watchlist-only toggles, `/free-agency` as the real bid/signing owner, and planning-only Scenario Lock copy when saved `blockFreeAgency` is active. `/free-agency` also labels saved `freeAgencyBids` won/lost rows in a read-only `Bid Resolution Summary` after round resolution. `/game-day` now labels saved `GameDayPackage` postgame receipt inputs without rerunning packages or game outcomes.
- Scouting/draft board/war room: read "Scouting, Draft Board, War Room, And Draft Recap Wiring" before touching prospect intel, private workouts, pro days, war-room trades, pick reveals, or recaps. Saved draft/scouting state is split across `game.draftClass`, `offseasonState.scoutingState`, `offseasonState.scoutingWatchlist`, `offseasonState.draftOrder`, `offseasonState.currentDraftPickIndex`, `offseasonState.completedDraftPickIds`, `game.scoutingDepartment`, `game.warRoomState`, `game.draftRecaps`, team `draftPicks`, and near-miss state; browser-local `WatchListPinButton` pins use `mfd.watchlist.v1` and are not the same as saved scouting watchlist or FA target watchlist. `ScoutingBoard` now labels these sources in `Scouting Sources` and shows route-local Scouting Action Receipts after existing scouting store actions resolve; those receipts are not durable history. `selectWarRoomState` returns stored war-room state only when it matches the current pick/team; otherwise it derives a deterministic preview with `buildDraftWarRoomState`. `DraftBoard` labels these sources in `Draft Board Sources`, renders read-only Pick Forecast rows before the draft button, renders read-only `Draft Market Receipt` rows for incoming real-backed war-room offers, advances CPU picks through `advanceWeek`/`advanceDraft`, stores pick reveal and Draft Pick Receipt UI locally after `makeDraftPick`, and uses `makeDraftPick` for real user picks. `DraftRecap` reads saved recaps, renders player profile links from saved recap pick ids, joins selected saved recap picks to current `game.players` / `selectTeams` for class follow-through, and does not create recap rows or player-history rows. Generated war-room offers now require live source-backed picks and owned future sweeteners; accepted live war-room offers update team `draftPicks` and live `offseasonState.draftOrder` owner/id entries through `acceptDraftTradeOffer -> applyDraftTradeOffer`; stale/no-op accepts do not commit. React should not duplicate draft class generation, pick mutation, rookie creation, news, dynasty events, press conferences, offer generation, war-room trade acceptance, rejection, recap grading, or player-history writes.
- Offseason calendar: the read-only `/week-advance` panel now exists through `selectOffseasonCalendar`; it reads existing phase/offseason/draft/training/CBA/expansion state and routes to existing owners only. There is still no separate persisted calendar object. Re-signing stages decisions until `advanceOffseason`; FA is three `advanceFreeAgency` rounds; street free agency is immediate signing; draft CPU picks advance through `advanceWeek`/`advanceDraft`; `post_draft` uses `finalizePostDraft`; `training_camp -> preseason` runs `runAllTrainingCamps`; and `preseason -> regular_season` starts the season without simulating Week 1. CBA/labor and expansion can interrupt this flow and surface paused CTAs to `/cba` or `/expansion-draft`.
- Roster/development/health/team ops: keep `GameState.players` and `Team.roster` synchronized. Depth-chart starter status is currently a loose `Player.isStarter` flag: `setStarter` now clones and commits `Team.roster` starter flags, mirrors `GameState.players`, and completes tutorial progress on promotes, but it still does not enforce legal lineup counts, update roles, or snapshot undo. `/depth-chart` labels this source boundary in `Depth Chart Sources`; do not redo it as a display slice. Shell/Monday/Week Advance warnings use coarse 22-starter counts, while Chip uses nine-room missing-depth checks. Preserve facility/medical/training-camp/alumni-mentor ownership boundaries and phase gates. `Settings` now shows read-only facility Upgrade Forecast rows from saved effects and `getFacilityLevelEffect`, medical Hiring Forecast rows from saved current staff/candidate pool/phase gate, and route-local Operations Action Receipts after enabled `upgradeFacility` / `hireMedicalStaff` commits; `upgradeFacility` and `hireMedicalStaff` remain the real commit paths. Active alumni mentors are top-level user-team development modifiers today; do not claim or add CPU-team mentor effects without explicit progression tests. Locker room is saved and sim-affecting: clique/captain constants, culture-score math, weekly regular-season/playoff updates, preseason/post-draft reinitialization, and culture/mentor/clutch OVR bonuses all live in `locker-room.ts` plus `franchise-week.ts`; store meetings/rallies/captain assignment commit state and now return route-local receipts after commits, while `TeamPersistedSchema` still passthroughs locker-room shape. `/locker-room` labels this source boundary in `Locker Room Sources` and shows the transient meeting/rally receipts; do not redo it as a display slice. `trust-aging.ts` also owns live rating-regression multipliers consumed by `progression.ts`; treat `getAgingMultiplier` changes as player-development balance math, not copy polish, and do not confuse trade-trust snapshots with player locker-room trust. Player profiles are read models from `player-profile.ts`; durable identity/history comes from `playerSeasonHistory`, `playerRivalries`, `relationships`, `bloodline`, `playerArchive`, awards, records, and milestones. `Player.devSnapshots` is in-memory-only. `/player-development` now uses route-local selected-player state over the current user roster, feeds the selected roster player into the existing generated report, three-year `projectDevelopmentCurve`, breakout candidates, and report-owned coach-impact narrative, and renders `Development Sources` no-write copy. It still does not run progression, spend training-camp work, mutate archetypes, or write durable development history. Player rivalries affect simulation through `buildPlayerRivalryContext` / `getRivalryGameBonus`, while bloodlines are assigned to draft prospects from archived/HOF parents, copied to drafted players, and surfaced through family relationship edges; `/legacy/bloodlines` now labels that selector/read-only route boundary. `ComparePlayersModal` calls engine comparables/projection helpers directly and should stay read-only unless a real store/engine mutation path is added.
- Game plan/prep/trick plays/special teams: preserve legacy contingency support. `/game-plan` display can generate opponent reports on demand, but reports become durable only when save actions or `franchise-week.ts` upsert them; current opponent reports are keyed by opponent/year/week, not user-team/opponent tuples. Saved weekly prep is current only when year/week/opponent match, stale prep rows can remain hidden, and week resolution clears `gamePlan` plus the current user's prep row while prep/film histories are capped at 24. `saveWeeklyPrepPlan` derives a saved `GamePlan` through store-local focus-to-scheme mappings and only fills key matchup when both the selected user player and first report key player exist, while Call Your Shot is persisted separately through `setCallYourShot` before week advance. `/film-room` renders saved reports and does not generate reports itself. Call Your Shot UI should pass live `selectUpcomingRivalry` context into `isCallYourShotEligible`; Week 15+ is late-season eligibility, not a rivalry proxy. Selected trick plays are now explicitly labeled as planned weekly-prep ids, not live sim execution; do not wire them into simulation without deliberately updating the existing non-wiring guard with balance coverage. Preserve special-teams ownership: user returners commit through Depth Chart/store actions, CPU teams auto-assign before regular-season simulation, offseason rebuilds every team's special-teams state, and snap-count allocation mutates `player.careerStats.snaps` through week/playoff advancement.
- Governance/owner/coaching/AI: keep `team.staff` and `team.coachingStaff` mirrors synchronized. Distinguish advisory AGM/front-office copy from committed effects, selector read models from commit paths, and direct store mutations from autosaved actions. `schedule_weeks` and `playoff_seeds_per_conf` are live in schedule generation, week advance, playoff bracket timing/size, and standings playoff-picture/clinch math. Year-sensitive rule consumers should use `getActiveRule(..., game.year)`/`getRuleValueForYear`, because `rules.entries[key].value` can hold a future-effective value. CBA deals project to revenue/cap/tag/roster/practice-squad/IR/playoff/draft-round rules, not schedule weeks, trade deadline, overtime, or comp-pick limit. CBA abstain persistence and commissioner-ruling persistence are schema-compatible; CPU CBA neutral scores now record as `abstain`, and failed CBA votes still clear proposal/vote maps after writing vote-summary news. `selectCoachingMarket` can lazily rebuild a stale market view without mutating saved `game.coachingMarket`; `refreshCoachingMarket` and staff actions commit it. Offseason carousel now mirrors CPU HC hires into both `team.staff.hc` and `team.coachingStaff.hc`, and normal user/open-slot staff moves go through mirror-writing helpers. Coaching modifiers are split: trait mods are live sim math, head-coach skill selections feed season-end coach development, optional position coaches feed progression/reporting, and coordinator specialties feed chemistry/scheme-fit. Position coaches are optional `team.positionCoaches` state; `/coaching` manages this field through explicit `initializePositionCoachesForTeam` and `upgradePositionCoachRole` store actions that write only `team.positionCoaches`, and championship rollover advances existing saved position-coach tenure through `advancePositionCoachSeason`, while poaching/history for position coaches remains future lifecycle work. Championship rollover calls `advanceSeasonEndCoaching` before year increment: `advanceCoachDevelopment` advances one active coach per staffed team from record/clinic/skill-tree inputs, position-coach tenure advances for already saved position-coach rooms, and `resolvePoachingCycle(nextState, RNG.ai)` can remove OC/DC staff, clear matching legacy mirrors, and write departure news/events. `addClinicXP` now accepts direct `/coaching` track ids and autosaves successful clinic XP commits through `commitGame`; unknown clinic actions remain no-op/no-autosave. Adaptive difficulty is saved gameplay state: `difficultyState` has schema/default/migration coverage, `advanceFranchiseWeek` applies `getAdaptiveModifier` only to non-user team OVR context through `buildTeamOvrBonus`, then updates streak/slider after user-game wins/losses; disabled mode still tracks recent results but resets the slider to 50 and skips adjustment history. Keep `league-rules.test.ts`, `cba-engine.test.ts`, `commissioner.test.ts`, `labor-relations.test.ts`, `owner-extended.test.ts`, `owner-personality.test.ts`, `adaptive-difficulty.test.ts`, coaching, reputation, and specialty tests green when changing those systems.
- Governance persistence: do not conflate commissioner rule-vote abstain with CBA owner-vote abstain. Rule votes support user abstain through type/schema/UI/store, but engine `castVote` and AI vote simulation are yes/no only. CBA owner votes support abstain through type/schema/UI/store, and CPU owner votes can abstain only when `cbaOwnerVoteForTeam` scores a proposal exactly neutral; threshold counting is store-local, and failed-vote state is usually cleared after generated news records the approve-reject-abstain summary. Non-empty commissioner rulings now serialize through `CommissionerRulingSchema` using `playerName`, `rationale`, `chemistryImpact`, and `ownerApprovalImpact`; schema parsing and the v35 import migration normalize legacy `description` / `approvalImpact` rows across `commissionerState.rulings` and `commissionerDisciplineLog`, defaulting missing `playerName` to `''` and missing `chemistryImpact` to `0`.
- CPU identity: `gm-strategies.ts` mutates saved `team.gmStrategy` and player trade-block flags; `ai-philosophy.ts` mutates saved `team.philosophy` from recent franchise history, cap stress, and roster age during offseason, and only applies GM strategy for non-maintain philosophies. A `maintain` philosophy write does not reset prior `gmStrategy` or clear existing trade-block flags, so UI labels/filters should display both fields or deliberately normalize the split with tests. Trade value/market, free-agency AI bids, draft-war-room trade offers, team-needs reports, FA target-board rows, deadline completed deals, and saved `tradeSuggestions` consume these fields in different ways. `AIBiasConfig` is optional playtest/harness bias, not saved dynasty identity. `teamNeedsCache` is saved; the web store clears stale reports on commit when roster-room or cap-space inputs change, while engine-only mutation paths still need explicit cache coordination. `/team-needs` displays selected-team CPU Intent, renders an `Intent Ledger` from saved philosophy/strategy, cap, critical needs, and trade-block flags, and filters comparison teams locally from these saved fields, but it does not run the AI helpers or refresh the cache. `/trades` renders read-only `Generated Offer Receipt` rows for saved user-team `tradeSuggestions`, `/fa-targets` renders read-only `FA Market Receipt` rows for saved target-board rows, `/draft` renders read-only `Draft Market Receipt` rows for incoming war-room offers, `/trade-deadline` renders read-only `Deadline Deal Receipt` rows for saved completed deadline deals, and `/trade-block` carries saved strategy/philosophy into read-only entries, filters them locally as Sellers/Buyers/Neutral, and renders per-target `Market Receipt` copy from existing selector row fields. `findTradeTargets`, Trade Finder rows, FA target-board rows, draft war-room market receipts, league trade-block views, and deadline completed-deal receipts are advisory/read-model surfaces, not proposal commits. Convention saves and imported/current cartridges now normalize persisted strategies to canonical `contend`/`rebuild`/`neutral` ids without applying roster trade-block side effects.
- Owner mandates/promises: `owner-goals.ts` owns setup mandates and mirrors them into `game.handshakes`; `evaluateOwnerMandates` applies owner approval, patience, owner-reputation, and AGM-impact consequences, while `evaluateHandshakes` maps `owner_mandate` mirrors from mandate `met`/`exceeded`/`missed` status and skips normal handshake deltas. Unevaluated mandate mirrors stay active instead of expiring from Week 18 alone, so preserve the `evaluateOwnerMandates` before `evaluateHandshakes` ordering. Player promises are separate committed handshakes with morale/chemistry/reputation effects; normal owner demands can expire with a smaller owner penalty, and no-trade promises can break immediately when the player leaves the team. `ownerMandates` is optional in the TypeScript type but schema-defaulted and v36-migrated, so use nullish fallbacks unless doing a save-shape slice; `/owner` and `/handshakes` now label these boundaries. Preseason generic owner demands are skipped for teams that already have a current-year owner handshake.
- Dynasty memory/rivalries/archives: distinguish saved league rivalries, saved player rivalries, content-declared rivalries, relationship graph rival edges, derived browser-local rivalry heat, narrative hooks, coach history, dynasty-window read models, franchise doctrines, Franchise Book hooks, and browser sidecars before changing sim boosts, hook priority, game-day package stakes, drama tags, or heat-map UI. FranchiseHub meters mix sources and now label them on `/franchise`: Homegrown is current roster plus `playerArchive` first-team history, Continuity is current starter flags plus browser-local `mfd.rosterContinuity.v1` keyed by `deriveDynastyId` and not cartridge state, and `RivalryHeatMap` is authored team rivalry content plus runtime lookup with W-L-T, total games, and latest meeting derived from saved completed schedule/playoff results. Pure `deriveRivalries`/`RivalryPayload` stay in engine, while `apps/web/src/lib/rivalry-storage.ts`, `apps/web/src/app/rivalry-rollover.ts`, `game-store.ts`, and `dynasty-sidecar-archive.ts` own the browser-local `mfd.rivalries.v1` lifecycle/export; it is current-save metadata, not durable all-time head-to-head history or `.mfd` cartridge data. Legacy rivalry playoff boundaries and expanded Week 19 regular-season drama tags should follow `getRegularSeasonWeekCount`/`isRegularSeason`, not fixed Week 18/19 assumptions. `/coaching/relationships` labels that it reads saved `game.relationships` only, does not infer graph edges from staff lineage fields, and keeps empty selector fallbacks stable for old/demo saves. Revenge-game flavor is player-archive driven today; saved `coachingHistory` does not synthesize coach revenge until deliberately wired. Doctrine helpers define 12 doctrines, but inspected live awarding only covers four playoff-rollover cases. Franchise Book chapter-alert wiring is shipped: playoff champion rollover calls `fireFranchiseBookChapterAlerts` after `archiveSeasonHistory` and before year increment to write deduped chapter-start `leagueNews` milestones for new non-Chapter-1 chapters. Keep `hooks-engine.test.ts`, `franchise-doctrines.test.ts`, `franchise-book-hook.test.ts`, and `franchise-week.test.ts` green when changing narrative or dynasty-memory generation.
- Season recap / era UI: `/season/recap` is a read-only `buildSeasonRecap` surface; PNG/text export is browser-only through `recap-share.ts`, while `export-frame.ts` is tested hidden-DOM export-shell scaffolding that the current recap screen does not call. Keep detected franchise-dashboard eras separate from user-named dynasty eras: `EraTransitionEmitter` is a session-only overlay over `selectFranchiseEras` / `detectFranchiseEras` and now renders staged `EraTransitionReveal` production reveals without saving or sidecar writes, while `DynastyEraPrompt` commits `userDynastyEras` and `team.era` through `actions.nameDynastyEra` / `startDynastyEra`.
- Franchise/legacy presentation: Dynasty Chronicle filters/detail modals are route-local UI over web-lib presenters; Chronicle export uses `createExportFrame`, while Season Recap currently captures its card directly. Rookie of the Year history reads browser-local `mfd.rookieOfYear.v1` through `readRookieOfYearEntries(dynastyId)` and labels the rollover/no-write boundary, Named Games reads saved `dynastyTimeline` named-game events through `selectNamedGames`, Jersey Retirement is read-only over saved retired-jersey/farewell-tour selectors, and franchise SVG helpers are store-free app presentation utilities, not design-system exports or state owners.
- Legacy/celebrations: distinguish saved ceremonies, Super Bowl presentation parade data, app-shell `ChampionshipParadeEmitter`, scrapbook playoff-lore cards and named-game notable moments, achievement toasts, season reports, durable archive routes, and app-shell `MilestoneCard` threshold overlays. The parade emitter is session-only, ref-suppressed, storage-free, and should not be treated as a saved ceremony or sidecar unless deliberately redesigned. `/super-bowl` has a separate route-local user-champion celebration dismissed through `mfd-celebration-dismissed:<champion.id>:<year>`. Scrapbook pending playoff-lore cards are sidecar buckets keyed by dynasty and season; dismissing `PlayoffLorePrompt` clears only transient `pendingPlayoffLoreReveal`, archiving one season consumes only that bucket, and new-game scrapbook clearing also clears pending lore for that dynasty. Saved `dynastyTimeline` named games become season scrapbook notable moments only when `buildScrapbookEntry` runs at the existing year-rollover archive path. Playoff-lore card rounds come from matched `playoffBracket.matchups[].round`, not absolute Weeks 19-22. `generateSuperBowlContext` now counts prior titles from canonical lowercase `playoffFinish: 'champion'` rows through a case-insensitive helper and has focused `super-bowl.test.ts` coverage. Hall of Fame ballot persistence/defaulting is shipped: production-shaped saves define `ballotWaitlist`, so `inductHallOfFame` uses deterministic ballot voting, while legacy in-memory callers with undefined waitlists keep the old direct-induction fallback. HOF career epilogues are generated while building HOF entries, accepted by the browser HOF archive sidecar, cartridge-durable through `CareerEpilogueSchema` / `HallOfFameEntrySchema`, displayed by `HallOfFamerDetailModal` when present and displayable, preferred by HOF ceremony/news copy, and reused by Dynasty Chronicle HOF timeline/detail copy when present. `/legacy` can read live saved `game.hallOfFame`, ballot counts, `Ballot Watch` rows, and read-only ballot pressure summary rows from saved `game.ballotWaitlist`, while `/franchise/hall` reads `mfd.hallOfFame.v1` sidecar snapshots, compares live saved count with current snapshot count/last sync, and can explicitly refresh the current dynasty sidecar via `Sync Current Save` without importing sidecar entries into `GameState`. Achievement entries are a saved catalog initialized by `createDefaultAchievements`; `checkAchievements` stamps locked entries with current year/week and is not a durable event queue; `selectNewlyUnlocked` filters only current-stamp achievements; `AchievementConditionSchema` now validates known metric ids; `App.tsx` adds session-local seen keys and timeout state for `AchievementUnlockToast`; locked hidden achievements hide progress only on surfaces that call `getAchievementProgress`; playoff achievement logic depends on `missed_playoffs` / `regular_season` canonical non-playoff strings; "Full House" follows live roster/practice-squad league rules; and `hidden:comeback_kings` unlocks from existing saved comeback named-game dynasty events won by the user team after the schedule-aware playoff boundary, bucketed by event year for one postseason run. The embedded `/legacy` gallery and standalone `/franchise/achievements` route both now use progress/mystery treatment for locked achievements, Monday Briefing has its own optional progress widget, Inbox shows up to two current newly unlocked cards, and app-shell `MilestoneCard` overlays are local UI, not saved `recentMilestones`.
- Chip/share scaffolds: `chipShare.ts` is disabled by default, has no external targets, has no visible production affordance today, and is guarded out of production UI by `architecture-boundaries.test.ts`. If wiring share UI, update that guard deliberately, define a local export/share-card flow first, and keep external posting absent unless real target contracts exist. Chip pending-decision counts are loose-state guidance from `decisionsPending.ts`, not Inbox counts or durable task receipts. Chip "Current board" / Where Am I copy should derive its week denominator from generated `game.schedule.length` via `whereAmI.ts`, falling back to 18, without becoming a parallel league-rule parser. `weeklyGuidance.ts` and `decisionImpact.ts` are presentational guidance helpers; Monday Briefing `ActionCenter` route CTAs and engine `getAGMWeeklyRecommendations` advice should not be treated as committed fixes. Chip dock storage is browser-local: `mfd.chip.local` needs a full valid prefs shape, `mfd.chip.read.v1` stores beat-id receipts, reset-onboarding clears only first-ten receipts plus machine/legacy skip state, and weekly dialogue events honor season-end/game-complete/week-rollover precedence while reading week/season quiet prefs rather than screen/reduced-motion prefs.
- Route coaching/tutorial: route coaching is a four-part web contract: `routeBeatRegistry.ts` owns the 53-key beat catalog, `useActiveRouteBeats.ts` owns route normalization/cache plus first-ten prepending, `ChipDock.tsx` owns display/progress/read-receipt writes, and `spotlightAnchors.test.ts` owns exact DOM anchors for non-null targets. `/awards` resolves to anchored `awards-hub` beats on the Awards Hub header and season scrubber; `/training-camp` resolves to anchored `training-camp` beats on Camp Readiness and Ops Carryover; `/dynasty` resolves to anchored `dynasty-save-load` beats on Portable Backup and the restore grid; `/settings` resolves to anchored `settings` beats on Simulation Preferences and Operations Source; `/mentors` resolves to anchored `mentors` beats on active/budget/effects summary and Network Source. `/locker-room`, `/front-office`, `/endorsements`, `/draft-recap`, `/relocate`, `/commissioner`, `/legends`, dynamic `/player/$playerId` profile/timeline routes, `/player-development`, `/compare`, `/rivalries`, `/league/weather`, `/broadcast`, `/presentation`, `/play-by-play`, `/game-flow`, `/film-room`, `/super-bowl`, `/owner`, `/handshakes`, `/analytics`, `/stat-central`, `/scenarios`, `/game-day`, `/schedule`, `/watch-list`, `/trade-block`, `/team-needs`, `/free-agency`, `/fa-targets`, `/waivers`, and `/practice-squad` resolve to unanchored route-specific keys until a deliberate DOM spotlight slice. `/news` resolves to `league-news`, `/newsroom` to `newsroom`, `/social` to `social-feed`, `/cba` to `cba`, and `/league-rules` to `league-rules`. The legacy tutorial is engine-backed `TutorialState` with separate coverage in `TutorialCoverage.test.tsx`. Adding or renaming routes requires coordinated updates to `App.tsx`/`NAV_ITEMS`, resolver aliases, coached-vs-uncoached route tests, optional spotlight anchors, and tutorial allowlist/steps. Not every nav route should get route coaching; some are explicitly uncoached. `/briefing` is a route-coaching alias for Monday Briefing while the registered app home route and Week 1 tutorial target are `/`; stale serialized `week1-briefing` tutorial rows are normalized by `TutorialStepSchema` during save parsing, and `/briefing` is not listed in `NAV_UNLOCK_RULES`.
- Media/events/newsroom/audio: distinguish live engine envelopes, saved `GameState.eventLog`, web Chip events, media digests, route-level feeds, breaking-news overlays, ghost commentary prefs, HOF `source: 'hof'` ghost lines, contingency `source: 'callout'` booth alerts, and audio cue sidecars. Off-field events are season-local living-world receipts: `franchise-week.ts` gates weekly generation, `off-field-events.ts` writes selected events/effects/eventLog/headlines, `activeEffects` also come from press conferences/rivalry typed sources, `agentMediaLeak` writes `offFieldEvents` outside the off-field module, and only `appliesToGame` OVR effects feed sim bonuses through `getGameEffectBonuses`. Press conferences split durable engine state from web response state: `recordPressConference` writes `recentPressConferences`/effects/headlines/event-log entries, while `postGameUi.pressConferenceQueue` is built from the latest postgame conference plus authored response pools, and `respondToPressConference` only stores `selectedTier`/`selectedResponse` today. Endorsement pending offers live in `game.endorsementOffers`; active/revenue selectors derive from roster player deals; accepting an offer now mutates the roster player when present, writes that player back into `game.players`, and removes only the pending offer. `EndorsementDeal` persistence is typed for `PlayerSchema.endorsements` and top-level `endorsementOffers` through `EndorsementDealSchema` / `EndorsementRequirementSchema`; roster-embedded player shape still follows the broader passthrough team save boundary. Apology-tour threads dedupe by source named-game id, beats are delivered from `beatsDelivered`, and Inbox/modal views are projections rather than durable read-state writers. `/news` loads `features/league-news/LeagueNews`, `/newsroom` loads `features/newsroom/NewsroomDigest`, `/social` uses a source-only newest-first projection over saved `game.socialFeed`, and there is no `features/news` folder today. Shipped source panels now label app-shell `BreakingNewsTicker` as `leagueNews`/`breakingNewsQueue` boundary context, `/newsroom` as saved league-news/rankings/storyline projection, embedded `PowerRankingsTicker` as selector/click/no-write source context, `/news` as saved `leagueNews` plus route-local filters, and `/social` as saved `socialFeed` plus newest-first source-only projection. `/newsroom` StorylineThreadCard renders Thread Receipt rows from saved thread source/timing/beats/lifecycle/no-write boundary, and `HeadlineModal` renders Story Receipt rows from the selected saved `game.leagueNews` item, without advancing, closing, seeding, generating news, appending `leagueNews`, dismissing breaking news, or writing saves. Media-cycle digests append after completed regular-season weeks, then storyline threads advance/close/seed. Current live storyline archetypes are `hot-seat-coach`, `qb-controversy`, `rookie-of-year-chase`, `records-chase`, and `comeback-player`; their triggers are source-owned thresholds and saved metadata is primitive-only, so adding an archetype or richer metadata is type/schema/migration/test work. `/broadcast` can target transient `useUiStore.broadcastGameId`; `/presentation`, `/play-by-play`, and `/game-flow` use latest broadcast selectors. Missing broadcast payloads are deterministic selector rebuilds, not save repair writes; `analyzeGameFlow` is pure presentation math over `BroadcastOutput`, and `PixelEkg` only renders supplied win-probability points. Ghost broadcast prefs default/fail open to enabled and only gate display; HOF/callout ghost lines remain generated, with legacy unsourced `Booth Alert` lines treated as callouts. `GhostBroadcastsPanel` labels `broadcast.ghostLines`, `mfd.broadcast.ghost.v1`, the HOF/callout split, legacy classification, and the read-only no-generate/no-broadcast-write/no-GameState/no-sim/no-RNG boundary. Audio cue queues are saved UI staging state that the app shell plays and clears from an effect, while `AudioController` browser objects remain tab-local; audio assets/synths are category-gated, critical queued cues bypass debounce, and the recent-playback map is capped. Breaking-news ticker stories come from `leagueNews`; full-screen interrupts come from `breakingNewsQueue[0]`, which is regenerated/replaced after week advance, and suppress the ticker until dismissed. Live event envelopes have guarded module-level seq/reset behavior across `createEventLog()` instances. Production companion code is guarded against importing `@mfd/engine/events`; update that guard only with an explicit bridge design.
- Tooling/CI/playtesting: preserve GitHub Pages `/MFD/` pathing, production-equivalent `VITE_CHIP_ENABLED=true`, built-page smoke coverage for `/MFD/` plus `/MFD/#/league/weather`, manual post-setup demo route smoke through `scripts/smoke-test-post-setup-route.mjs`, manual chunk boundaries, Math.random audit expectations, shadow baseline discipline, and host-noise filtering. CI currently runs recursive typecheck/test, Chip-enabled build, bundle-size gate, built-page smoke, Math.random scan, and a save-version printout; it does not run `test:perft`, `test:shadow`, `smoke-full-season`, post-setup route smoke, real `grade-season`, or web `test:e2e` unless workflows are changed. Built-page smoke is a boot/deploy guard; post-setup route smoke launches the convention demo, verifies one or more initialized hash routes through default env vars or `SMOKE_POST_SETUP_ROUTES_JSON`, can opt into a real convention-demo week-advance click through `SMOKE_ADVANCE_WEEK=1`, can opt into a copied-cartridge import restore through `SMOKE_CARTRIDGE_ROUND_TRIP=1`, can opt into a `.mfd` Blob download/upload restore through `SMOKE_CARTRIDGE_FILE_ROUND_TRIP=1`, and can opt into a local manual save-slot load/restore through `SMOKE_LOCAL_SAVE_SLOT_ROUND_TRIP=1` without replacing a golden-path e2e crawl. Classify tooling before trusting it: ESLint is cleanup surfacing except React hook errors, madge/knip are cleanup diagnostics unless promoted to gates, `cleanup-gate.sh` is local cleanup-wave automation with hardcoded current `SAVE_VERSION`, and `age-curve-report` is deterministic progression telemetry that writes parent-workspace `.codex/MFD` artifacts without mutating game state. `test:perft`/`playtest:all` is the fast persona harness; `perf-budget` is stripped as host noise from canonical counts. `test:shadow` compares 5/10/20-year SPEEDRUNNER seed-42 baselines under `_canon/seeds/mfd`, and update mode is a reviewed baseline change, not routine cleanup. `test:grading` only tests helper behavior; real `grade-season` requires judge API keys and baseline files. The default playtest harness uses a synthetic 18-week fixture for benchmark stability, while `detectPhaseBoundaries` now follows `getRegularSeasonWeekCount(context.state)` for generated/configured 17/18/19-week frames. Fixture runs alone are still not proof of every live schedule-rule path. `speedrunner-20y` shadow is a frozen truncated 800-step corpus with existing high-severity anomalies, so a pass means "matches baseline," not "clean 20-season sim."
- Visual e2e and ambient files: `apps/web/e2e/chip-art-v3.pw.cjs` and `apps/web/e2e/chip-art-shell-polish-v2.pw.cjs` are manual Chip PNG layout/readability evidence scripts, not broad gameplay e2e coverage. `apps/web/src/vite-env.d.ts` only references Vite client types plus `__MFD_VERSION__`; `packages/engine/src/global.d.ts` only declares `structuredClone<T>` for the ES2022 TypeScript lib target.

Implementation loop:
1. Create a short TODO with Phase 1 audit/select, Phase 2 implement, Phase 3 verify/docs/next steps.
2. Read the touched source and tests before editing.
3. Implement the smallest complete vertical slice.
4. Add or update focused tests before broadening.
5. Run the narrowest relevant verification immediately.
6. Update `CODEX_GAME_GUIDE.md` for changed ownership/wiring and `CODEX_IMPROVEMENT_PLAN.md` for completed, rejected, or newly discovered slices.
7. Update `CODEX_GOAT_MARATHON_PROMPT.md` only if this handoff prompt becomes stale.
8. Run targeted `rg` checks for stale route names, constants, docs, or copy related to the slice.
9. Record the slice run ledger so a future continuation can resume without re-auditing completed work.
10. If clean, pick the next independent slice and repeat.

Verification matrix:
- Docs-only: structural `rg`/`sed` review is enough; say no package tests were needed.
- Engine system: focused Vitest files first, then `pnpm --filter @mfd/engine test` when shared behavior or sim math changed.
- Save/data: `pnpm --filter @mfd/engine test -- save` plus focused web persistence/sidecar tests when browser storage changes.
- Sim math/RNG: focused deterministic tests, `bash scripts/check-math-random.sh`, and season smoke/playtest when balance risk is material.
- Store actions/selectors: focused `apps/web/src/app/store` tests and affected route/component tests.
- UI route: focused component tests plus route/nav/source tests, tutorial/route-coaching tests if relevant, and browser verification when a local target is feasible.
- Content: content-loader/schema/accessor tests, runtime import smoke, direct-import exception review, and docs.
- Broad frontend: package typecheck/build or focused package equivalents when touching shared UI, routes, app shell, or bundling; use `VITE_CHIP_ENABLED=true pnpm build` plus `bash scripts/smoke-test-built-page.sh` when verifying deploy-equivalent artifacts.
- If a command cannot run, report the exact command, why it could not run, and the signal it would have provided.

Documentation requirements:
- Update `CODEX_GAME_GUIDE.md` when route wiring, save schema, CI gates, content directories/loaders/schemas/helpers, week-advance behavior, store patterns, RNG channels, engine exports, sidecar ownership, design-system conventions, or source ownership changes.
- Update `CODEX_IMPROVEMENT_PLAN.md` after completing a slice, rejecting a slice, changing priority, discovering drift, or identifying a better first implementation path.
- Update this file when the next-window prompt no longer reflects source facts, priority order, candidate slices, verification expectations, or doc-update rules.
- Do not add `CHANGELOG.md` unless the user asks for release notes or repo convention requires it.

Definition of done for this marathon:
- At least one complete vertical slice is implemented and verified, unless a P0 audit reveals a more important docs/tooling correction that must come first.
- Every touched source boundary has a matching test/doc update or a clearly stated reason why not.
- No deterministic sim, save compatibility, route/navigation, browser sidecar, or content-loader risk is left undocumented.
- All touched docs are synchronized.
- Final response uses exactly these sections: Understanding, Plan, Patch, Verification, Risks / Rollback.
- Final response lists what shipped, tests run, tests not run, residual risks, rollback guidance, and the best next slice.

Keep moving with discipline. Ship correct code changes with minimal risk, tight verification, and clear handoff notes.
```

## Companion Notes

- This prompt is intentionally optimized for a fresh Codex window: it forces the next agent to re-confirm source facts, then ship vertical slices from the improvement plan.
- The first visible deliverable in the new window should be a candidate scoreboard, followed by one selected slice and the specific source/tests being inspected.
- The safest default starting point is another read-only forecast/receipt or route/store guard that improves user clarity without touching save schema or sim math.
- The highest-upside risky tracks remain durable all-time rivalry head-to-head history, deeper HOF workflow beyond Ballot Watch pressure summary, and deeper sim/game-plan effects; these require stricter migration, import validation, and deterministic balance verification.

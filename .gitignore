node_modules/
dist/
.turbo/
*.tsbuildinfo
.DS_Store
.env
.env.local
coverage/
playwright-report/
storybook-static/
tmp/

# Local-only Claude/Codex agent state
.claude/settings.local.json
.claude/worktrees/

# Playwright runner artifacts
.playwright-cli/
.playwright-mcp/
test-results/

# Generated playwright screenshots
output/
# Regenerated on every `playwright test ui-overhaul-baseline` run — the numbers
# in geometry.json are the durable artifact, not these captures.
docs/ui-overhaul/evidence/baseline-legacy/*.png

# Corrupt-backup files from iCloud-related editor failures
*.unreadable-*
*.corrupt-backup-*

# 2026-07-10 repo cleanup — keep deleted junk from coming back
.logs/screenshots/
artifacts/
.codex/
*.rtf
package-lock.json
